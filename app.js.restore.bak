// Initialize data/// Initialize data*// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize dataB// Initialize datae// Initialize datav// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data // Initialize data-// Initialize data // Initialize dataA// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datac// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize dataC// Initialize datao// Initialize datar// Initialize datae generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize dataB// Initialize datae// Initialize datav// Initialize datae// Initialize datar// Initialize dataa// Initialize datag// Initialize datae// Initialize data // Initialize dataI// Initialize datan// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datar// Initialize datay// Initialize data // Initialize dataC// Initialize datao// Initialize datan// Initialize datat// Initialize datar// Initialize datao// Initialize datal// Initialize data // Initialize dataS// Initialize datay// Initialize datas// Initialize datat// Initialize datae// Initialize datam generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data*// Initialize data/ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize dataD// Initialize dataA// Initialize dataT// Initialize dataA// Initialize data // Initialize dataL// Initialize dataA// Initialize dataY// Initialize dataE// Initialize dataR// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data // Initialize data=// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data:// Initialize data // Initialize data'// Initialize datab// Initialize datae// Initialize datav// Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data_// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data:// Initialize data // Initialize data'// Initialize datab// Initialize datae// Initialize datav// Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data_// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data:// Initialize data // Initialize data'// Initialize datab// Initialize datae// Initialize datav// Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data_// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize dataD// Initialize dataE// Initialize dataF// Initialize dataA// Initialize dataU// Initialize dataL// Initialize dataT// Initialize data_// Initialize dataC// Initialize dataA// Initialize dataT// Initialize dataE// Initialize dataG// Initialize dataO// Initialize dataR// Initialize dataI// Initialize dataE// Initialize dataS// Initialize data // Initialize data=// Initialize data // Initialize data[ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data1// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataC// Initialize datae// Initialize datar// Initialize datav// Initialize datae// Initialize dataj// Initialize dataa// Initialize datas// Initialize data'// Initialize data,// Initialize data // Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize datað// Initialize dataŸ// Initialize data// Initialize dataº// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize data#// Initialize dataf// Initialize data5// Initialize data9// Initialize datae// Initialize data0// Initialize datab// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data2// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataV// Initialize datai// Initialize datan// Initialize datah// Initialize datao// Initialize datas// Initialize data'// Initialize data,// Initialize data // Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize datað// Initialize dataŸ// Initialize data// Initialize data·// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize data#// Initialize datae// Initialize dataf// Initialize data4// Initialize data4// Initialize data4// Initialize data4// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data3// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataD// Initialize datae// Initialize datas// Initialize datat// Initialize datai// Initialize datal// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data'// Initialize data,// Initialize data // Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize datað// Initialize dataŸ// Initialize data¥// Initialize dataƒ// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize data#// Initialize datae// Initialize data8// Initialize dataa// Initialize data3// Initialize data3// Initialize datad// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data4// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataR// Initialize datae// Initialize dataf// Initialize datar// Initialize datai// Initialize datag// Initialize datae// Initialize datar// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datas// Initialize data'// Initialize data,// Initialize data // Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize datað// Initialize dataŸ// Initialize data¥// Initialize data¤// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize data#// Initialize data3// Initialize datab// Initialize data8// Initialize data2// Initialize dataf// Initialize data6// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data5// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataS// Initialize datau// Initialize datac// Initialize datao// Initialize datas// Initialize data'// Initialize data,// Initialize data // Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize datað// Initialize dataŸ// Initialize data§// Initialize dataƒ// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize data#// Initialize data1// Initialize data0// Initialize datab// Initialize data9// Initialize data8// Initialize data1// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data6// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataÃ// Initialize data// Initialize datag// Initialize datau// Initialize dataa// Initialize datas// Initialize data'// Initialize data,// Initialize data // Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize datað// Initialize dataŸ// Initialize data’// Initialize data§// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize data#// Initialize data0// Initialize data6// Initialize datab// Initialize data6// Initialize datad// Initialize data4// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data7// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataE// Initialize datan// Initialize datae// Initialize datar// Initialize datag// Initialize dataÃ// Initialize data©// Initialize datat// Initialize datai// Initialize datac// Initialize datao// Initialize datas// Initialize data'// Initialize data,// Initialize data // Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize dataâ// Initialize dataš// Initialize data¡// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize data#// Initialize data8// Initialize datab// Initialize data5// Initialize datac// Initialize dataf// Initialize data6// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize dataS// Initialize dataA// Initialize dataM// Initialize dataP// Initialize dataL// Initialize dataE// Initialize data_// Initialize dataP// Initialize dataR// Initialize dataO// Initialize dataD// Initialize dataU// Initialize dataC// Initialize dataT// Initialize dataS// Initialize data // Initialize data=// Initialize data // Initialize data[ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data1// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataC// Initialize datae// Initialize datar// Initialize datav// Initialize datae// Initialize dataj// Initialize dataa// Initialize data // Initialize dataI// Initialize dataP// Initialize dataA// Initialize data // Initialize dataA// Initialize datar// Initialize datat// Initialize datae// Initialize datas// Initialize dataa// Initialize datan// Initialize dataa// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize dataB// Initialize datar// Initialize datae// Initialize dataw// Initialize dataD// Initialize datao// Initialize datag// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data1// Initialize data'// Initialize data,// Initialize data // Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize data3// Initialize data5// Initialize data0// Initialize datam// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data8// Initialize data.// Initialize data5// Initialize data0// Initialize data,// Initialize data // Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data1// Initialize data4// Initialize data.// Initialize data9// Initialize data0// Initialize data,// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data4// Initialize data8// Initialize data,// Initialize data // Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data:// Initialize data // Initialize data1// Initialize data2// Initialize data,// Initialize data // Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize dataD// Initialize datai// Initialize datas// Initialize datat// Initialize datar// Initialize datai// Initialize datab// Initialize datau// Initialize datai// Initialize datad// Initialize datao// Initialize datar// Initialize dataa// Initialize data // Initialize dataH// Initialize datao// Initialize datap// Initialize data'// Initialize data,// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize datar// Initialize datai// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize dataI// Initialize dataP// Initialize dataA// Initialize data // Initialize datae// Initialize datan// Initialize datac// Initialize datao// Initialize datar// Initialize datap// Initialize dataa// Initialize datad// Initialize dataa// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize data // Initialize datan// Initialize datao// Initialize datat// Initialize dataa// Initialize datas// Initialize data // Initialize datac// Initialize dataÃ// Initialize data­// Initialize datat// Initialize datar// Initialize datai// Initialize datac// Initialize dataa// Initialize datas// Initialize data // Initialize datae// Initialize data // Initialize dataa// Initialize datam// Initialize dataa// Initialize datar// Initialize datag// Initialize datao// Initialize datar// Initialize data // Initialize datae// Initialize dataq// Initialize datau// Initialize datai// Initialize datal// Initialize datai// Initialize datab// Initialize datar// Initialize dataa// Initialize datad// Initialize datao// Initialize data.// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data2// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataV// Initialize datai// Initialize datan// Initialize datah// Initialize datao// Initialize data // Initialize dataT// Initialize datai// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize dataR// Initialize datae// Initialize datas// Initialize datae// Initialize datar// Initialize datav// Initialize dataa// Initialize data'// Initialize data,// Initialize data // Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize dataC// Initialize dataa// Initialize datas// Initialize datai// Initialize datal// Initialize datal// Initialize datae// Initialize datar// Initialize datao// Initialize data // Initialize datad// Initialize datae// Initialize datal// Initialize data // Initialize dataD// Initialize datai// Initialize dataa// Initialize datab// Initialize datal// Initialize datao// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data2// Initialize data'// Initialize data,// Initialize data // Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize data7// Initialize data5// Initialize data0// Initialize datam// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data3// Initialize data2// Initialize data.// Initialize data0// Initialize data0// Initialize data,// Initialize data // Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data5// Initialize data9// Initialize data.// Initialize data9// Initialize data0// Initialize data,// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data1// Initialize data5// Initialize data,// Initialize data // Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data:// Initialize data // Initialize data5// Initialize data,// Initialize data // Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize dataI// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datad// Initialize datao// Initialize datar// Initialize dataa// Initialize data // Initialize dataV// Initialize datai// Initialize datan// Initialize datah// Initialize datao// Initialize datas// Initialize data // Initialize dataS// Initialize dataA// Initialize data'// Initialize data,// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize datar// Initialize datai// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize dataC// Initialize dataa// Initialize datab// Initialize datae// Initialize datar// Initialize datan// Initialize datae// Initialize datat// Initialize data // Initialize dataS// Initialize dataa// Initialize datau// Initialize datav// Initialize datai// Initialize datag// Initialize datan// Initialize datao// Initialize datan// Initialize data // Initialize datac// Initialize datah// Initialize datai// Initialize datal// Initialize datae// Initialize datan// Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize data // Initialize datat// Initialize dataa// Initialize datan// Initialize datai// Initialize datan// Initialize datao// Initialize datas// Initialize data // Initialize datam// Initialize dataa// Initialize datac// Initialize datai// Initialize datao// Initialize datas// Initialize data.// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data3// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataW// Initialize datah// Initialize datai// Initialize datas// Initialize datak// Initialize datay// Initialize data // Initialize data1// Initialize data2// Initialize data // Initialize dataA// Initialize datan// Initialize datao// Initialize datas// Initialize data'// Initialize data,// Initialize data // Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize dataJ// Initialize datao// Initialize datah// Initialize datan// Initialize datan// Initialize datai// Initialize datae// Initialize data // Initialize dataW// Initialize dataa// Initialize datal// Initialize datak// Initialize datae// Initialize datar// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data3// Initialize data'// Initialize data,// Initialize data // Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize data7// Initialize data5// Initialize data0// Initialize datam// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data8// Initialize data9// Initialize data.// Initialize data9// Initialize data0// Initialize data,// Initialize data // Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data1// Initialize data5// Initialize data9// Initialize data.// Initialize data9// Initialize data0// Initialize data,// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data8// Initialize data,// Initialize data // Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data:// Initialize data // Initialize data3// Initialize data,// Initialize data // Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize dataD// Initialize datai// Initialize dataa// Initialize datag// Initialize datae// Initialize datao// Initialize data // Initialize dataB// Initialize datar// Initialize dataa// Initialize datas// Initialize datai// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize datar// Initialize datai// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize dataB// Initialize datal// Initialize datae// Initialize datan// Initialize datad// Initialize data // Initialize datas// Initialize datau// Initialize dataa// Initialize datav// Initialize datae// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize data // Initialize datan// Initialize datao// Initialize datat// Initialize dataa// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize datam// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize data // Initialize datab// Initialize dataa// Initialize datau// Initialize datan// Initialize datai// Initialize datal// Initialize datah// Initialize dataa// Initialize data.// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data4// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataC// Initialize datao// Initialize datac// Initialize dataa// Initialize data-// Initialize dataC// Initialize datao// Initialize datal// Initialize dataa// Initialize data // Initialize dataL// Initialize dataa// Initialize datat// Initialize dataa// Initialize data'// Initialize data,// Initialize data // Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize dataC// Initialize datao// Initialize datac// Initialize dataa// Initialize data-// Initialize dataC// Initialize datao// Initialize datal// Initialize dataa// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data4// Initialize data'// Initialize data,// Initialize data // Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize data3// Initialize data5// Initialize data0// Initialize datam// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data2// Initialize data.// Initialize data5// Initialize data0// Initialize data,// Initialize data // Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data5// Initialize data.// Initialize data0// Initialize data0// Initialize data,// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data1// Initialize data2// Initialize data0// Initialize data,// Initialize data // Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data:// Initialize data // Initialize data2// Initialize data4// Initialize data,// Initialize data // Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize dataC// Initialize datao// Initialize datac// Initialize dataa// Initialize data-// Initialize dataC// Initialize datao// Initialize datal// Initialize dataa// Initialize data // Initialize dataF// Initialize dataE// Initialize dataM// Initialize dataS// Initialize dataA// Initialize data'// Initialize data,// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize datar// Initialize datai// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize dataO// Initialize data // Initialize datar// Initialize datae// Initialize dataf// Initialize datar// Initialize datai// Initialize datag// Initialize datae// Initialize datar// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize data // Initialize datam// Initialize dataa// Initialize datai// Initialize datas// Initialize data // Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datad// Initialize datao// Initialize data // Initialize datad// Initialize datao// Initialize data // Initialize datam// Initialize datau// Initialize datan// Initialize datad// Initialize datao// Initialize data.// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data5// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataS// Initialize datau// Initialize datac// Initialize datao// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize dataL// Initialize dataa// Initialize datar// Initialize dataa// Initialize datan// Initialize dataj// Initialize dataa// Initialize data // Initialize dataN// Initialize dataa// Initialize datat// Initialize datau// Initialize datar// Initialize dataa// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize dataN// Initialize dataa// Initialize datat// Initialize datau// Initialize datar// Initialize dataa// Initialize datal// Initialize data // Initialize dataO// Initialize datan// Initialize datae// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data5// Initialize data'// Initialize data,// Initialize data // Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize data9// Initialize data0// Initialize data0// Initialize datam// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data9// Initialize data.// Initialize data8// Initialize data0// Initialize data,// Initialize data // Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data1// Initialize data6// Initialize data.// Initialize data9// Initialize data0// Initialize data,// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data2// Initialize data0// Initialize data,// Initialize data // Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data:// Initialize data // Initialize data8// Initialize data,// Initialize data // Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize dataN// Initialize dataa// Initialize datat// Initialize datau// Initialize datar// Initialize dataa// Initialize datal// Initialize data // Initialize dataO// Initialize datan// Initialize datae// Initialize data // Initialize dataL// Initialize datat// Initialize datad// Initialize dataa// Initialize data'// Initialize data,// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize datar// Initialize datai// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize dataS// Initialize datau// Initialize datac// Initialize datao// Initialize data // Initialize datai// Initialize datan// Initialize datat// Initialize datae// Initialize datag// Initialize datar// Initialize dataa// Initialize datal// Initialize data // Initialize datas// Initialize datae// Initialize datam// Initialize data // Initialize dataa// Initialize datad// Initialize datai// Initialize dataÃ// Initialize data§// Initialize dataÃ// Initialize data£// Initialize datao// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize dataa// Initialize dataÃ// Initialize data§// Initialize dataÃ// Initialize dataº// Initialize datac// Initialize dataa// Initialize datar// Initialize data.// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data6// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataÃ// Initialize data// Initialize datag// Initialize datau// Initialize dataa// Initialize data // Initialize dataM// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize dataa// Initialize datal// Initialize data // Initialize dataP// Initialize datar// Initialize datae// Initialize datam// Initialize datai// Initialize datau// Initialize datam// Initialize data'// Initialize data,// Initialize data // Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize dataS// Initialize dataa// Initialize datan// Initialize data // Initialize dataP// Initialize datae// Initialize datal// Initialize datal// Initialize datae// Initialize datag// Initialize datar// Initialize datai// Initialize datan// Initialize datao// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data6// Initialize data'// Initialize data,// Initialize data // Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize data5// Initialize data0// Initialize data0// Initialize datam// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data4// Initialize data.// Initialize data5// Initialize data0// Initialize data,// Initialize data // Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data8// Initialize data.// Initialize data9// Initialize data0// Initialize data,// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data6// Initialize data0// Initialize data,// Initialize data // Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data:// Initialize data // Initialize data2// Initialize data0// Initialize data,// Initialize data // Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize dataN// Initialize datae// Initialize datas// Initialize datat// Initialize datal// Initialize dataÃ// Initialize data©// Initialize data // Initialize dataW// Initialize dataa// Initialize datat// Initialize datae// Initialize datar// Initialize datas// Initialize data'// Initialize data,// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize datar// Initialize datai// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize dataÃ// Initialize data// Initialize datag// Initialize datau// Initialize dataa// Initialize data // Initialize datam// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize dataa// Initialize datal// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize data // Initialize datag// Initialize dataÃ// Initialize data¡// Initialize datas// Initialize data,// Initialize data // Initialize datai// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datad// Initialize dataa// Initialize data // Initialize datad// Initialize dataa// Initialize data // Initialize dataI// Initialize datat// Initialize dataÃ// Initialize data¡// Initialize datal// Initialize datai// Initialize dataa// Initialize data.// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data7// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataE// Initialize datan// Initialize datae// Initialize datar// Initialize datag// Initialize dataÃ// Initialize data©// Initialize datat// Initialize datai// Initialize datac// Initialize datao// Initialize data // Initialize dataM// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize datae// Initialize datar// Initialize data'// Initialize data,// Initialize data // Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize dataM// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize datae// Initialize datar// Initialize data // Initialize dataE// Initialize datan// Initialize datae// Initialize datar// Initialize datag// Initialize datay// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data7// Initialize data'// Initialize data,// Initialize data // Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize data4// Initialize data7// Initialize data3// Initialize datam// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data6// Initialize data.// Initialize data9// Initialize data0// Initialize data,// Initialize data // Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data1// Initialize data1// Initialize data.// Initialize data9// Initialize data0// Initialize data,// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data3// Initialize data6// Initialize data,// Initialize data // Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data:// Initialize data // Initialize data1// Initialize data0// Initialize data,// Initialize data // Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize dataM// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize datae// Initialize datar// Initialize data // Initialize dataB// Initialize datae// Initialize datav// Initialize datae// Initialize datar// Initialize dataa// Initialize datag// Initialize datae// Initialize data'// Initialize data,// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize datar// Initialize datai// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize dataB// Initialize datae// Initialize datab// Initialize datai// Initialize datad// Initialize dataa// Initialize data // Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize datag// Initialize dataÃ// Initialize data©// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize data // Initialize datas// Initialize dataa// Initialize datab// Initialize datao// Initialize datar// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data.// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data8// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataC// Initialize datae// Initialize datar// Initialize datav// Initialize datae// Initialize dataj// Initialize dataa// Initialize data // Initialize dataP// Initialize datai// Initialize datal// Initialize datas// Initialize datae// Initialize datan// Initialize data // Initialize dataP// Initialize datar// Initialize datae// Initialize datam// Initialize datai// Initialize datau// Initialize datam// Initialize data'// Initialize data,// Initialize data // Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize dataH// Initialize datae// Initialize datai// Initialize datan// Initialize datae// Initialize datak// Initialize datae// Initialize datan// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data1// Initialize data'// Initialize data,// Initialize data // Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize data3// Initialize data3// Initialize data0// Initialize datam// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data4// Initialize data.// Initialize data2// Initialize data0// Initialize data,// Initialize data // Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data7// Initialize data.// Initialize data9// Initialize data0// Initialize data,// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data7// Initialize data2// Initialize data,// Initialize data // Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data:// Initialize data // Initialize data2// Initialize data4// Initialize data,// Initialize data // Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize dataH// Initialize datae// Initialize datai// Initialize datan// Initialize datae// Initialize datak// Initialize datae// Initialize datan// Initialize data // Initialize dataB// Initialize datar// Initialize dataa// Initialize datas// Initialize datai// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize datar// Initialize datai// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize dataL// Initialize dataa// Initialize datag// Initialize datae// Initialize datar// Initialize data // Initialize datap// Initialize datar// Initialize datae// Initialize datam// Initialize datai// Initialize datau// Initialize datam// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize data // Initialize datal// Initialize dataÃ// Initialize dataº// Initialize datap// Initialize datau// Initialize datal// Initialize datao// Initialize data // Initialize datae// Initialize datau// Initialize datar// Initialize datao// Initialize datap// Initialize datae// Initialize datau// Initialize data.// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data9// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataV// Initialize datai// Initialize datan// Initialize datah// Initialize datao// Initialize data // Initialize dataR// Initialize datao// Initialize datas// Initialize dataÃ// Initialize data©// Initialize data // Initialize dataP// Initialize datar// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datac// Initialize datae// Initialize data'// Initialize data,// Initialize data // Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize dataM// Initialize datai// Initialize datar// Initialize dataa// Initialize datav// Initialize dataa// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data2// Initialize data'// Initialize data,// Initialize data // Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize data7// Initialize data5// Initialize data0// Initialize datam// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data6// Initialize data5// Initialize data.// Initialize data0// Initialize data0// Initialize data,// Initialize data // Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data1// Initialize data1// Initialize data9// Initialize data.// Initialize data9// Initialize data0// Initialize data,// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data4// Initialize data,// Initialize data // Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data:// Initialize data // Initialize data3// Initialize data,// Initialize data // Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize dataI// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datad// Initialize datao// Initialize datar// Initialize dataa// Initialize data // Initialize dataP// Initialize datar// Initialize datae// Initialize datam// Initialize datai// Initialize datau// Initialize datam// Initialize data'// Initialize data,// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize datar// Initialize datai// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize dataR// Initialize datao// Initialize datas// Initialize dataÃ// Initialize data©// Initialize data // Initialize datae// Initialize datal// Initialize datae// Initialize datag// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize data // Initialize datad// Initialize dataa// Initialize data // Initialize datar// Initialize datae// Initialize datag// Initialize datai// Initialize dataÃ// Initialize data£// Initialize datao// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize dataP// Initialize datar// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datac// Initialize datae// Initialize data,// Initialize data // Initialize dataF// Initialize datar// Initialize dataa// Initialize datan// Initialize dataÃ// Initialize data§// Initialize dataa// Initialize data.// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data1// Initialize data0// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataG// Initialize datai// Initialize datan// Initialize data // Initialize dataL// Initialize datao// Initialize datan// Initialize datad// Initialize datao// Initialize datan// Initialize data // Initialize dataD// Initialize datar// Initialize datay// Initialize data'// Initialize data,// Initialize data // Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize dataT// Initialize dataa// Initialize datan// Initialize dataq// Initialize datau// Initialize datae// Initialize datar// Initialize dataa// Initialize datay// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data3// Initialize data'// Initialize data,// Initialize data // Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize data7// Initialize data5// Initialize data0// Initialize datam// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data7// Initialize data2// Initialize data.// Initialize data0// Initialize data0// Initialize data,// Initialize data // Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data1// Initialize data2// Initialize data9// Initialize data.// Initialize data9// Initialize data0// Initialize data,// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data1// Initialize data0// Initialize data,// Initialize data // Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data:// Initialize data // Initialize data4// Initialize data,// Initialize data // Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize dataD// Initialize datai// Initialize dataa// Initialize datag// Initialize datae// Initialize datao// Initialize data // Initialize dataB// Initialize datar// Initialize dataa// Initialize datas// Initialize datai// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize datar// Initialize datai// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize dataG// Initialize datai// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataÃ// Initialize data¡// Initialize datas// Initialize datas// Initialize datai// Initialize datac// Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datat// Initialize datar// Initialize datao// Initialize data // Initialize datab// Initialize datao// Initialize datat// Initialize dataÃ// Initialize data¢// Initialize datan// Initialize datai// Initialize datac// Initialize datao// Initialize datas// Initialize data.// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data1// Initialize data1// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataV// Initialize datao// Initialize datad// Initialize datak// Initialize dataa// Initialize data // Initialize dataP// Initialize datar// Initialize datae// Initialize datam// Initialize datai// Initialize datau// Initialize datam// Initialize data'// Initialize data,// Initialize data // Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize dataG// Initialize datar// Initialize datae// Initialize datay// Initialize data // Initialize dataG// Initialize datao// Initialize datao// Initialize datas// Initialize datae// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data3// Initialize data'// Initialize data,// Initialize data // Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize data7// Initialize data5// Initialize data0// Initialize datam// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data9// Initialize data5// Initialize data.// Initialize data0// Initialize data0// Initialize data,// Initialize data // Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data1// Initialize data7// Initialize data9// Initialize data.// Initialize data9// Initialize data0// Initialize data,// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data6// Initialize data,// Initialize data // Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data:// Initialize data // Initialize data3// Initialize data,// Initialize data // Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize dataB// Initialize dataa// Initialize datac// Initialize dataa// Initialize datar// Initialize datad// Initialize datai// Initialize data // Initialize dataM// Initialize dataa// Initialize datar// Initialize datat// Initialize datai// Initialize datan// Initialize datai// Initialize data'// Initialize data,// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize datar// Initialize datai// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize dataV// Initialize datao// Initialize datad// Initialize datak// Initialize dataa// Initialize data // Initialize dataf// Initialize datar// Initialize dataa// Initialize datan// Initialize datac// Initialize datae// Initialize datas// Initialize dataa// Initialize data // Initialize dataf// Initialize datae// Initialize datai// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize data // Initialize datat// Initialize datar// Initialize datai// Initialize datag// Initialize datao// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize dataP// Initialize datai// Initialize datac// Initialize dataa// Initialize datar// Initialize datad// Initialize datai// Initialize dataa// Initialize data.// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data1// Initialize data2// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataR// Initialize datae// Initialize datad// Initialize data // Initialize dataB// Initialize datau// Initialize datal// Initialize datal// Initialize data // Initialize dataE// Initialize datan// Initialize datae// Initialize datar// Initialize datag// Initialize datay// Initialize data'// Initialize data,// Initialize data // Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize dataR// Initialize datae// Initialize datad// Initialize data // Initialize dataB// Initialize datau// Initialize datal// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data7// Initialize data'// Initialize data,// Initialize data // Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize data2// Initialize data5// Initialize data0// Initialize datam// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data5// Initialize data.// Initialize data9// Initialize data0// Initialize data,// Initialize data // Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data9// Initialize data.// Initialize data9// Initialize data0// Initialize data,// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data2// Initialize data,// Initialize data // Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data:// Initialize data // Initialize data1// Initialize data5// Initialize data,// Initialize data // Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize dataR// Initialize datae// Initialize datad// Initialize data // Initialize dataB// Initialize datau// Initialize datal// Initialize datal// Initialize data // Initialize dataB// Initialize datar// Initialize dataa// Initialize datas// Initialize datai// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize datar// Initialize datai// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize dataB// Initialize datae// Initialize datab// Initialize datai// Initialize datad// Initialize dataa// Initialize data // Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize datag// Initialize dataÃ// Initialize data©// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize data // Initialize datal// Initialize dataÃ// Initialize data­// Initialize datad// Initialize datae// Initialize datar// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize datam// Initialize datae// Initialize datar// Initialize datac// Initialize dataa// Initialize datad// Initialize datao// Initialize data.// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data1// Initialize data3// Initialize data'// Initialize data,// Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize dataC// Initialize datae// Initialize datar// Initialize datav// Initialize datae// Initialize dataj// Initialize dataa// Initialize data // Initialize dataW// Initialize datai// Initialize datat// Initialize datab// Initialize datai// Initialize datae// Initialize datar// Initialize data'// Initialize data,// Initialize data // Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize dataH// Initialize datao// Initialize datae// Initialize datag// Initialize dataa// Initialize dataa// Initialize datar// Initialize datad// Initialize datae// Initialize datan// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize data1// Initialize data'// Initialize data,// Initialize data // Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize data3// Initialize data3// Initialize data0// Initialize datam// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data6// Initialize data.// Initialize data5// Initialize data0// Initialize data,// Initialize data // Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize data1// Initialize data2// Initialize data.// Initialize data9// Initialize data0// Initialize data,// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data0// Initialize data,// Initialize data // Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data:// Initialize data // Initialize data1// Initialize data2// Initialize data,// Initialize data // Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize dataA// Initialize dataB// Initialize data // Initialize dataI// Initialize datan// Initialize dataB// Initialize datae// Initialize datav// Initialize data'// Initialize data,// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize datar// Initialize datai// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize dataC// Initialize datae// Initialize datar// Initialize datav// Initialize datae// Initialize dataj// Initialize dataa// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize datat// Initialize datar// Initialize datai// Initialize datag// Initialize datao// Initialize data // Initialize datab// Initialize datae// Initialize datal// Initialize datag// Initialize dataa// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize data // Initialize datac// Initialize datao// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datao// Initialize data // Initialize datae// Initialize data // Initialize datac// Initialize dataa// Initialize datas// Initialize datac// Initialize dataa// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize datal// Initialize dataa// Initialize datar// Initialize dataa// Initialize datan// Initialize dataj// Initialize dataa// Initialize data.// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize dataS// Initialize dataA// Initialize dataM// Initialize dataP// Initialize dataL// Initialize dataE// Initialize data_// Initialize dataM// Initialize dataO// Initialize dataV// Initialize dataE// Initialize dataM// Initialize dataE// Initialize dataN// Initialize dataT// Initialize dataS// Initialize data // Initialize data=// Initialize data // Initialize data(// Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize data[// Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datan// Initialize datao// Initialize dataw// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize datas// Initialize data_// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data // Initialize data=// Initialize data // Initialize data[// Initialize data'// Initialize dataC// Initialize datao// Initialize datam// Initialize datap// Initialize datar// Initialize dataa// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize dataf// Initialize datao// Initialize datar// Initialize datan// Initialize datae// Initialize datac// Initialize datae// Initialize datad// Initialize datao// Initialize datar// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataD// Initialize datae// Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize dataÃ// Initialize data§// Initialize dataÃ// Initialize data£// Initialize datao// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize datac// Initialize datal// Initialize datai// Initialize datae// Initialize datan// Initialize datat// Initialize datae// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataA// Initialize dataj// Initialize datau// Initialize datas// Initialize datat// Initialize datae// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize datai// Initialize datan// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataÃ// Initialize data¡// Initialize datar// Initialize datai// Initialize datao// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataT// Initialize datar// Initialize dataa// Initialize datan// Initialize datas// Initialize dataf// Initialize datae// Initialize datar// Initialize dataÃ// Initialize dataª// Initialize datan// Initialize datac// Initialize datai// Initialize dataa// Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datae// Initialize data // Initialize datau// Initialize datan// Initialize datai// Initialize datad// Initialize dataa// Initialize datad// Initialize datae// Initialize datas// Initialize data'// Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize datas// Initialize data_// Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize data[// Initialize data'// Initialize dataV// Initialize datae// Initialize datan// Initialize datad// Initialize dataa// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataQ// Initialize datau// Initialize datae// Initialize datab// Initialize datar// Initialize dataa// Initialize data/// Initialize dataA// Initialize datav// Initialize dataa// Initialize datar// Initialize datai// Initialize dataa// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataV// Initialize datae// Initialize datan// Initialize datac// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataC// Initialize datao// Initialize datan// Initialize datas// Initialize datau// Initialize datam// Initialize datao// Initialize data // Initialize datai// Initialize datan// Initialize datat// Initialize datae// Initialize datar// Initialize datan// Initialize datao// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataA// Initialize dataj// Initialize datau// Initialize datas// Initialize datat// Initialize datae// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize datai// Initialize datan// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataÃ// Initialize data¡// Initialize datar// Initialize datai// Initialize datao// Initialize data'// Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize data[ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data1// Initialize data'// Initialize data,// Initialize data // Initialize dataq// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data2// Initialize data4// Initialize data,// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize dataA// Initialize datag// Initialize datao// Initialize data:// Initialize data // Initialize data6// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data4// Initialize data'// Initialize data,// Initialize data // Initialize dataq// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data4// Initialize data8// Initialize data,// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize dataA// Initialize datag// Initialize datao// Initialize data:// Initialize data // Initialize data5// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data8// Initialize data'// Initialize data,// Initialize data // Initialize dataq// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data3// Initialize data6// Initialize data,// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize dataA// Initialize datag// Initialize datao// Initialize data:// Initialize data // Initialize data4// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data3// Initialize data'// Initialize data,// Initialize data // Initialize dataq// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data6// Initialize data,// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize dataA// Initialize datag// Initialize datao// Initialize data:// Initialize data // Initialize data3// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data6// Initialize data'// Initialize data,// Initialize data // Initialize dataq// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data3// Initialize data0// Initialize data,// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize dataA// Initialize datag// Initialize datao// Initialize data:// Initialize data // Initialize data2// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data7// Initialize data'// Initialize data,// Initialize data // Initialize dataq// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data1// Initialize data2// Initialize data,// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize dataA// Initialize datag// Initialize datao// Initialize data:// Initialize data // Initialize data1// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data2// Initialize data'// Initialize data,// Initialize data // Initialize dataq// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data1// Initialize data0// Initialize data,// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize dataA// Initialize datag// Initialize datao// Initialize data:// Initialize data // Initialize data0// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize data[ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data1// Initialize data'// Initialize data,// Initialize data // Initialize dataq// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data6// Initialize data,// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize dataA// Initialize datag// Initialize datao// Initialize data:// Initialize data // Initialize data5// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data4// Initialize data'// Initialize data,// Initialize data // Initialize dataq// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data1// Initialize data2// Initialize data,// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize dataA// Initialize datag// Initialize datao// Initialize data:// Initialize data // Initialize data4// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data8// Initialize data'// Initialize data,// Initialize data // Initialize dataq// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data8// Initialize data,// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize dataA// Initialize datag// Initialize datao// Initialize data:// Initialize data // Initialize data3// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data1// Initialize data2// Initialize data'// Initialize data,// Initialize data // Initialize dataq// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data1// Initialize data0// Initialize data,// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize dataA// Initialize datag// Initialize datao// Initialize data:// Initialize data // Initialize data2// Initialize data,// Initialize data // Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize dataV// Initialize datae// Initialize datan// Initialize datad// Initialize dataa// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data9// Initialize data'// Initialize data,// Initialize data // Initialize dataq// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data2// Initialize data,// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize dataA// Initialize datag// Initialize datao// Initialize data:// Initialize data // Initialize data1// Initialize data,// Initialize data // Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize dataV// Initialize datae// Initialize datan// Initialize datad// Initialize dataa// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data{// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize datap// Initialize data1// Initialize data3// Initialize data'// Initialize data,// Initialize data // Initialize dataq// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize data5// Initialize data,// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize dataA// Initialize datag// Initialize datao// Initialize data:// Initialize data // Initialize data0// Initialize data,// Initialize data // Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize data'// Initialize dataV// Initialize datae// Initialize datan// Initialize datac// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data'// Initialize data // Initialize data}// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize data(// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data,// Initialize data // Initialize datai// Initialize datan// Initialize datad// Initialize datae// Initialize datax// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize datan// Initialize datao// Initialize dataw// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data.// Initialize datas// Initialize datae// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data.// Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize dataA// Initialize datag// Initialize datao// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data.// Initialize datas// Initialize datae// Initialize datat// Initialize dataH// Initialize datao// Initialize datau// Initialize datar// Initialize datas// Initialize data(// Initialize data9// Initialize data // Initialize data+// Initialize data // Initialize datai// Initialize datan// Initialize datad// Initialize datae// Initialize datax// Initialize data,// Initialize data // Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize dataf// Initialize datal// Initialize datao// Initialize datao// Initialize datar// Initialize data(// Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize datao// Initialize datam// Initialize data(// Initialize data)// Initialize data // Initialize data*// Initialize data // Initialize data5// Initialize data9// Initialize data)// Initialize data,// Initialize data // Initialize data0// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize datap// Initialize datau// Initialize datas// Initialize datah// Initialize data(// Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data`// Initialize datam// Initialize datao// Initialize datav// Initialize data-// Initialize datae// Initialize data$// Initialize data{// Initialize datai// Initialize datan// Initialize datad// Initialize datae// Initialize datax// Initialize data}// Initialize data`// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data.// Initialize dataq// Initialize datat// Initialize datay// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize datas// Initialize data_// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data[// Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize dataf// Initialize datal// Initialize datao// Initialize datao// Initialize datar// Initialize data(// Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize datao// Initialize datam// Initialize data(// Initialize data)// Initialize data // Initialize data*// Initialize data // Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize datas// Initialize data_// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data)// Initialize data]// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datan// Initialize datao// Initialize datat// Initialize datae// Initialize datas// Initialize data:// Initialize data // Initialize data'// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data:// Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data.// Initialize datat// Initialize datao// Initialize dataI// Initialize dataS// Initialize dataO// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data)// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize data(// Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize data,// Initialize data // Initialize datai// Initialize datan// Initialize datad// Initialize datae// Initialize datax// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize datan// Initialize datao// Initialize dataw// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data.// Initialize datas// Initialize datae// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize data.// Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize dataA// Initialize datag// Initialize datao// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data.// Initialize datas// Initialize datae// Initialize datat// Initialize dataH// Initialize datao// Initialize datau// Initialize datar// Initialize datas// Initialize data(// Initialize data1// Initialize data4// Initialize data // Initialize data+// Initialize data // Initialize datai// Initialize datan// Initialize datad// Initialize datae// Initialize datax// Initialize data,// Initialize data // Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize dataf// Initialize datal// Initialize datao// Initialize datao// Initialize datar// Initialize data(// Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize datao// Initialize datam// Initialize data(// Initialize data)// Initialize data // Initialize data*// Initialize data // Initialize data5// Initialize data9// Initialize data)// Initialize data,// Initialize data // Initialize data0// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize datap// Initialize datau// Initialize datas// Initialize datah// Initialize data(// Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize data`// Initialize datam// Initialize datao// Initialize datav// Initialize data-// Initialize datax// Initialize data$// Initialize data{// Initialize datai// Initialize datan// Initialize datad// Initialize datae// Initialize datax// Initialize data}// Initialize data`// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize data.// Initialize dataq// Initialize datat// Initialize datay// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize data.// Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize datas// Initialize data_// Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize data[// Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize dataf// Initialize datal// Initialize datao// Initialize datao// Initialize datar// Initialize data(// Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize datao// Initialize datam// Initialize data(// Initialize data)// Initialize data // Initialize data*// Initialize data // Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize datas// Initialize data_// Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data)// Initialize data]// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datan// Initialize datao// Initialize datat// Initialize datae// Initialize datas// Initialize data:// Initialize data // Initialize data'// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data:// Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data.// Initialize datat// Initialize datao// Initialize dataI// Initialize dataS// Initialize dataO// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data)// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datar// Initialize datat// Initialize data(// Initialize data(// Initialize dataa// Initialize data,// Initialize data // Initialize datab// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize datab// Initialize data.// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize dataa// Initialize data.// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data)// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data)// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize dataS// Initialize datat// Initialize datao// Initialize datar// Initialize dataa// Initialize datag// Initialize datae// Initialize data // Initialize datah// Initialize datae// Initialize datal// Initialize datap// Initialize datae// Initialize datar// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datag// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize dataa// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataM// Initialize dataa// Initialize datai// Initialize datas// Initialize data // Initialize datar// Initialize datao// Initialize datab// Initialize datau// Initialize datas// Initialize datat// Initialize datao// Initialize data:// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datab// Initialize datai// Initialize datan// Initialize dataa// Initialize data // Initialize datat// Initialize datai// Initialize datam// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize datam// Initialize datap// Initialize data // Initialize data+// Initialize data // Initialize datad// Initialize datau// Initialize dataa// Initialize datas// Initialize data // Initialize datap// Initialize dataa// Initialize datar// Initialize datat// Initialize datae// Initialize datas// Initialize data // Initialize dataa// Initialize datal// Initialize datae// Initialize dataa// Initialize datat// Initialize dataó// Initialize datar// Initialize datai// Initialize dataa// Initialize datas// Initialize data // Initialize datap// Initialize dataa// Initialize datar// Initialize dataa// Initialize data // Initialize datae// Initialize datav// Initialize datai// Initialize datat// Initialize dataa// Initialize datar// Initialize data // Initialize datac// Initialize datao// Initialize datal// Initialize datai// Initialize datas// Initialize dataõ// Initialize datae// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datai// Initialize datam// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize datam// Initialize datap// Initialize data // Initialize data=// Initialize data // Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data.// Initialize datan// Initialize datao// Initialize dataw// Initialize data(// Initialize data)// Initialize data.// Initialize datat// Initialize datao// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data3// Initialize data6// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize datao// Initialize datam// Initialize dataP// Initialize dataa// Initialize datar// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize datao// Initialize datam// Initialize data(// Initialize data)// Initialize data.// Initialize datat// Initialize datao// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data3// Initialize data6// Initialize data)// Initialize data.// Initialize datas// Initialize datau// Initialize datab// Initialize datas// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data2// Initialize data,// Initialize data // Initialize data8// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datai// Initialize datac// Initialize datar// Initialize datao// Initialize datas// Initialize dataP// Initialize dataa// Initialize datar// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize dataf// Initialize datal// Initialize datao// Initialize datao// Initialize datar// Initialize data(// Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize datao// Initialize datam// Initialize data(// Initialize data)// Initialize data // Initialize data*// Initialize data // Initialize data1// Initialize data0// Initialize data0// Initialize data0// Initialize data0// Initialize data0// Initialize data0// Initialize data)// Initialize data.// Initialize datat// Initialize datao// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data3// Initialize data6// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data'// Initialize datai// Initialize datad// Initialize data-// Initialize data'// Initialize data // Initialize data+// Initialize data // Initialize datat// Initialize datai// Initialize datam// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize datam// Initialize datap// Initialize data // Initialize data+// Initialize data // Initialize data'// Initialize data-// Initialize data'// Initialize data // Initialize data+// Initialize data // Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize datao// Initialize datam// Initialize dataP// Initialize dataa// Initialize datar// Initialize datat// Initialize data // Initialize data+// Initialize data // Initialize data'// Initialize data-// Initialize data'// Initialize data // Initialize data+// Initialize data // Initialize datam// Initialize datai// Initialize datac// Initialize datar// Initialize datao// Initialize datas// Initialize dataP// Initialize dataa// Initialize datar// Initialize datat// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize dataI// Initialize datan// Initialize datai// Initialize datat// Initialize datai// Initialize dataa// Initialize datal// Initialize datai// Initialize dataz// Initialize datae// Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize dataa generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize datal// Initialize datae// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datal// Initialize datao// Initialize dataa// Initialize datad// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize datal// Initialize datae// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datal// Initialize datao// Initialize dataa// Initialize datad// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize datal// Initialize datae// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datal// Initialize datao// Initialize dataa// Initialize datad// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize data{// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize data[// Initialize data.// Initialize data.// Initialize data.// Initialize dataD// Initialize dataE// Initialize dataF// Initialize dataA// Initialize dataU// Initialize dataL// Initialize dataT// Initialize data_// Initialize dataC// Initialize dataA// Initialize dataT// Initialize dataE// Initialize dataG// Initialize dataO// Initialize dataR// Initialize dataI// Initialize dataE// Initialize dataS// Initialize data]// Initialize data;// Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data;// Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize data[// Initialize data.// Initialize data.// Initialize data.// Initialize dataS// Initialize dataA// Initialize dataM// Initialize dataP// Initialize dataL// Initialize dataE// Initialize data_// Initialize dataP// Initialize dataR// Initialize dataO// Initialize dataD// Initialize dataU// Initialize dataC// Initialize dataT// Initialize dataS// Initialize data]// Initialize data;// Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data;// Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize data[// Initialize data.// Initialize data.// Initialize data.// Initialize dataS// Initialize dataA// Initialize dataM// Initialize dataP// Initialize dataL// Initialize dataE// Initialize data_// Initialize dataM// Initialize dataO// Initialize dataV// Initialize dataE// Initialize dataM// Initialize dataE// Initialize dataN// Initialize dataT// Initialize dataS// Initialize data]// Initialize data;// Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data;// Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize dataU// Initialize dataT// Initialize dataI// Initialize dataL// Initialize dataI// Initialize dataT// Initialize dataI// Initialize dataE// Initialize dataS// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datat// Initialize dataC// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datac// Initialize datay// Initialize data(// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataI// Initialize datan// Initialize datat// Initialize datal// Initialize data.// Initialize dataN// Initialize datau// Initialize datam// Initialize datab// Initialize datae// Initialize datar// Initialize dataF// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datat// Initialize data(// Initialize data'// Initialize datap// Initialize datat// Initialize data-// Initialize dataB// Initialize dataR// Initialize data'// Initialize data,// Initialize data // Initialize data{// Initialize data // Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datac// Initialize datay// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datac// Initialize datay// Initialize data:// Initialize data // Initialize data'// Initialize dataB// Initialize dataR// Initialize dataL// Initialize data'// Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datat// Initialize data(// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data0// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize datai// Initialize datas// Initialize datao// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize datai// Initialize datas// Initialize datao// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data)// Initialize data.// Initialize datat// Initialize datao// Initialize dataL// Initialize datao// Initialize datac// Initialize dataa// Initialize datal// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data'// Initialize datap// Initialize datat// Initialize data-// Initialize dataB// Initialize dataR// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize dataT// Initialize datai// Initialize datam// Initialize datae// Initialize data(// Initialize datai// Initialize datas// Initialize datao// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize datai// Initialize datas// Initialize datao// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize datad// Initialize data.// Initialize datat// Initialize datao// Initialize dataL// Initialize datao// Initialize datac// Initialize dataa// Initialize datal// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data'// Initialize datap// Initialize datat// Initialize data-// Initialize dataB// Initialize dataR// Initialize data'// Initialize data)// Initialize data // Initialize data+// Initialize data // Initialize data'// Initialize data // Initialize data'// Initialize data // Initialize data+// Initialize data // Initialize datad// Initialize data.// Initialize datat// Initialize datao// Initialize dataL// Initialize datao// Initialize datac// Initialize dataa// Initialize datal// Initialize datae// Initialize dataT// Initialize datai// Initialize datam// Initialize datae// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data'// Initialize datap// Initialize datat// Initialize data-// Initialize dataB// Initialize dataR// Initialize data'// Initialize data,// Initialize data // Initialize data{// Initialize data // Initialize datah// Initialize datao// Initialize datau// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize data2// Initialize data-// Initialize datad// Initialize datai// Initialize datag// Initialize datai// Initialize datat// Initialize data'// Initialize data,// Initialize data // Initialize datam// Initialize datai// Initialize datan// Initialize datau// Initialize datat// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize data2// Initialize data-// Initialize datad// Initialize datai// Initialize datag// Initialize datai// Initialize datat// Initialize data'// Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datai// Initialize datad// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datan// Initialize datad// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datai// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datai// Initialize datad// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datan// Initialize datad// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datai// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataS// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data(// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data{// Initialize data // Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data:// Initialize data // Initialize data'// Initialize dataS// Initialize datae// Initialize datam// Initialize data // Initialize dataE// Initialize datas// Initialize datat// Initialize datao// Initialize dataq// Initialize datau// Initialize datae// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data:// Initialize data // Initialize data'// Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae// Initialize data-// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data'// Initialize data,// Initialize data // Initialize datal// Initialize datae// Initialize datav// Initialize datae// Initialize datal// Initialize data:// Initialize data // Initialize data'// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data'// Initialize data // Initialize data}// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data // Initialize data<// Initialize data=// Initialize data // Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize dataf// Initialize datal// Initialize datao// Initialize datao// Initialize datar// Initialize data(// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data // Initialize data*// Initialize data // Initialize data0// Initialize data.// Initialize data3// Initialize data)// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data{// Initialize data // Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data:// Initialize data // Initialize data'// Initialize dataC// Initialize datar// Initialize dataÃ// Initialize data­// Initialize datat// Initialize datai// Initialize datac// Initialize datao// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data:// Initialize data // Initialize data'// Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae// Initialize data-// Initialize datac// Initialize datar// Initialize datai// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize datal// Initialize datae// Initialize datav// Initialize datae// Initialize datal// Initialize data:// Initialize data // Initialize data'// Initialize datac// Initialize datar// Initialize datai// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize datal// Initialize data'// Initialize data // Initialize data}// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data // Initialize data<// Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data{// Initialize data // Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data:// Initialize data // Initialize data'// Initialize dataB// Initialize dataa// Initialize datai// Initialize datax// Initialize datao// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data:// Initialize data // Initialize data'// Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae// Initialize data-// Initialize datal// Initialize datao// Initialize dataw// Initialize data'// Initialize data,// Initialize data // Initialize datal// Initialize datae// Initialize datav// Initialize datae// Initialize datal// Initialize data:// Initialize data // Initialize data'// Initialize datal// Initialize datao// Initialize dataw// Initialize data'// Initialize data // Initialize data}// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data{// Initialize data // Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data:// Initialize data // Initialize data'// Initialize dataO// Initialize dataK// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data:// Initialize data // Initialize data'// Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae// Initialize data-// Initialize datao// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize datal// Initialize datae// Initialize datav// Initialize datae// Initialize datal// Initialize data:// Initialize data // Initialize data'// Initialize datao// Initialize datak// Initialize data'// Initialize data // Initialize data}// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datai// Initialize datas// Initialize dataT// Initialize datao// Initialize datad// Initialize dataa// Initialize datay// Initialize data(// Initialize datai// Initialize datas// Initialize datao// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize datai// Initialize datas// Initialize datao// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datan// Initialize datao// Initialize dataw// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize datad// Initialize data.// Initialize datat// Initialize datao// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datan// Initialize datao// Initialize dataw// Initialize data.// Initialize datat// Initialize datao// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize dataT// Initialize dataO// Initialize dataA// Initialize dataS// Initialize dataT// Initialize data // Initialize dataS// Initialize dataY// Initialize dataS// Initialize dataT// Initialize dataE// Initialize dataM// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize datam// Initialize datae// Initialize datas// Initialize datas// Initialize dataa// Initialize datag// Initialize datae// Initialize data,// Initialize data // Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize datai// Initialize datan// Initialize dataf// Initialize datao// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datat// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data-// Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datau// Initialize datac// Initialize datac// Initialize datae// Initialize datas// Initialize datas// Initialize data:// Initialize data // Initialize data'// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datac// Initialize datai// Initialize datar// Initialize datac// Initialize datal// Initialize datae// Initialize data-// Initialize datac// Initialize datah// Initialize datae// Initialize datac// Initialize datak// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datac// Initialize datai// Initialize datar// Initialize datac// Initialize datal// Initialize datae// Initialize data-// Initialize datax// Initialize datam// Initialize dataa// Initialize datar// Initialize datak// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data:// Initialize data // Initialize data'// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datat// Initialize datar// Initialize datai// Initialize dataa// Initialize datan// Initialize datag// Initialize datal// Initialize datae// Initialize data-// Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize dataa// Initialize datam// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize datan// Initialize dataf// Initialize datao// Initialize data:// Initialize data // Initialize data'// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datac// Initialize datai// Initialize datar// Initialize datac// Initialize datal// Initialize datae// Initialize data-// Initialize datai// Initialize datan// Initialize dataf// Initialize datao// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datac// Initialize datar// Initialize datae// Initialize dataa// Initialize datat// Initialize datae// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data(// Initialize data'// Initialize datad// Initialize datai// Initialize datav// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize data`// Initialize datat// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data // Initialize data$// Initialize data{// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data}// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data`// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize data$// Initialize data{// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize data[// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data]// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize data.// Initialize datai// Initialize datan// Initialize dataf// Initialize datao// Initialize data}// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data$// Initialize data{// Initialize datam// Initialize datae// Initialize datas// Initialize datas// Initialize dataa// Initialize datag// Initialize datae// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize dataa// Initialize datap// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize dataC// Initialize datah// Initialize datai// Initialize datal// Initialize datad// Initialize data(// Initialize datat// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datae// Initialize datat// Initialize dataT// Initialize datai// Initialize datam// Initialize datae// Initialize datao// Initialize datau// Initialize datat// Initialize data(// Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize data(// Initialize data'// Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datae// Initialize datat// Initialize dataT// Initialize datai// Initialize datam// Initialize datae// Initialize datao// Initialize datau// Initialize datat// Initialize data(// Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datat// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data.// Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize data(// Initialize data)// Initialize data,// Initialize data // Initialize data3// Initialize data0// Initialize data0// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data,// Initialize data // Initialize data3// Initialize data5// Initialize data0// Initialize data0// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize dataM// Initialize dataO// Initialize dataD// Initialize dataA// Initialize dataL// Initialize data // Initialize dataS// Initialize dataY// Initialize dataS// Initialize dataT// Initialize dataE// Initialize dataM// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize dataI// Initialize datad// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize data(// Initialize data'// Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data.// Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data.// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize dataf// Initialize datal// Initialize datao// Initialize dataw// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize datah// Initialize datai// Initialize datad// Initialize datad// Initialize datae// Initialize datan// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize dataI// Initialize datad// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize data(// Initialize data'// Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data.// Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data.// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize dataf// Initialize datal// Initialize datao// Initialize dataw// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datas// Initialize datae// Initialize datat// Initialize datau// Initialize datap// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize dataC// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataH// Initialize dataa// Initialize datan// Initialize datad// Initialize datal// Initialize datae// Initialize datar// Initialize datas// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize datae// Initialize datar// Initialize datay// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize datao// Initialize datar// Initialize dataA// Initialize datal// Initialize datal// Initialize data(// Initialize data'// Initialize data.// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data-// Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize data-// Initialize datab// Initialize datat// Initialize datan// Initialize data'// Initialize data)// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize datab// Initialize datat// Initialize datan// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datab// Initialize datat// Initialize datan// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize datab// Initialize datat// Initialize datan// Initialize data.// Initialize datad// Initialize dataa// Initialize datat// Initialize dataa// Initialize datas// Initialize datae// Initialize datat// Initialize data.// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data)// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize datae// Initialize datar// Initialize datay// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize datao// Initialize datar// Initialize dataA// Initialize datal// Initialize datal// Initialize data(// Initialize data'// Initialize data.// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data-// Initialize datac// Initialize dataa// Initialize datan// Initialize datac// Initialize datae// Initialize datal// Initialize data'// Initialize data)// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize datab// Initialize datat// Initialize datan// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datab// Initialize datat// Initialize datan// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize datab// Initialize datat// Initialize datan// Initialize data.// Initialize datad// Initialize dataa// Initialize datat// Initialize dataa// Initialize datas// Initialize datae// Initialize datat// Initialize data.// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data)// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize datae// Initialize datar// Initialize datay// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize datao// Initialize datar// Initialize dataA// Initialize datal// Initialize datal// Initialize data(// Initialize data'// Initialize data.// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data-// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize datal// Initialize dataa// Initialize datay// Initialize data'// Initialize data)// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize datal// Initialize dataa// Initialize datay// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize datal// Initialize dataa// Initialize datay// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize datae// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datae// Initialize data.// Initialize datat// Initialize dataa// Initialize datar// Initialize datag// Initialize datae// Initialize datat// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize datal// Initialize dataa// Initialize datay// Initialize data)// Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize datal// Initialize dataa// Initialize datay// Initialize data.// Initialize datai// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize dataN// Initialize dataA// Initialize dataV// Initialize dataI// Initialize dataG// Initialize dataA// Initialize dataT// Initialize dataI// Initialize dataO// Initialize dataN// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize dataP// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataT// Initialize dataI// Initialize dataT// Initialize dataL// Initialize dataE// Initialize dataS// Initialize data // Initialize data=// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize dataa// Initialize datas// Initialize datah// Initialize datab// Initialize datao// Initialize dataa// Initialize datar// Initialize datad// Initialize data:// Initialize data // Initialize data'// Initialize dataD// Initialize dataa// Initialize datas// Initialize datah// Initialize datab// Initialize datao// Initialize dataa// Initialize datar// Initialize datad// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data:// Initialize data // Initialize data'// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data:// Initialize data // Initialize data'// Initialize dataM// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataÃ// Initialize data§// Initialize dataÃ// Initialize dataµ// Initialize datae// Initialize datas// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data:// Initialize data // Initialize data'// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize datas// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize datas// Initialize data:// Initialize data // Initialize data'// Initialize dataR// Initialize datae// Initialize datal// Initialize dataa// Initialize datat// Initialize dataÃ// Initialize data³// Initialize datar// Initialize datai// Initialize datao// Initialize datas// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datan// Initialize dataa// Initialize datav// Initialize datai// Initialize datag// Initialize dataa// Initialize datat// Initialize datae// Initialize dataT// Initialize datao// Initialize data(// Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize datae// Initialize datar// Initialize datay// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize datao// Initialize datar// Initialize dataA// Initialize datal// Initialize datal// Initialize data(// Initialize data'// Initialize data.// Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize data(// Initialize data'// Initialize dataa// Initialize datac// Initialize datat// Initialize datai// Initialize datav// Initialize datae// Initialize data'// Initialize data)// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize datae// Initialize datar// Initialize datay// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize datao// Initialize datar// Initialize dataA// Initialize datal// Initialize datal// Initialize data(// Initialize data'// Initialize data.// Initialize datan// Initialize dataa// Initialize datav// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data'// Initialize data)// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize datan// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datan// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize data(// Initialize data'// Initialize dataa// Initialize datac// Initialize datat// Initialize datai// Initialize datav// Initialize datae// Initialize data'// Initialize data)// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data`// Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize data-// Initialize data$// Initialize data{// Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data`// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datan// Initialize dataa// Initialize datav// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize datae// Initialize datar// Initialize datay// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize datao// Initialize datar// Initialize data(// Initialize data`// Initialize data.// Initialize datan// Initialize dataa// Initialize datav// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data[// Initialize datad// Initialize dataa// Initialize datat// Initialize dataa// Initialize data-// Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize data=// Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data]// Initialize data`// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize data)// Initialize data // Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize data(// Initialize data'// Initialize dataa// Initialize datac// Initialize datat// Initialize datai// Initialize datav// Initialize datae// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datan// Initialize dataa// Initialize datav// Initialize data)// Initialize data // Initialize datan// Initialize dataa// Initialize datav// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize data(// Initialize data'// Initialize dataa// Initialize datac// Initialize datat// Initialize datai// Initialize datav// Initialize datae// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datab// Initialize datar// Initialize datae// Initialize dataa// Initialize datad// Initialize datac// Initialize datar// Initialize datau// Initialize datam// Initialize datab// Initialize data-// Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize dataC// Initialize datao// Initialize datan// Initialize datat// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize dataP// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataT// Initialize dataI// Initialize dataT// Initialize dataL// Initialize dataE// Initialize dataS// Initialize data[// Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data]// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataC// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize data // Initialize datam// Initialize datao// Initialize datab// Initialize datai// Initialize datal// Initialize datae// Initialize data // Initialize datas// Initialize datai// Initialize datad// Initialize datae// Initialize datab// Initialize dataa// Initialize datar generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datas// Initialize datai// Initialize datad// Initialize datae// Initialize datab// Initialize dataa// Initialize datar// Initialize data'// Initialize data)// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datab// Initialize datai// Initialize datal// Initialize datae// Initialize data-// Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize datal// Initialize dataa// Initialize datay// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize datae// Initialize datar// Initialize datay// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize datao// Initialize datar// Initialize data(// Initialize data'// Initialize data.// Initialize datam// Initialize datao// Initialize datab// Initialize datai// Initialize datal// Initialize datae// Initialize data-// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize datal// Initialize dataa// Initialize datay// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize datal// Initialize dataa// Initialize datay// Initialize data)// Initialize data // Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize datal// Initialize dataa// Initialize datay// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize data(// Initialize data'// Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize dataf// Initialize datar// Initialize datae// Initialize datas// Initialize datah// Initialize data // Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize dataa generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataw// Initialize datai// Initialize datat// Initialize datac// Initialize datah// Initialize data // Initialize data(// Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datas// Initialize datae// Initialize data // Initialize data'// Initialize datad// Initialize dataa// Initialize datas// Initialize datah// Initialize datab// Initialize datao// Initialize dataa// Initialize datar// Initialize datad// Initialize data'// Initialize data:// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataD// Initialize dataa// Initialize datas// Initialize datah// Initialize datab// Initialize datao// Initialize dataa// Initialize datar// Initialize datad// Initialize data(// Initialize data)// Initialize data;// Initialize data // Initialize datab// Initialize datar// Initialize datae// Initialize dataa// Initialize datak// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datas// Initialize datae// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data'// Initialize data:// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data;// Initialize data // Initialize datab// Initialize datar// Initialize datae// Initialize dataa// Initialize datak// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datas// Initialize datae// Initialize data // Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data'// Initialize data:// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data;// Initialize data // Initialize datab// Initialize datar// Initialize datae// Initialize dataa// Initialize datak// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datas// Initialize datae// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data'// Initialize data:// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data;// Initialize data // Initialize datab// Initialize datar// Initialize datae// Initialize dataa// Initialize datak// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datas// Initialize datae// Initialize data // Initialize data'// Initialize datar// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize datas// Initialize data'// Initialize data:// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataR// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data;// Initialize data // Initialize datab// Initialize datar// Initialize datae// Initialize dataa// Initialize datak// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datas// Initialize datae// Initialize datat// Initialize datau// Initialize datap// Initialize dataN// Initialize dataa// Initialize datav// Initialize datai// Initialize datag// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize datae// Initialize datar// Initialize datay// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize datao// Initialize datar// Initialize dataA// Initialize datal// Initialize datal// Initialize data(// Initialize data'// Initialize data.// Initialize datan// Initialize dataa// Initialize datav// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data'// Initialize data)// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize datae// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datae// Initialize data.// Initialize datap// Initialize datar// Initialize datae// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataD// Initialize datae// Initialize dataf// Initialize dataa// Initialize datau// Initialize datal// Initialize datat// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datan// Initialize dataa// Initialize datav// Initialize datai// Initialize datag// Initialize dataa// Initialize datat// Initialize datae// Initialize dataT// Initialize datao// Initialize data(// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data.// Initialize datad// Initialize dataa// Initialize datat// Initialize dataa// Initialize datas// Initialize datae// Initialize datat// Initialize data.// Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataC// Initialize dataa// Initialize datar// Initialize datad// Initialize data // Initialize datal// Initialize datai// Initialize datan// Initialize datak// Initialize datas// Initialize data // Initialize data(// Initialize datag// Initialize datao// Initialize datat// Initialize datao// Initialize data) generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize datae// Initialize datar// Initialize datay// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize datao// Initialize datar// Initialize dataA// Initialize datal// Initialize datal// Initialize data(// Initialize data'// Initialize data[// Initialize datad// Initialize dataa// Initialize datat// Initialize dataa// Initialize data-// Initialize datag// Initialize datao// Initialize datat// Initialize datao// Initialize data]// Initialize data'// Initialize data)// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize datal// Initialize datai// Initialize datan// Initialize datak// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datal// Initialize datai// Initialize datan// Initialize datak// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize datae// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datae// Initialize data.// Initialize datap// Initialize datar// Initialize datae// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataD// Initialize datae// Initialize dataf// Initialize dataa// Initialize datau// Initialize datal// Initialize datat// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datan// Initialize dataa// Initialize datav// Initialize datai// Initialize datag// Initialize dataa// Initialize datat// Initialize datae// Initialize dataT// Initialize datao// Initialize data(// Initialize datal// Initialize datai// Initialize datan// Initialize datak// Initialize data.// Initialize datad// Initialize dataa// Initialize datat// Initialize dataa// Initialize datas// Initialize datae// Initialize datat// Initialize data.// Initialize datag// Initialize datao// Initialize datat// Initialize datao// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize dataS// Initialize dataI// Initialize dataD// Initialize dataE// Initialize dataB// Initialize dataA// Initialize dataR// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datas// Initialize datae// Initialize datat// Initialize datau// Initialize datap// Initialize dataS// Initialize datai// Initialize datad// Initialize datae// Initialize datab// Initialize dataa// Initialize datar// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datai// Initialize datad// Initialize datae// Initialize datab// Initialize dataa// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datas// Initialize datai// Initialize datad// Initialize datae// Initialize datab// Initialize dataa// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datao// Initialize datag// Initialize datag// Initialize datal// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datas// Initialize datai// Initialize datad// Initialize datae// Initialize datab// Initialize dataa// Initialize datar// Initialize data-// Initialize datat// Initialize datao// Initialize datag// Initialize datag// Initialize datal// Initialize datae// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datab// Initialize datai// Initialize datal// Initialize datae// Initialize dataB// Initialize datat// Initialize datan// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datab// Initialize datai// Initialize datal// Initialize datae// Initialize data-// Initialize datam// Initialize datae// Initialize datan// Initialize datau// Initialize data-// Initialize datab// Initialize datat// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datao// Initialize datag// Initialize datag// Initialize datal// Initialize datae// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datai// Initialize datad// Initialize datae// Initialize datab// Initialize dataa// Initialize datar// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize datat// Initialize datao// Initialize datag// Initialize datag// Initialize datal// Initialize datae// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datal// Initialize datal// Initialize dataa// Initialize datap// Initialize datas// Initialize datae// Initialize datad// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize datat// Initialize datao// Initialize datag// Initialize datag// Initialize datal// Initialize datae// Initialize data(// Initialize data'// Initialize datas// Initialize datai// Initialize datad// Initialize datae// Initialize datab// Initialize dataa// Initialize datar// Initialize data-// Initialize datac// Initialize datao// Initialize datal// Initialize datal// Initialize dataa// Initialize datap// Initialize datas// Initialize datae// Initialize datad// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataM// Initialize datao// Initialize datab// Initialize datai// Initialize datal// Initialize datae generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datal// Initialize datae// Initialize datat// Initialize data // Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize datal// Initialize dataa// Initialize datay// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datac// Initialize datar// Initialize datae// Initialize dataa// Initialize datat// Initialize datae// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data(// Initialize data'// Initialize datad// Initialize datai// Initialize datav// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize datal// Initialize dataa// Initialize datay// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize datam// Initialize datao// Initialize datab// Initialize datai// Initialize datal// Initialize datae// Initialize data-// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize datal// Initialize dataa// Initialize datay// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data.// Initialize dataa// Initialize datap// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize dataC// Initialize datah// Initialize datai// Initialize datal// Initialize datad// Initialize data(// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize datal// Initialize dataa// Initialize datay// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datab// Initialize datai// Initialize datal// Initialize datae// Initialize dataB// Initialize datat// Initialize datan// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datai// Initialize datad// Initialize datae// Initialize datab// Initialize dataa// Initialize datar// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datab// Initialize datai// Initialize datal// Initialize datae// Initialize data-// Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize datal// Initialize dataa// Initialize datay// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize data(// Initialize data'// Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize datal// Initialize dataa// Initialize datay// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datai// Initialize datad// Initialize datae// Initialize datab// Initialize dataa// Initialize datar// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datab// Initialize datai// Initialize datal// Initialize datae// Initialize data-// Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize datal// Initialize dataa// Initialize datay// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize data(// Initialize data'// Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize dataD// Initialize dataA// Initialize dataS// Initialize dataH// Initialize dataB// Initialize dataO// Initialize dataA// Initialize dataR// Initialize dataD// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataD// Initialize dataa// Initialize datas// Initialize datah// Initialize datab// Initialize datao// Initialize dataa// Initialize datar// Initialize datad// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataK// Initialize dataP// Initialize dataI// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataD// Initialize dataa// Initialize datas// Initialize datah// Initialize datab// Initialize datao// Initialize dataa// Initialize datar// Initialize datad// Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataC// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataD// Initialize dataa// Initialize datas// Initialize datah// Initialize datab// Initialize datao// Initialize dataa// Initialize datar// Initialize datad// Initialize dataA// Initialize datal// Initialize datae// Initialize datar// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataT// Initialize datao// Initialize datap// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataK// Initialize dataP// Initialize dataI// Initialize datas// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datar// Initialize datae// Initialize datad// Initialize datau// Initialize datac// Initialize datae// Initialize data(// Initialize data(// Initialize datas// Initialize datau// Initialize datam// Initialize data,// Initialize data // Initialize datap// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datas// Initialize datau// Initialize datam// Initialize data // Initialize data+// Initialize data // Initialize data(// Initialize datap// Initialize data.// Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data // Initialize data*// Initialize data // Initialize datap// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data)// Initialize data,// Initialize data // Initialize data0// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datal// Initialize datao// Initialize dataw// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataS// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data(// Initialize datap// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datal// Initialize datao// Initialize dataw// Initialize data'// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize datar// Initialize datai// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize datal// Initialize data'// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataT// Initialize datao// Initialize datad// Initialize dataa// Initialize datay// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datai// Initialize datas// Initialize dataT// Initialize datao// Initialize datad// Initialize dataa// Initialize datay// Initialize data(// Initialize datam// Initialize data.// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data)// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize dataa// Initialize datan// Initialize datai// Initialize datam// Initialize dataa// Initialize datat// Initialize datae// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data(// Initialize data'// Initialize datak// Initialize datap// Initialize datai// Initialize data-// Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize data-// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data'// Initialize data,// Initialize data // Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datak// Initialize datap// Initialize datai// Initialize data-// Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data-// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize dataC// Initialize datao// Initialize datan// Initialize datat// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datat// Initialize dataC// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datac// Initialize datay// Initialize data(// Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize dataa// Initialize datan// Initialize datai// Initialize datam// Initialize dataa// Initialize datat// Initialize datae// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data(// Initialize data'// Initialize datak// Initialize datap// Initialize datai// Initialize data-// Initialize datal// Initialize datao// Initialize dataw// Initialize data-// Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize datal// Initialize datao// Initialize dataw// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize dataa// Initialize datan// Initialize datai// Initialize datam// Initialize dataa// Initialize datat// Initialize datae// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data(// Initialize data'// Initialize datak// Initialize datap// Initialize datai// Initialize data-// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data-// Initialize datat// Initialize datao// Initialize datad// Initialize dataa// Initialize datay// Initialize data'// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataT// Initialize datao// Initialize datad// Initialize dataa// Initialize datay// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataU// Initialize datap// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data // Initialize datan// Initialize datao// Initialize datat// Initialize datai// Initialize dataf// Initialize datai// Initialize datac// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datan// Initialize datao// Initialize datat// Initialize datai// Initialize dataf// Initialize datai// Initialize datac// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data-// Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datal// Initialize datao// Initialize dataw// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae// Initialize data.// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize dataC// Initialize datao// Initialize datan// Initialize datat// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datal// Initialize datao// Initialize dataw// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize data(// Initialize data'// Initialize datah// Initialize datai// Initialize datad// Initialize datad// Initialize datae// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize data(// Initialize data'// Initialize datah// Initialize datai// Initialize datad// Initialize datad// Initialize datae// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize dataa// Initialize datan// Initialize datai// Initialize datam// Initialize dataa// Initialize datat// Initialize datae// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data(// Initialize datae// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataI// Initialize datad// Initialize data,// Initialize data // Initialize datat// Initialize dataa// Initialize datar// Initialize datag// Initialize datae// Initialize datat// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datae// Initialize datal// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datae// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize dataa// Initialize datar// Initialize datas// Initialize datae// Initialize dataI// Initialize datan// Initialize datat// Initialize data(// Initialize datae// Initialize datal// Initialize data.// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize dataC// Initialize datao// Initialize datan// Initialize datat// Initialize datae// Initialize datan// Initialize datat// Initialize data)// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data0// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datac// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datat// Initialize dataa// Initialize datar// Initialize datag// Initialize datae// Initialize datat// Initialize data)// Initialize data // Initialize data{// Initialize data // Initialize datae// Initialize datal// Initialize data.// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize dataC// Initialize datao// Initialize datan// Initialize datat// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datat// Initialize dataa// Initialize datar// Initialize datag// Initialize datae// Initialize datat// Initialize data;// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data;// Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datad// Initialize datau// Initialize datar// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize data=// Initialize data // Initialize data6// Initialize data0// Initialize data0// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datar// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datar// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datan// Initialize datac// Initialize datae// Initialize data.// Initialize datan// Initialize datao// Initialize dataw// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datau// Initialize datap// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize datan// Initialize datao// Initialize dataw// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datae// Initialize datal// Initialize dataa// Initialize datap// Initialize datas// Initialize datae// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datao// Initialize dataw// Initialize data // Initialize data-// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datar// Initialize datat// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datag// Initialize datar// Initialize datae// Initialize datas// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datam// Initialize datai// Initialize datan// Initialize data(// Initialize datae// Initialize datal// Initialize dataa// Initialize datap// Initialize datas// Initialize datae// Initialize datad// Initialize data // Initialize data/// Initialize data // Initialize datad// Initialize datau// Initialize datar// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data,// Initialize data // Initialize data1// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datae// Initialize dataa// Initialize datas// Initialize datae// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize data1// Initialize data // Initialize data-// Initialize data // Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datap// Initialize datao// Initialize dataw// Initialize data(// Initialize data1// Initialize data // Initialize data-// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datag// Initialize datar// Initialize datae// Initialize datas// Initialize datas// Initialize data,// Initialize data // Initialize data3// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datae// Initialize datal// Initialize data.// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize dataC// Initialize datao// Initialize datan// Initialize datat// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datar// Initialize datao// Initialize datau// Initialize datan// Initialize datad// Initialize data(// Initialize datac// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize data+// Initialize data // Initialize data(// Initialize datat// Initialize dataa// Initialize datar// Initialize datag// Initialize datae// Initialize datat// Initialize data // Initialize data-// Initialize data // Initialize datac// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datat// Initialize data)// Initialize data // Initialize data*// Initialize data // Initialize datae// Initialize dataa// Initialize datas// Initialize datae// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datar// Initialize datao// Initialize datag// Initialize datar// Initialize datae// Initialize datas// Initialize datas// Initialize data // Initialize data<// Initialize data // Initialize data1// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize dataq// Initialize datau// Initialize datae// Initialize datas// Initialize datat// Initialize dataA// Initialize datan// Initialize datai// Initialize datam// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize dataF// Initialize datar// Initialize dataa// Initialize datam// Initialize datae// Initialize data(// Initialize datau// Initialize datap// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize dataq// Initialize datau// Initialize datae// Initialize datas// Initialize datat// Initialize dataA// Initialize datan// Initialize datai// Initialize datam// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize dataF// Initialize datar// Initialize dataa// Initialize datam// Initialize datae// Initialize data(// Initialize datau// Initialize datap// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataD// Initialize dataa// Initialize datas// Initialize datah// Initialize datab// Initialize datao// Initialize dataa// Initialize datar// Initialize datad// Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datad// Initialize dataa// Initialize datas// Initialize datah// Initialize datab// Initialize datao// Initialize dataa// Initialize datar// Initialize datad// Initialize data-// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data-// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datar// Initialize datae// Initialize datac// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datal// Initialize datai// Initialize datac// Initialize datae// Initialize data(// Initialize data0// Initialize data,// Initialize data // Initialize data8// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datar// Initialize datae// Initialize datac// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datat// Initialize datar// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data-// Initialize datar// Initialize datao// Initialize dataw// Initialize data"// Initialize data>// Initialize data<// Initialize datat// Initialize datad// Initialize data // Initialize datac// Initialize datao// Initialize datal// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data=// Initialize data"// Initialize data4// Initialize data"// Initialize data>// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data-// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datae// Initialize data-// Initialize datas// Initialize datam// Initialize data"// Initialize data>// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datai// Initialize datan// Initialize datab// Initialize datao// Initialize datax// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data // Initialize dataN// Initialize datae// Initialize datan// Initialize datah// Initialize datau// Initialize datam// Initialize dataa// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataÃ// Initialize data§// Initialize dataÃ// Initialize data£// Initialize datao// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data>// Initialize data<// Initialize data/// Initialize datat// Initialize datar// Initialize data>// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize datar// Initialize datae// Initialize datac// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data?// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datad// Initialize datao// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize dataC// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize data.// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae// Initialize data-// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae// Initialize data-// Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize dataL// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize data.// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize dataâ// Initialize data†// Initialize data“// Initialize data // Initialize dataE// Initialize datan// Initialize datat// Initialize datar// Initialize dataa// Initialize datad// Initialize dataa// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize dataâ// Initialize data†// Initialize data‘// Initialize data // Initialize dataS// Initialize dataa// Initialize dataÃ// Initialize data­// Initialize datad// Initialize dataa// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize dataq// Initialize datat// Initialize datay// Initialize dataP// Initialize datar// Initialize datae// Initialize dataf// Initialize datai// Initialize datax// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize data.// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize data+// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize data-// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data`// Initialize data<// Initialize datat// Initialize datar// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataw// Initialize data-// Initialize datab// Initialize datao// Initialize datal// Initialize datad// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae// Initialize data // Initialize data$// Initialize data{// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize dataC// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data}// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize dataL// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize data.// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize data-// Initialize datas// Initialize datau// Initialize datac// Initialize datac// Initialize datae// Initialize datas// Initialize datas// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize data-// Initialize datad// Initialize dataa// Initialize datan// Initialize datag// Initialize datae// Initialize datar// Initialize data'// Initialize data}// Initialize data // Initialize dataf// Initialize dataw// Initialize data-// Initialize datab// Initialize datao// Initialize datal// Initialize datad// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize dataq// Initialize datat// Initialize datay// Initialize dataP// Initialize datar// Initialize datae// Initialize dataf// Initialize datai// Initialize datax// Initialize data}// Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize data-// Initialize datam// Initialize datau// Initialize datat// Initialize datae// Initialize datad// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize data.// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data)// Initialize data}// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datat// Initialize datar// Initialize data>// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize dataj// Initialize datao// Initialize datai// Initialize datan// Initialize data(// Initialize data'// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataC// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data-// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize data{// Initialize data}// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize datas// Initialize data[// Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data]// Initialize data // Initialize data=// Initialize data // Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize datas// Initialize data[// Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data]// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data+// Initialize data // Initialize datap// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data(// Initialize data{// Initialize data // Initialize data.// Initialize data.// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize data,// Initialize data // Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data:// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize datas// Initialize data[// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datai// Initialize datad// Initialize data]// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data0// Initialize data // Initialize data}// Initialize data)// Initialize data) generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data) generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data.// Initialize datas// Initialize datao// Initialize datar// Initialize datat// Initialize data(// Initialize data(// Initialize dataa// Initialize data,// Initialize data // Initialize datab// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datab// Initialize data.// Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data-// Initialize data // Initialize dataa// Initialize data.// Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data-// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data"// Initialize data>// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data-// Initialize datap// Initialize datai// Initialize datae// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize dataC// Initialize dataa// Initialize datad// Initialize dataa// Initialize datas// Initialize datat// Initialize datar// Initialize datae// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize dataa// Initialize datax// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datam// Initialize dataa// Initialize datax// Initialize data(// Initialize data.// Initialize data.// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data)// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datam// Initialize dataa// Initialize datax// Initialize data(// Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data/// Initialize data // Initialize datam// Initialize dataa// Initialize datax// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data)// Initialize data // Initialize data*// Initialize data // Initialize data1// Initialize data0// Initialize data0// Initialize data,// Initialize data // Initialize data5// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data`// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data-// Initialize datab// Initialize dataa// Initialize datar// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data-// Initialize datab// Initialize dataa// Initialize datar// Initialize data-// Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data}// Initialize data // Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data-// Initialize datab// Initialize dataa// Initialize datar// Initialize data-// Initialize datat// Initialize datar// Initialize dataa// Initialize datac// Initialize datak// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data-// Initialize datab// Initialize dataa// Initialize datar// Initialize data-// Initialize dataf// Initialize datai// Initialize datal// Initialize datal// Initialize data"// Initialize data // Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize dataw// Initialize datai// Initialize datad// Initialize datat// Initialize datah// Initialize data:// Initialize data$// Initialize data{// Initialize datap// Initialize datac// Initialize datat// Initialize data}// Initialize data%// Initialize data;// Initialize data // Initialize datab// Initialize dataa// Initialize datac// Initialize datak// Initialize datag// Initialize datar// Initialize datao// Initialize datau// Initialize datan// Initialize datad// Initialize data:// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data}// Initialize data;// Initialize data"// Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize dataa// Initialize data-// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data=// Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datau// Initialize datan// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data-// Initialize datab// Initialize dataa// Initialize datar// Initialize data-// Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize dataj// Initialize datao// Initialize datai// Initialize datan// Initialize data(// Initialize data'// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataD// Initialize dataa// Initialize datas// Initialize datah// Initialize datab// Initialize datao// Initialize dataa// Initialize datar// Initialize datad// Initialize dataA// Initialize datal// Initialize datae// Initialize datar// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datad// Initialize dataa// Initialize datas// Initialize datah// Initialize datab// Initialize datao// Initialize dataa// Initialize datar// Initialize datad// Initialize data-// Initialize dataa// Initialize datal// Initialize datae// Initialize datar// Initialize datat// Initialize datas// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize dataa// Initialize datal// Initialize datae// Initialize datar// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataS// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data(// Initialize datap// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datao// Initialize datak// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize datas// Initialize datao// Initialize datar// Initialize datat// Initialize data(// Initialize data(// Initialize dataa// Initialize data,// Initialize data // Initialize datab// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize dataa// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data // Initialize data-// Initialize data // Initialize datab// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize dataa// Initialize datal// Initialize datae// Initialize datar// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataa// Initialize datal// Initialize datae// Initialize datar// Initialize datat// Initialize data-// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data"// Initialize data>// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datac// Initialize datai// Initialize datar// Initialize datac// Initialize datal// Initialize datae// Initialize data-// Initialize datac// Initialize datah// Initialize datae// Initialize datac// Initialize datak// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize dataT// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize data // Initialize datae// Initialize datas// Initialize datat// Initialize datao// Initialize dataq// Initialize datau// Initialize datae// Initialize data // Initialize dataa// Initialize datad// Initialize datae// Initialize dataq// Initialize datau// Initialize dataa// Initialize datad// Initialize datao// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize dataa// Initialize datal// Initialize datae// Initialize datar// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datal// Initialize datai// Initialize datac// Initialize datae// Initialize data(// Initialize data0// Initialize data,// Initialize data // Initialize data6// Initialize data)// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataS// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data(// Initialize datap// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datai// Initialize datas// Initialize dataC// Initialize datar// Initialize datai// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize datal// Initialize data // Initialize data=// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize datar// Initialize datai// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize datal// Initialize data'// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize dataC// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datai// Initialize datas// Initialize dataC// Initialize datar// Initialize datai// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize datal// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize datac// Initialize datar// Initialize datai// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize datal// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize dataa// Initialize datal// Initialize datae// Initialize datar// Initialize datat// Initialize dataC// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datai// Initialize datas// Initialize dataC// Initialize datar// Initialize datai// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize datal// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize dataq// Initialize datat// Initialize datay// Initialize dataC// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datai// Initialize datas// Initialize dataC// Initialize datar// Initialize datai// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize datal// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize datac// Initialize datar// Initialize datai// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize datal// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data`// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataa// Initialize datal// Initialize datae// Initialize datar// Initialize datat// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize dataa// Initialize datal// Initialize datae// Initialize datar// Initialize datat// Initialize dataC// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data}// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize data$// Initialize data{// Initialize datai// Initialize datas// Initialize dataC// Initialize datar// Initialize datai// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize datal// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize datac// Initialize datai// Initialize datar// Initialize datac// Initialize datal// Initialize datae// Initialize data-// Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize dataa// Initialize datam// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize datat// Initialize datar// Initialize datai// Initialize dataa// Initialize datan// Initialize datag// Initialize datal// Initialize datae// Initialize data-// Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize dataa// Initialize datam// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data'// Initialize data}// Initialize data // Initialize dataa// Initialize datal// Initialize datae// Initialize datar// Initialize datat// Initialize data-// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data // Initialize data$// Initialize data{// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize dataC// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data}// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataa// Initialize datal// Initialize datae// Initialize datar// Initialize datat// Initialize data-// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize data"// Initialize data>// Initialize data<// Initialize datas// Initialize datat// Initialize datar// Initialize datao// Initialize datan// Initialize datag// Initialize data>// Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datat// Initialize datar// Initialize datao// Initialize datan// Initialize datag// Initialize data>// Initialize data // Initialize dataâ// Initialize data€// Initialize data”// Initialize data // Initialize data$// Initialize data{// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataa// Initialize datal// Initialize datae// Initialize datar// Initialize datat// Initialize data-// Initialize dataq// Initialize datat// Initialize datay// Initialize data // Initialize data$// Initialize data{// Initialize dataq// Initialize datat// Initialize datay// Initialize dataC// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data}// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data}// Initialize data // Initialize datau// Initialize datan// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize dataj// Initialize datao// Initialize datai// Initialize datan// Initialize data(// Initialize data'// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataT// Initialize datao// Initialize datap// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datat// Initialize datao// Initialize datap// Initialize data-// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data-// Initialize datal// Initialize datai// Initialize datas// Initialize datat// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datao// Initialize datar// Initialize datat// Initialize datae// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize data[// Initialize data.// Initialize data.// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data] generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data(// Initialize data{// Initialize data // Initialize data.// Initialize data.// Initialize data.// Initialize datap// Initialize data,// Initialize data // Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data:// Initialize data // Initialize datap// Initialize data.// Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data // Initialize data*// Initialize data // Initialize datap// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data // Initialize data}// Initialize data)// Initialize data) generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data.// Initialize datas// Initialize datao// Initialize datar// Initialize datat// Initialize data(// Initialize data(// Initialize dataa// Initialize data,// Initialize data // Initialize datab// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datab// Initialize data.// Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data-// Initialize data // Initialize dataa// Initialize data.// Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data) generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data.// Initialize datas// Initialize datal// Initialize datai// Initialize datac// Initialize datae// Initialize data(// Initialize data0// Initialize data,// Initialize data // Initialize data5// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datas// Initialize datao// Initialize datar// Initialize datat// Initialize datae// Initialize datad// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data0// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize datas// Initialize datao// Initialize datar// Initialize datat// Initialize datae// Initialize datad// Initialize data[// Initialize data0// Initialize data]// Initialize data.// Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data-// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data"// Initialize data>// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datar// Initialize dataa// Initialize datan// Initialize datak// Initialize datai// Initialize datan// Initialize datag// Initialize data-// Initialize datas// Initialize datat// Initialize dataa// Initialize datar// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize dataC// Initialize dataa// Initialize datad// Initialize dataa// Initialize datas// Initialize datat// Initialize datar// Initialize datae// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datar// Initialize dataa// Initialize datan// Initialize datak// Initialize dataC// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize data[// Initialize data'// Initialize datag// Initialize datao// Initialize datal// Initialize datad// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datas// Initialize datai// Initialize datal// Initialize datav// Initialize datae// Initialize datar// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datab// Initialize datar// Initialize datao// Initialize datan// Initialize dataz// Initialize datae// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datan// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datal// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datan// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datal// Initialize data'// Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize datas// Initialize datao// Initialize datar// Initialize datat// Initialize datae// Initialize datad// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize data(// Initialize datap// Initialize data,// Initialize data // Initialize datai// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data`// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datat// Initialize datao// Initialize datap// Initialize data-// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datat// Initialize datao// Initialize datap// Initialize data-// Initialize datar// Initialize dataa// Initialize datan// Initialize datak// Initialize data // Initialize data$// Initialize data{// Initialize datar// Initialize dataa// Initialize datan// Initialize datak// Initialize dataC// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize datae// Initialize datas// Initialize data[// Initialize datai// Initialize data]// Initialize data}// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datai// Initialize data // Initialize data+// Initialize data // Initialize data1// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datat// Initialize datao// Initialize datap// Initialize data-// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datai// Initialize datan// Initialize dataf// Initialize datao// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datat// Initialize datao// Initialize datap// Initialize data-// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datat// Initialize datao// Initialize datap// Initialize data-// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datac// Initialize dataa// Initialize datat// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data?// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data // Initialize data+// Initialize data // Initialize data'// Initialize data // Initialize data'// Initialize data // Initialize data+// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize data'// Initialize data}// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datat// Initialize datao// Initialize datap// Initialize data-// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datat// Initialize dataC// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datac// Initialize datay// Initialize data(// Initialize datap// Initialize data.// Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data)// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize dataj// Initialize datao// Initialize datai// Initialize datan// Initialize data(// Initialize data'// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize dataP// Initialize dataR// Initialize dataO// Initialize dataD// Initialize dataU// Initialize dataC// Initialize dataT// Initialize dataS// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datao// Initialize datap// Initialize datau// Initialize datal// Initialize dataa// Initialize datat// Initialize datae// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data-// Initialize datat// Initialize dataa// Initialize datab// Initialize datal// Initialize datae// Initialize data-// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datal// Initialize datae// Initialize datat// Initialize data // Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize datae// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataF// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize datae// Initialize datad// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize datae// Initialize datad// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datat// Initialize datar// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data-// Initialize datar// Initialize datao// Initialize dataw// Initialize data"// Initialize data>// Initialize data<// Initialize datat// Initialize datad// Initialize data // Initialize datac// Initialize datao// Initialize datal// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data=// Initialize data"// Initialize data7// Initialize data"// Initialize data>// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data-// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datae// Initialize data"// Initialize data>// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datab// Initialize datao// Initialize datax// Initialize datae// Initialize datas// Initialize data-// Initialize datas// Initialize datat// Initialize dataa// Initialize datac// Initialize datak// Initialize datae// Initialize datad// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data<// Initialize datah// Initialize data3// Initialize data>// Initialize dataN// Initialize datae// Initialize datan// Initialize datah// Initialize datau// Initialize datam// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize datae// Initialize datan// Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize datar// Initialize dataa// Initialize datad// Initialize datao// Initialize data<// Initialize data/// Initialize datah// Initialize data3// Initialize data>// Initialize data<// Initialize datap// Initialize data>// Initialize dataA// Initialize dataj// Initialize datau// Initialize datas// Initialize datat// Initialize datae// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datar// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datau// Initialize data // Initialize datac// Initialize dataa// Initialize datad// Initialize dataa// Initialize datas// Initialize datat// Initialize datar// Initialize datae// Initialize data // Initialize datan// Initialize datao// Initialize datav// Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data<// Initialize data/// Initialize datap// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data>// Initialize data<// Initialize data/// Initialize datat// Initialize datar// Initialize data>// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize datae// Initialize datad// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataS// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data(// Initialize datap// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize dataI// Initialize datac// Initialize datao// Initialize datan// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data?// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize datað// Initialize dataŸ// Initialize data“// Initialize data¦// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize dataC// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data?// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize data#// Initialize data6// Initialize data6// Initialize data6// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data?// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize dataS// Initialize datae// Initialize datam// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data`// Initialize data<// Initialize datat// Initialize datar// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datac// Initialize datae// Initialize datal// Initialize datal// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data"// Initialize data // Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize datab// Initialize dataa// Initialize datac// Initialize datak// Initialize datag// Initialize datar// Initialize datao// Initialize datau// Initialize datan// Initialize datad// Initialize data:// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize dataC// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data}// Initialize data2// Initialize data0// Initialize data;// Initialize data // Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data:// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize dataC// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data}// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize dataI// Initialize datac// Initialize datao// Initialize datan// Initialize data}// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datai// Initialize datan// Initialize dataf// Initialize datao// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data'// Initialize data'// Initialize data}// Initialize data // Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize dataâ// Initialize data€// Initialize data¢// Initialize data // Initialize data'// Initialize data // Initialize data+// Initialize data // Initialize datap// Initialize data.// Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize data'// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data>// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data>// Initialize data$// Initialize data{// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datat// Initialize dataC// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datac// Initialize datay// Initialize data(// Initialize datap// Initialize data.// Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data)// Initialize data}// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataw// Initialize data-// Initialize datab// Initialize datao// Initialize datal// Initialize datad// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datat// Initialize dataC// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datac// Initialize datay// Initialize data(// Initialize datap// Initialize data.// Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data)// Initialize data}// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataw// Initialize data-// Initialize datab// Initialize datao// Initialize datal// Initialize datad// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data}// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae// Initialize data // Initialize data$// Initialize data{// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data}// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datat// Initialize dataa// Initialize datab// Initialize datal// Initialize datae// Initialize data-// Initialize dataa// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize datas// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datab// Initialize datau// Initialize datat// Initialize datat// Initialize datao// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datab// Initialize datat// Initialize datan// Initialize data-// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data-// Initialize datas// Initialize datam// Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data"// Initialize data // Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize dataN// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize dataa// Initialize datad// Initialize dataa// Initialize data"// Initialize data // Initialize datao// Initialize datan// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data=// Initialize data"// Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataQ// Initialize datau// Initialize datai// Initialize datac// Initialize datak// Initialize dataE// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data(// Initialize data'// Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data}// Initialize data'// Initialize data)// Initialize data"// Initialize data>// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize dataa// Initialize datar// Initialize datar// Initialize datao// Initialize dataw// Initialize data-// Initialize datad// Initialize datao// Initialize dataw// Initialize datan// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data<// Initialize data/// Initialize datab// Initialize datau// Initialize datat// Initialize datat// Initialize datao// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datab// Initialize datau// Initialize datat// Initialize datat// Initialize datao// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datab// Initialize datat// Initialize datan// Initialize data-// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data-// Initialize datas// Initialize datam// Initialize data // Initialize datae// Initialize datad// Initialize datai// Initialize datat// Initialize data"// Initialize data // Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize dataE// Initialize datad// Initialize datai// Initialize datat// Initialize dataa// Initialize datar// Initialize data"// Initialize data // Initialize datao// Initialize datan// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data=// Initialize data"// Initialize datae// Initialize datad// Initialize datai// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data(// Initialize data'// Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data}// Initialize data'// Initialize data)// Initialize data"// Initialize data>// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datap// Initialize datae// Initialize datan// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data<// Initialize data/// Initialize datab// Initialize datau// Initialize datat// Initialize datat// Initialize datao// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datab// Initialize datau// Initialize datat// Initialize datat// Initialize datao// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datab// Initialize datat// Initialize datan// Initialize data-// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data-// Initialize datas// Initialize datam// Initialize data // Initialize datad// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data"// Initialize data // Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize dataE// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data"// Initialize data // Initialize datao// Initialize datan// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data=// Initialize data"// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data(// Initialize data'// Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data}// Initialize data'// Initialize data)// Initialize data"// Initialize data>// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datat// Initialize datar// Initialize dataa// Initialize datas// Initialize datah// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data<// Initialize data/// Initialize datab// Initialize datau// Initialize datat// Initialize datat// Initialize datao// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datat// Initialize datar// Initialize data>// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize dataj// Initialize datao// Initialize datai// Initialize datan// Initialize data(// Initialize data'// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataF// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize datae// Initialize datad// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datae// Initialize dataa// Initialize datar// Initialize datac// Initialize datah// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datas// Initialize datae// Initialize dataa// Initialize datar// Initialize datac// Initialize datah// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data.// Initialize datat// Initialize datao// Initialize dataL// Initialize datao// Initialize dataw// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datas// Initialize datae// Initialize data(// Initialize data)// Initialize data.// Initialize datat// Initialize datar// Initialize datai// Initialize datam// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize dataF// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data-// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataF// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data-// Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datas// Initialize datae// Initialize dataa// Initialize datar// Initialize datac// Initialize datah// Initialize data // Initialize data&// Initialize data&// Initialize data // Initialize data!// Initialize datap// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data.// Initialize datat// Initialize datao// Initialize dataL// Initialize datao// Initialize dataw// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datas// Initialize datae// Initialize data(// Initialize data)// Initialize data.// Initialize datai// Initialize datan// Initialize datac// Initialize datal// Initialize datau// Initialize datad// Initialize datae// Initialize datas// Initialize data(// Initialize datas// Initialize datae// Initialize dataa// Initialize datar// Initialize datac// Initialize datah// Initialize data)// Initialize data // Initialize data&// Initialize data&// Initialize data // Initialize data!// Initialize data(// Initialize datap// Initialize data.// Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data'// Initialize data'// Initialize data)// Initialize data.// Initialize datat// Initialize datao// Initialize dataL// Initialize datao// Initialize dataw// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datas// Initialize datae// Initialize data(// Initialize data)// Initialize data.// Initialize datai// Initialize datan// Initialize datac// Initialize datal// Initialize datau// Initialize datad// Initialize datae// Initialize datas// Initialize data(// Initialize datas// Initialize datae// Initialize dataa// Initialize datar// Initialize datac// Initialize datah// Initialize data)// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize dataf// Initialize dataa// Initialize datal// Initialize datas// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize dataF// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data // Initialize data&// Initialize data&// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize dataF// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize dataf// Initialize dataa// Initialize datal// Initialize datas// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataF// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataS// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data(// Initialize datap// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataF// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize dataf// Initialize dataa// Initialize datal// Initialize datas// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize datat// Initialize datar// Initialize datau// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datap// Initialize datao// Initialize datap// Initialize datau// Initialize datal// Initialize dataa// Initialize datat// Initialize datae// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize data[ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data-// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize datas// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datas// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datat// Initialize dataV// Initialize dataa// Initialize datal// Initialize data // Initialize data=// Initialize data // Initialize datas// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datai// Initialize datas// Initialize dataF// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datas// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize data.// Initialize datai// Initialize datad// Initialize data.// Initialize datai// Initialize datan// Initialize datac// Initialize datal// Initialize datau// Initialize datad// Initialize datae// Initialize datas// Initialize data(// Initialize data'// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize datai// Initialize datas// Initialize dataF// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize data<// Initialize datao// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data=// Initialize data"// Initialize data"// Initialize data>// Initialize dataT// Initialize datao// Initialize datad// Initialize dataa// Initialize datas// Initialize data // Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize datas// Initialize data<// Initialize data/// Initialize datao// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data>// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data+// Initialize data=// Initialize data // Initialize data`// Initialize data<// Initialize datao// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data=// Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datai// Initialize datad// Initialize data}// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data}// Initialize data // Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data<// Initialize data/// Initialize datao// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data>// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datac// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datat// Initialize dataV// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize datas// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datat// Initialize dataV// Initialize dataa// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data-// Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize data.// Initialize datar// Initialize datae// Initialize datas// Initialize datae// Initialize datat// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datai// Initialize datad// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data0// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datao// Initialize datap// Initialize datau// Initialize datal// Initialize dataa// Initialize datat// Initialize datae// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datap// Initialize datae// Initialize datan// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data // Initialize dataE// Initialize datad// Initialize datai// Initialize datat// Initialize dataa// Initialize datar// Initialize data // Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datai// Initialize datad// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datai// Initialize datad// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data'// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize data-// Initialize datap// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize data-// Initialize datap// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datam// Initialize datai// Initialize datan// Initialize data-// Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data'// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize datar// Initialize datai// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize datar// Initialize datai// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data'// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize dataw// Initialize datai// Initialize datan// Initialize datae// Initialize data-// Initialize datab// Initialize datao// Initialize datat// Initialize datat// Initialize datal// Initialize datae// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data // Initialize dataN// Initialize datao// Initialize datav// Initialize datao// Initialize data // Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data(// Initialize datae// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datae// Initialize data.// Initialize datap// Initialize datar// Initialize datae// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataD// Initialize datae// Initialize dataf// Initialize dataa// Initialize datau// Initialize datal// Initialize datat// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datai// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datai// Initialize datad// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data // Initialize data=// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data.// Initialize datat// Initialize datar// Initialize datai// Initialize datam// Initialize data(// Initialize data)// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data:// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data.// Initialize datat// Initialize datar// Initialize datai// Initialize datam// Initialize data(// Initialize data)// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data:// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data.// Initialize datat// Initialize datar// Initialize datai// Initialize datam// Initialize data(// Initialize data)// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize datap// Initialize dataa// Initialize datar// Initialize datas// Initialize datae// Initialize dataF// Initialize datal// Initialize datao// Initialize dataa// Initialize datat// Initialize data(// Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize data-// Initialize datap// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data)// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data0// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data:// Initialize data // Initialize datap// Initialize dataa// Initialize datar// Initialize datas// Initialize datae// Initialize dataF// Initialize datal// Initialize datao// Initialize dataa// Initialize datat// Initialize data(// Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize data-// Initialize datap// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data)// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data0// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data:// Initialize data // Initialize datap// Initialize dataa// Initialize datar// Initialize datas// Initialize datae// Initialize dataI// Initialize datan// Initialize datat// Initialize data(// Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datam// Initialize datai// Initialize datan// Initialize data-// Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data)// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data0// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data:// Initialize data // Initialize datap// Initialize dataa// Initialize datar// Initialize datas// Initialize datae// Initialize dataI// Initialize datan// Initialize datat// Initialize data(// Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data)// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data0// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data:// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data.// Initialize datat// Initialize datar// Initialize datai// Initialize datam// Initialize data(// Initialize data)// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize datar// Initialize datai// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize datar// Initialize datai// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data.// Initialize datat// Initialize datar// Initialize datai// Initialize datam// Initialize data(// Initialize data)// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datak// Initialize datau// Initialize data:// Initialize data // Initialize data(// Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datas// Initialize datak// Initialize datau// Initialize data'// Initialize data)// Initialize data // Initialize data&// Initialize data&// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datas// Initialize datak// Initialize datau// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data.// Initialize datat// Initialize datar// Initialize datai// Initialize datam// Initialize data(// Initialize data)// Initialize data)// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data'// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datae// Initialize datax// Initialize datap// Initialize datai// Initialize datar// Initialize datay// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data:// Initialize data // Initialize data(// Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datae// Initialize datax// Initialize datap// Initialize datai// Initialize datar// Initialize datay// Initialize data-// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data'// Initialize data)// Initialize data // Initialize data&// Initialize data&// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datae// Initialize datax// Initialize datap// Initialize datai// Initialize datar// Initialize datay// Initialize data-// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data)// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data'// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize dataV// Initialize dataA// Initialize dataL// Initialize dataI// Initialize dataD// Initialize dataA// Initialize dataÇ// Initialize dataÕ// Initialize dataE// Initialize dataS// Initialize data // Initialize dataC// Initialize dataR// Initialize dataÍ// Initialize dataT// Initialize dataI// Initialize dataC// Initialize dataA// Initialize dataS// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataN// Initialize datao// Initialize datam// Initialize datae// Initialize data // Initialize datad// Initialize datao// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datao// Initialize datab// Initialize datar// Initialize datai// Initialize datag// Initialize dataa// Initialize datat// Initialize dataó// Initialize datar// Initialize datai// Initialize datao// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data // Initialize data>// Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize data❌// Initialize data // Initialize dataP// Initialize datar// Initialize datae// Initialize dataç// Initialize datao// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize dataa// Initialize data // Initialize datad// Initialize datae// Initialize datav// Initialize datae// Initialize data // Initialize datas// Initialize datae// Initialize datar// Initialize data // Initialize dataM// Initialize dataA// Initialize dataI// Initialize dataO// Initialize dataR// Initialize data // Initialize dataq// Initialize datau// Initialize datae// Initialize data // Initialize datap// Initialize datar// Initialize datae// Initialize dataç// Initialize datao// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datar// Initialize dataa// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data // Initialize data<// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataE// Initialize datas// Initialize datat// Initialize datao// Initialize dataq// Initialize datau// Initialize datae// Initialize data // Initialize datam// Initialize dataí// Initialize datan// Initialize datai// Initialize datam// Initialize datao// Initialize data // Initialize datan// Initialize dataã// Initialize datao// Initialize data // Initialize datap// Initialize datao// Initialize datad// Initialize datae// Initialize data // Initialize datas// Initialize datae// Initialize datar// Initialize data // Initialize datan// Initialize datae// Initialize datag// Initialize dataa// Initialize datat// Initialize datai// Initialize datav// Initialize datao// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data // Initialize data<// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataQ// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datad// Initialize dataa// Initialize datad// Initialize datae// Initialize data // Initialize datan// Initialize dataã// Initialize datao// Initialize data // Initialize datap// Initialize datao// Initialize datad// Initialize datae// Initialize data // Initialize datas// Initialize datae// Initialize datar// Initialize data // Initialize datan// Initialize datae// Initialize datag// Initialize dataa// Initialize datat// Initialize datai// Initialize datav// Initialize dataa// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataV// Initialize dataa// Initialize datal// Initialize datai// Initialize datad// Initialize dataa// Initialize datar// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datau// Initialize datap// Initialize datal// Initialize datai// Initialize datac// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize dataa// Initialize datap// Initialize datae// Initialize datan// Initialize dataa// Initialize datas// Initialize data // Initialize dataa// Initialize datao// Initialize data // Initialize datac// Initialize datar// Initialize datai// Initialize dataa// Initialize datar// Initialize data // Initialize datan// Initialize datao// Initialize datav// Initialize datao// Initialize data) generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datai// Initialize datad// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datad// Initialize datau// Initialize datap// Initialize datal// Initialize datai// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data.// Initialize datat// Initialize datao// Initialize dataL// Initialize datao// Initialize dataw// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datas// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data.// Initialize datat// Initialize datao// Initialize dataL// Initialize datao// Initialize dataw// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datas// Initialize datae// Initialize data(// Initialize data)// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datad// Initialize datau// Initialize datap// Initialize datal// Initialize datai// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize data⚠// Initialize data️// Initialize data // Initialize dataU// Initialize datam// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize data // Initialize datao// Initialize data // Initialize datan// Initialize datao// Initialize datam// Initialize datae// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize dataj// Initialize dataá// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize data!// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize datae// Initialize data // Initialize datau// Initialize datam// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datai// Initialize datad// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datai// Initialize datan// Initialize datad// Initialize datae// Initialize datax// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datan// Initialize datad// Initialize dataI// Initialize datan// Initialize datad// Initialize datae// Initialize datax// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datai// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datai// Initialize datan// Initialize datad// Initialize datae// Initialize datax// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize data-// Initialize data1// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data[// Initialize datai// Initialize datan// Initialize datad// Initialize datae// Initialize datax// Initialize data]// Initialize data // Initialize data=// Initialize data // Initialize data{// Initialize data // Initialize data.// Initialize data.// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data[// Initialize datai// Initialize datan// Initialize datad// Initialize datae// Initialize datax// Initialize data]// Initialize data,// Initialize data // Initialize data.// Initialize data.// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data // Initialize data}// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize dataa// Initialize datat// Initialize datau// Initialize dataa// Initialize datal// Initialize datai// Initialize dataz// Initialize dataa// Initialize datad// Initialize datao// Initialize data!// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datas// Initialize datau// Initialize datac// Initialize datac// Initialize datae// Initialize datas// Initialize datas// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize dataa// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datap// Initialize datau// Initialize datas// Initialize datah// Initialize data(// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datac// Initialize dataa// Initialize datad// Initialize dataa// Initialize datas// Initialize datat// Initialize datar// Initialize dataa// Initialize datad// Initialize datao// Initialize data!// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datas// Initialize datau// Initialize datac// Initialize datac// Initialize datae// Initialize datas// Initialize datas// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datad// Initialize datai// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data(// Initialize datai// Initialize datad// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datai// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data)// Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize datal// Initialize datae// Initialize datat// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize datal// Initialize datae// Initialize datat// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data(// Initialize datai// Initialize datad// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datai// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data  generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataV// Initialize datae// Initialize datar// Initialize datai// Initialize dataf// Initialize datai// Initialize datac// Initialize dataa// Initialize datar// Initialize data // Initialize datas// Initialize datae// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataõ// Initialize datae// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datai// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data  generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datai// Initialize datad// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data  generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datae// Initialize datas// Initialize datas// Initialize dataa// Initialize datag// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize dataC// Initialize datao// Initialize datan// Initialize datat// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize data`// Initialize data❌// Initialize data // Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataE// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datad// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data-// Initialize datab// Initialize datat// Initialize datan// Initialize data'// Initialize data)// Initialize data.// Initialize datad// Initialize datai// Initialize datas// Initialize dataa// Initialize datab// Initialize datal// Initialize datae// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datat// Initialize datar// Initialize datau// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datae// Initialize datas// Initialize datas// Initialize dataa// Initialize datag// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize dataC// Initialize datao// Initialize datan// Initialize datat// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize data`// Initialize dataT// Initialize datae// Initialize datam// Initialize data // Initialize datac// Initialize datae// Initialize datar// Initialize datat// Initialize datae// Initialize dataz// Initialize dataa// Initialize data // Initialize dataq// Initialize datau// Initialize datae// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datae// Initialize dataj// Initialize dataa// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data?// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datad// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data-// Initialize datab// Initialize datat// Initialize datan// Initialize data'// Initialize data)// Initialize data.// Initialize datad// Initialize datai// Initialize datas// Initialize dataa// Initialize datab// Initialize datal// Initialize datae// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize dataf// Initialize dataa// Initialize datal// Initialize datas// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data  generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataÃ// Initialize data­// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataÃ// Initialize data­// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize dataM// Initialize dataO// Initialize dataV// Initialize dataE// Initialize dataM// Initialize dataE// Initialize dataN// Initialize dataT// Initialize dataS// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data-// Initialize datat// Initialize dataa// Initialize datab// Initialize datal// Initialize datae// Initialize data-// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datal// Initialize datae// Initialize datat// Initialize data // Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize datae// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataF// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize datae// Initialize datad// Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize datae// Initialize datad// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datat// Initialize datar// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data-// Initialize datar// Initialize datao// Initialize dataw// Initialize data"// Initialize data>// Initialize data<// Initialize datat// Initialize datad// Initialize data // Initialize datac// Initialize datao// Initialize datal// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data=// Initialize data"// Initialize data6// Initialize data"// Initialize data>// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data-// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datae// Initialize data"// Initialize data>// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize dataa// Initialize datar// Initialize datar// Initialize datao// Initialize dataw// Initialize data-// Initialize datar// Initialize datai// Initialize datag// Initialize datah// Initialize datat// Initialize data-// Initialize dataa// Initialize datar// Initialize datar// Initialize datao// Initialize dataw// Initialize data-// Initialize datal// Initialize datae// Initialize dataf// Initialize datat// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data<// Initialize datah// Initialize data3// Initialize data>// Initialize dataN// Initialize datae// Initialize datan// Initialize datah// Initialize datau// Initialize datam// Initialize dataa// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataÃ// Initialize data§// Initialize dataÃ// Initialize data£// Initialize datao// Initialize data<// Initialize data/// Initialize datah// Initialize data3// Initialize data>// Initialize data<// Initialize datap// Initialize data>// Initialize dataR// Initialize datae// Initialize datag// Initialize datai// Initialize datas// Initialize datat// Initialize datar// Initialize datae// Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize dataa// Initialize datad// Initialize dataa// Initialize datas// Initialize data // Initialize datao// Initialize datau// Initialize data // Initialize datas// Initialize dataa// Initialize dataÃ// Initialize data­// Initialize datad// Initialize dataa// Initialize datas// Initialize data<// Initialize data/// Initialize datap// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data>// Initialize data<// Initialize data/// Initialize datat// Initialize datar// Initialize data>// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize datae// Initialize datad// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data?// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datad// Initialize datao// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datai// Initialize datas// Initialize dataE// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize data.// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data`// Initialize data<// Initialize datat// Initialize datar// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data>// Initialize data$// Initialize data{// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize dataT// Initialize datai// Initialize datam// Initialize datae// Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize data.// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data)// Initialize data}// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataw// Initialize data-// Initialize datab// Initialize datao// Initialize datal// Initialize datad// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae// Initialize data // Initialize data$// Initialize data{// Initialize datai// Initialize datas// Initialize dataE// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae// Initialize data-// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae// Initialize data-// Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize data'// Initialize data}// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datai// Initialize datas// Initialize dataE// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize dataâ// Initialize data†// Initialize data“// Initialize data // Initialize dataE// Initialize datan// Initialize datat// Initialize datar// Initialize dataa// Initialize datad// Initialize dataa// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize dataâ// Initialize data†// Initialize data‘// Initialize data // Initialize dataS// Initialize dataa// Initialize dataÃ// Initialize data­// Initialize datad// Initialize dataa// Initialize data'// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize data$// Initialize data{// Initialize datai// Initialize datas// Initialize dataE// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize data-// Initialize datas// Initialize datau// Initialize datac// Initialize datac// Initialize datae// Initialize datas// Initialize datas// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize data-// Initialize datad// Initialize dataa// Initialize datan// Initialize datag// Initialize datae// Initialize datar// Initialize data'// Initialize data}// Initialize data // Initialize dataf// Initialize dataw// Initialize data-// Initialize datab// Initialize datao// Initialize datal// Initialize datad// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datai// Initialize datas// Initialize dataE// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize data+// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize data-// Initialize data'// Initialize data}// Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data}// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data>// Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize data.// Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize data}// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize data-// Initialize datam// Initialize datau// Initialize datat// Initialize datae// Initialize datad// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize data.// Initialize datan// Initialize datao// Initialize datat// Initialize datae// Initialize datas// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data'// Initialize dataâ// Initialize data€// Initialize data”// Initialize data'// Initialize data}// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datat// Initialize datar// Initialize data>// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize dataj// Initialize datao// Initialize datai// Initialize datan// Initialize data(// Initialize data'// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataF// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize datae// Initialize datad// Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datae// Initialize dataa// Initialize datar// Initialize datac// Initialize datah// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datas// Initialize datae// Initialize dataa// Initialize datar// Initialize datac// Initialize datah// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data.// Initialize datat// Initialize datao// Initialize dataL// Initialize datao// Initialize dataw// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datas// Initialize datae// Initialize data(// Initialize data)// Initialize data.// Initialize datat// Initialize datar// Initialize datai// Initialize datam// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize dataF// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data-// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize dataS// Initialize datat// Initialize dataa// Initialize datar// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data-// Initialize datas// Initialize datat// Initialize dataa// Initialize datar// Initialize datat// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize dataE// Initialize datan// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data-// Initialize datae// Initialize datan// Initialize datad// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize dataF// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data // Initialize data&// Initialize data&// Initialize data // Initialize datam// Initialize data.// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize dataF// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize dataf// Initialize dataa// Initialize datal// Initialize datas// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datas// Initialize datae// Initialize dataa// Initialize datar// Initialize datac// Initialize datah// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data?// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data.// Initialize datat// Initialize datao// Initialize dataL// Initialize datao// Initialize dataw// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datas// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data.// Initialize datai// Initialize datan// Initialize datac// Initialize datal// Initialize datau// Initialize datad// Initialize datae// Initialize datas// Initialize data(// Initialize datas// Initialize datae// Initialize dataa// Initialize datar// Initialize datac// Initialize datah// Initialize data)// Initialize data // Initialize data&// Initialize data&// Initialize data // Initialize data!// Initialize datam// Initialize data.// Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize data.// Initialize datat// Initialize datao// Initialize dataL// Initialize datao// Initialize dataw// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datas// Initialize datae// Initialize data(// Initialize data)// Initialize data.// Initialize datai// Initialize datan// Initialize datac// Initialize datal// Initialize datau// Initialize datad// Initialize datae// Initialize datas// Initialize data(// Initialize datas// Initialize datae// Initialize dataa// Initialize datar// Initialize datac// Initialize datah// Initialize data)// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize dataf// Initialize dataa// Initialize datal// Initialize datas// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize dataS// Initialize datat// Initialize dataa// Initialize datar// Initialize datat// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize datam// Initialize data.// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data)// Initialize data.// Initialize datat// Initialize datao// Initialize dataI// Initialize dataS// Initialize dataO// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data)// Initialize data.// Initialize datas// Initialize datap// Initialize datal// Initialize datai// Initialize datat// Initialize data(// Initialize data'// Initialize dataT// Initialize data'// Initialize data)// Initialize data[// Initialize data0// Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data // Initialize data<// Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize dataS// Initialize datat// Initialize dataa// Initialize datar// Initialize datat// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize dataf// Initialize dataa// Initialize datal// Initialize datas// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize dataE// Initialize datan// Initialize datad// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize datam// Initialize data.// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data)// Initialize data.// Initialize datat// Initialize datao// Initialize dataI// Initialize dataS// Initialize dataO// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data)// Initialize data.// Initialize datas// Initialize datap// Initialize datal// Initialize datai// Initialize datat// Initialize data(// Initialize data'// Initialize dataT// Initialize data'// Initialize data)// Initialize data[// Initialize data0// Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data // Initialize data>// Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize dataE// Initialize datan// Initialize datad// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize dataf// Initialize dataa// Initialize datal// Initialize datas// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize datat// Initialize datar// Initialize datau// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data-// Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize datae// Initialize dataa// Initialize datad// Initialize datae// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data-// Initialize datah// Initialize datae// Initialize dataa// Initialize datad// Initialize datae// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datau// Initialize datab// Initialize datam// Initialize datai// Initialize datat// Initialize dataB// Initialize datat// Initialize datan// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datas// Initialize datau// Initialize datab// Initialize datam// Initialize datai// Initialize datat// Initialize data-// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datab// Initialize datat// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize data.// Initialize datar// Initialize datae// Initialize datas// Initialize datae// Initialize datat// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataP// Initialize datao// Initialize datap// Initialize datau// Initialize datal// Initialize dataa// Initialize datat// Initialize datae// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize datas// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datao// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data=// Initialize data"// Initialize data"// Initialize data>// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize datae// Initialize data // Initialize datau// Initialize datam// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data<// Initialize data/// Initialize datao// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data>// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize dataI// Initialize datac// Initialize datao// Initialize datan// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data?// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize datað// Initialize dataŸ// Initialize data“// Initialize data¦// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data+// Initialize data=// Initialize data // Initialize data`// Initialize data<// Initialize datao// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data=// Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data}// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize dataI// Initialize datac// Initialize datao// Initialize datan// Initialize data}// Initialize data // Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data // Initialize data(// Initialize datae// Initialize datas// Initialize datat// Initialize datao// Initialize dataq// Initialize datau// Initialize datae// Initialize data:// Initialize data // Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data}// Initialize data)// Initialize data<// Initialize data/// Initialize datao// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data>// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataP// Initialize datao// Initialize datap// Initialize datau// Initialize datal// Initialize dataa// Initialize datat// Initialize datae// Initialize data // Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize dataR// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize data[// Initialize data'// Initialize dataC// Initialize datao// Initialize datam// Initialize datap// Initialize datar// Initialize dataa// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize dataf// Initialize datao// Initialize datar// Initialize datan// Initialize datae// Initialize datac// Initialize datae// Initialize datad// Initialize datao// Initialize datar// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataD// Initialize datae// Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize dataÃ// Initialize data§// Initialize dataÃ// Initialize data£// Initialize datao// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize datac// Initialize datal// Initialize datai// Initialize datae// Initialize datan// Initialize datat// Initialize datae// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataA// Initialize dataj// Initialize datau// Initialize datas// Initialize datat// Initialize datae// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize datai// Initialize datan// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataÃ// Initialize data¡// Initialize datar// Initialize datai// Initialize datao// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataT// Initialize datar// Initialize dataa// Initialize datan// Initialize datas// Initialize dataf// Initialize datae// Initialize datar// Initialize dataÃ// Initialize dataª// Initialize datan// Initialize datac// Initialize datai// Initialize dataa// Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datae// Initialize data // Initialize datau// Initialize datan// Initialize datai// Initialize datad// Initialize dataa// Initialize datad// Initialize datae// Initialize datas// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataB// Initialize datao// Initialize datan// Initialize datai// Initialize dataf// Initialize datai// Initialize datac// Initialize dataa// Initialize dataÃ// Initialize data§// Initialize dataÃ// Initialize data£// Initialize datao// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataO// Initialize datau// Initialize datat// Initialize datar// Initialize datao// Initialize data'// Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize dataR// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize data[// Initialize data'// Initialize dataV// Initialize datae// Initialize datan// Initialize datad// Initialize dataa// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataQ// Initialize datau// Initialize datae// Initialize datab// Initialize datar// Initialize dataa// Initialize data/// Initialize dataA// Initialize datav// Initialize dataa// Initialize datar// Initialize datai// Initialize dataa// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataV// Initialize datae// Initialize datan// Initialize datac// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataC// Initialize datao// Initialize datan// Initialize datas// Initialize datau// Initialize datam// Initialize datao// Initialize data // Initialize datai// Initialize datan// Initialize datat// Initialize datae// Initialize datar// Initialize datan// Initialize datao// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataA// Initialize dataj// Initialize datau// Initialize datas// Initialize datat// Initialize datae// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize datai// Initialize datan// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataÃ// Initialize data¡// Initialize datar// Initialize datai// Initialize datao// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataT// Initialize datar// Initialize dataa// Initialize datan// Initialize datas// Initialize dataf// Initialize datae// Initialize datar// Initialize dataÃ// Initialize dataª// Initialize datan// Initialize datac// Initialize datai// Initialize dataa// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataO// Initialize datau// Initialize datat// Initialize datar// Initialize datao// Initialize data'// Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datao// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data=// Initialize data"// Initialize data"// Initialize data>// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize datae// Initialize data<// Initialize data/// Initialize datao// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data>// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data(// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data // Initialize data?// Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize dataR// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize datas// Initialize data // Initialize data:// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize dataR// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize datas// Initialize data)// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize datar// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data+// Initialize data=// Initialize data // Initialize data`// Initialize data<// Initialize datao// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data=// Initialize data"// Initialize data$// Initialize data{// Initialize datar// Initialize data}// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datar// Initialize data}// Initialize data<// Initialize data/// Initialize datao// Initialize datap// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data>// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize dataa// Initialize datar// Initialize datar// Initialize datao// Initialize dataw// Initialize data-// Initialize datad// Initialize datao// Initialize dataw// Initialize datan// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data // Initialize dataN// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize dataE// Initialize datan// Initialize datat// Initialize datar// Initialize dataa// Initialize datad// Initialize dataa// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datah// Initialize datae// Initialize dataa// Initialize datad// Initialize datae// Initialize datar// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data-// Initialize datah// Initialize datae// Initialize dataa// Initialize datad// Initialize datae// Initialize datar// Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datau// Initialize datab// Initialize datam// Initialize datai// Initialize datat// Initialize dataB// Initialize datat// Initialize datan// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize datab// Initialize datat// Initialize datan// Initialize data // Initialize datab// Initialize datat// Initialize datan// Initialize data-// Initialize datas// Initialize datau// Initialize datac// Initialize datac// Initialize datae// Initialize datas// Initialize datas// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datau// Initialize datab// Initialize datam// Initialize datai// Initialize datat// Initialize dataB// Initialize datat// Initialize datan// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize dataa// Initialize datar// Initialize datar// Initialize datao// Initialize dataw// Initialize data-// Initialize datad// Initialize datao// Initialize dataw// Initialize datan// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data // Initialize dataR// Initialize datae// Initialize datag// Initialize datai// Initialize datas// Initialize datat// Initialize datar// Initialize dataa// Initialize datar// Initialize data // Initialize dataE// Initialize datan// Initialize datat// Initialize datar// Initialize dataa// Initialize datad// Initialize dataa// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize dataa// Initialize datar// Initialize datar// Initialize datao// Initialize dataw// Initialize data-// Initialize datau// Initialize datap// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data // Initialize dataN// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize dataS// Initialize dataa// Initialize dataÃ// Initialize data­// Initialize datad// Initialize dataa// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datah// Initialize datae// Initialize dataa// Initialize datad// Initialize datae// Initialize datar// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data-// Initialize datah// Initialize datae// Initialize dataa// Initialize datad// Initialize datae// Initialize datar// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datau// Initialize datab// Initialize datam// Initialize datai// Initialize datat// Initialize dataB// Initialize datat// Initialize datan// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize datab// Initialize datat// Initialize datan// Initialize data // Initialize datab// Initialize datat// Initialize datan// Initialize data-// Initialize datad// Initialize dataa// Initialize datan// Initialize datag// Initialize datae// Initialize datar// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datau// Initialize datab// Initialize datam// Initialize datai// Initialize datat// Initialize dataB// Initialize datat// Initialize datan// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize dataa// Initialize datar// Initialize datar// Initialize datao// Initialize dataw// Initialize data-// Initialize datau// Initialize datap// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data // Initialize dataR// Initialize datae// Initialize datag// Initialize datai// Initialize datas// Initialize datat// Initialize datar// Initialize dataa// Initialize datar// Initialize data // Initialize dataS// Initialize dataa// Initialize dataÃ// Initialize data­// Initialize datad// Initialize dataa// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataQ// Initialize datau// Initialize datai// Initialize datac// Initialize datak// Initialize dataE// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data(// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data(// Initialize datae// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datae// Initialize data.// Initialize datap// Initialize datar// Initialize datae// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataD// Initialize datae// Initialize dataf// Initialize dataa// Initialize datau// Initialize datal// Initialize datat// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize dataa// Initialize datar// Initialize datas// Initialize datae// Initialize dataI// Initialize datan// Initialize datat// Initialize data(// Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data)// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data0// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datan// Initialize datao// Initialize datat// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datan// Initialize datao// Initialize datat// Initialize datae// Initialize datas// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data.// Initialize datat// Initialize datar// Initialize datai// Initialize datam// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data // Initialize data<// Initialize data=// Initialize data // Initialize data0// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataP// Initialize datar// Initialize datae// Initialize datae// Initialize datan// Initialize datac// Initialize datah// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datac// Initialize dataa// Initialize datam// Initialize datap// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datab// Initialize datar// Initialize datai// Initialize datag// Initialize dataa// Initialize datat// Initialize dataÃ// Initialize data³// Initialize datar// Initialize datai// Initialize datao// Initialize datas// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize datan// Initialize dataÃ// Initialize data£// Initialize datao// Initialize data // Initialize datae// Initialize datan// Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize datar// Initialize dataa// Initialize datad// Initialize datao// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize data'// Initialize data // Initialize data&// Initialize data&// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data // Initialize data>// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataE// Initialize datas// Initialize datat// Initialize datao// Initialize dataq// Initialize datau// Initialize datae// Initialize data // Initialize datai// Initialize datan// Initialize datas// Initialize datau// Initialize dataf// Initialize datai// Initialize datac// Initialize datai// Initialize datae// Initialize datan// Initialize datat// Initialize datae// Initialize data!// Initialize data // Initialize dataD// Initialize datai// Initialize datas// Initialize datap// Initialize datao// Initialize datan// Initialize dataÃ// Initialize data­// Initialize datav// Initialize datae// Initialize datal// Initialize data:// Initialize data // Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data}// Initialize data // Initialize datau// Initialize datan// Initialize datai// Initialize datad// Initialize dataa// Initialize datad// Initialize datae// Initialize datas// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataU// Initialize datap// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data // Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data // Initialize data+// Initialize data=// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data // Initialize data-// Initialize data=// Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataC// Initialize datar// Initialize datae// Initialize dataa// Initialize datat// Initialize datae// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize datar// Initialize datae// Initialize datac// Initialize datao// Initialize datar// Initialize datad generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize datad// Initialize data:// Initialize data // Initialize datag// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize dataa// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data(// Initialize data)// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize dataa// Initialize datas// Initialize datao// Initialize datan// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datan// Initialize datao// Initialize datat// Initialize datae// Initialize datas// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data:// Initialize data // Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data.// Initialize datat// Initialize datao// Initialize dataI// Initialize dataS// Initialize dataO// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data)// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize datau// Initialize datan// Initialize datas// Initialize datah// Initialize datai// Initialize dataf// Initialize datat// Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data( generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data`// Initialize data$// Initialize data{// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize dataE// Initialize datan// Initialize datat// Initialize datar// Initialize dataa// Initialize datad// Initialize dataa// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize dataS// Initialize dataa// Initialize dataÃ// Initialize data­// Initialize datad// Initialize dataa// Initialize data'// Initialize data}// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize data$// Initialize data{// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data}// Initialize datax// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datar// Initialize datae// Initialize datag// Initialize datai// Initialize datas// Initialize datat// Initialize datar// Initialize dataa// Initialize datad// Initialize dataa// Initialize data!// Initialize data`// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize datas// Initialize datau// Initialize datac// Initialize datac// Initialize datae// Initialize datas// Initialize datas// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data' generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize dataf// Initialize datar// Initialize datae// Initialize datas// Initialize datah// Initialize data // Initialize datac// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize datap// Initialize dataa// Initialize datag// Initialize datae generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize dataa// Initialize datac// Initialize datat// Initialize datai// Initialize datav// Initialize datae// Initialize dataP// Initialize dataa// Initialize datag// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize datae// Initialize datar// Initialize datay// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize datao// Initialize datar// Initialize data(// Initialize data'// Initialize data.// Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize data.// Initialize dataa// Initialize datac// Initialize datat// Initialize datai// Initialize datav// Initialize datae// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize dataa// Initialize datac// Initialize datat// Initialize datai// Initialize datav// Initialize datae// Initialize dataP// Initialize dataa// Initialize datag// Initialize datae// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize dataa// Initialize datac// Initialize datat// Initialize datai// Initialize datav// Initialize datae// Initialize dataP// Initialize dataa// Initialize datag// Initialize datae// Initialize data.// Initialize datai// Initialize datad// Initialize data.// Initialize datar// Initialize datae// Initialize datap// Initialize datal// Initialize dataa// Initialize datac// Initialize datae// Initialize data(// Initialize data'// Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize data-// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datan// Initialize dataa// Initialize datav// Initialize datai// Initialize datag// Initialize dataa// Initialize datat// Initialize datae// Initialize dataT// Initialize datao// Initialize data(// Initialize datap// Initialize dataa// Initialize datag// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize dataC// Initialize dataA// Initialize dataT// Initialize dataE// Initialize dataG// Initialize dataO// Initialize dataR// Initialize dataI// Initialize dataE// Initialize dataS// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data-// Initialize datag// Initialize datar// Initialize datai// Initialize datad// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data-// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datae// Initialize data"// Initialize data // Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize datag// Initialize datar// Initialize datai// Initialize datad// Initialize data-// Initialize datac// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datan// Initialize data:// Initialize data // Initialize data1// Initialize data/// Initialize data-// Initialize data1// Initialize data"// Initialize data>// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datat// Initialize dataa// Initialize datag// Initialize datas// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data<// Initialize datah// Initialize data3// Initialize data>// Initialize dataN// Initialize datae// Initialize datan// Initialize datah// Initialize datau// Initialize datam// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data<// Initialize data/// Initialize datah// Initialize data3// Initialize data>// Initialize data<// Initialize datap// Initialize data>// Initialize dataC// Initialize datar// Initialize datai// Initialize datae// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize datas// Initialize data // Initialize datap// Initialize dataa// Initialize datar// Initialize dataa// Initialize data // Initialize datao// Initialize datar// Initialize datag// Initialize dataa// Initialize datan// Initialize datai// Initialize dataz// Initialize dataa// Initialize datar// Initialize data // Initialize datas// Initialize datae// Initialize datau// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data<// Initialize data/// Initialize datap// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datai// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datai// Initialize datad// Initialize data)// Initialize data.// Initialize datar// Initialize datae// Initialize datad// Initialize datau// Initialize datac// Initialize datae// Initialize data(// Initialize data(// Initialize datas// Initialize datau// Initialize datam// Initialize data,// Initialize data // Initialize datap// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datas// Initialize datau// Initialize datam// Initialize data // Initialize data+// Initialize data // Initialize datap// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data,// Initialize data // Initialize data0// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data`// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data-// Initialize datac// Initialize dataa// Initialize datar// Initialize datad// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize dataa// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize datas// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datab// Initialize datau// Initialize datat// Initialize datat// Initialize datao// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datab// Initialize datat// Initialize datan// Initialize data-// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data-// Initialize datas// Initialize datam// Initialize data // Initialize datae// Initialize datad// Initialize datai// Initialize datat// Initialize data"// Initialize data // Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize dataE// Initialize datad// Initialize datai// Initialize datat// Initialize dataa// Initialize datar// Initialize data"// Initialize data // Initialize datao// Initialize datan// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data=// Initialize data"// Initialize datae// Initialize datad// Initialize datai// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data(// Initialize data'// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datai// Initialize datad// Initialize data}// Initialize data'// Initialize data)// Initialize data"// Initialize data>// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datap// Initialize datae// Initialize datan// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data<// Initialize data/// Initialize datab// Initialize datau// Initialize datat// Initialize datat// Initialize datao// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datab// Initialize datau// Initialize datat// Initialize datat// Initialize datao// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datab// Initialize datat// Initialize datan// Initialize data-// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data-// Initialize datas// Initialize datam// Initialize data // Initialize datad// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data"// Initialize data // Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize dataE// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data"// Initialize data // Initialize datao// Initialize datan// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data=// Initialize data"// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data(// Initialize data'// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datai// Initialize datad// Initialize data}// Initialize data'// Initialize data)// Initialize data"// Initialize data>// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datat// Initialize datar// Initialize dataa// Initialize datas// Initialize datah// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data<// Initialize data/// Initialize datab// Initialize datau// Initialize datat// Initialize datat// Initialize datao// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize data1// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize datas// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize data'// Initialize data}// Initialize data // Initialize dataâ// Initialize data€// Initialize data¢// Initialize data // Initialize data$// Initialize data{// Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data}// Initialize data // Initialize datau// Initialize datan// Initialize data // Initialize datae// Initialize datam// Initialize data // Initialize datae// Initialize datas// Initialize datat// Initialize datao// Initialize dataq// Initialize datau// Initialize datae// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datac// Initialize dataa// Initialize datat// Initialize data-// Initialize datab// Initialize dataa// Initialize datar// Initialize data"// Initialize data // Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize datab// Initialize dataa// Initialize datac// Initialize datak// Initialize datag// Initialize datar// Initialize datao// Initialize datau// Initialize datan// Initialize datad// Initialize data:// Initialize data // Initialize datal// Initialize datai// Initialize datan// Initialize datae// Initialize dataa// Initialize datar// Initialize data-// Initialize datag// Initialize datar// Initialize dataa// Initialize datad// Initialize datai// Initialize datae// Initialize datan// Initialize datat// Initialize data(// Initialize data9// Initialize data0// Initialize datad// Initialize datae// Initialize datag// Initialize data,// Initialize data // Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data}// Initialize data,// Initialize data // Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data}// Initialize data4// Initialize data0// Initialize data)// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize dataj// Initialize datao// Initialize datai// Initialize datan// Initialize data(// Initialize data'// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data-// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data-// Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize data.// Initialize datar// Initialize datae// Initialize datas// Initialize datae// Initialize datat// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data-// Initialize datai// Initialize datad// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data-// Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data#// Initialize datae// Initialize data8// Initialize dataa// Initialize data3// Initialize data3// Initialize datad// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datap// Initialize datae// Initialize datan// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data // Initialize dataE// Initialize datad// Initialize datai// Initialize datat// Initialize dataa// Initialize datar// Initialize data // Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data-// Initialize datai// Initialize datad// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data.// Initialize datai// Initialize datad// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data-// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data-// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data.// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data-// Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data.// Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datat// Initialize dataa// Initialize datag// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data // Initialize dataN// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data(// Initialize datae// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datae// Initialize data.// Initialize datap// Initialize datar// Initialize datae// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataD// Initialize datae// Initialize dataf// Initialize dataa// Initialize datau// Initialize datal// Initialize datat// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datai// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data-// Initialize datai// Initialize datad// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data // Initialize data=// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data:// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data-// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data.// Initialize datat// Initialize datar// Initialize datai// Initialize datam// Initialize data(// Initialize data)// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data:// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data-// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data.// Initialize datat// Initialize datar// Initialize datai// Initialize datam// Initialize data(// Initialize data)// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data'// Initialize datað// Initialize dataŸ// Initialize data“// Initialize data¦// Initialize data'// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data:// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data-// Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datac// Initialize dataa// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataI// Initialize datan// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize datae// Initialize data // Initialize datao// Initialize data // Initialize datan// Initialize datao// Initialize datam// Initialize datae// Initialize data // Initialize datad// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datai// Initialize datad// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datai// Initialize datan// Initialize datad// Initialize datae// Initialize datax// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datan// Initialize datad// Initialize dataI// Initialize datan// Initialize datad// Initialize datae// Initialize datax// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datai// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datai// Initialize datan// Initialize datad// Initialize datae// Initialize datax// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize data-// Initialize data1// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data[// Initialize datai// Initialize datan// Initialize datad// Initialize datae// Initialize datax// Initialize data]// Initialize data // Initialize data=// Initialize data // Initialize data{// Initialize data // Initialize data.// Initialize data.// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data[// Initialize datai// Initialize datan// Initialize datad// Initialize datae// Initialize datax// Initialize data]// Initialize data,// Initialize data // Initialize data.// Initialize data.// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data // Initialize data}// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize dataa// Initialize datat// Initialize datau// Initialize dataa// Initialize datal// Initialize datai// Initialize dataz// Initialize dataa// Initialize datad// Initialize dataa// Initialize data!// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datas// Initialize datau// Initialize datac// Initialize datac// Initialize datae// Initialize datas// Initialize datas// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize dataa// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize datap// Initialize datau// Initialize datas// Initialize datah// Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datac// Initialize datar// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data!// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datas// Initialize datau// Initialize datac// Initialize datac// Initialize datae// Initialize datas// Initialize datas// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datad// Initialize datai// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data(// Initialize datai// Initialize datad// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datai// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize data)// Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data(// Initialize datai// Initialize datad// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datai// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datac// Initialize dataa// Initialize datat// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datai// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datai// Initialize datad// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datae// Initialize datas// Initialize datas// Initialize dataa// Initialize datag// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize dataC// Initialize datao// Initialize datan// Initialize datat// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize data`// Initialize dataA// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize datau// Initialize datai// Initialize data // Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data.// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datae// Initialize datas// Initialize datas// Initialize dataa// Initialize datag// Initialize datae// Initialize data'// Initialize data)// Initialize data.// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize dataC// Initialize datao// Initialize datan// Initialize datat// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize data`// Initialize dataE// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data?// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize dataR// Initialize dataE// Initialize dataP// Initialize dataO// Initialize dataR// Initialize dataT// Initialize dataS// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataR// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataR// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize dataC// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataR// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataW// Initialize datae// Initialize datae// Initialize datak// Initialize dataC// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataR// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataL// Initialize datao// Initialize dataw// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataR// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataM// Initialize datao// Initialize datas// Initialize datat// Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datad// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataR// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataS// Initialize datau// Initialize datam// Initialize datam// Initialize dataa// Initialize datar// Initialize datay// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataR// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize dataC// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datar// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize data-// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data-// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datai// Initialize datad// Initialize data) generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data.// Initialize datar// Initialize datae// Initialize datad// Initialize datau// Initialize datac// Initialize datae// Initialize data(// Initialize data(// Initialize datas// Initialize datau// Initialize datam// Initialize data,// Initialize data // Initialize datap// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datas// Initialize datau// Initialize datam// Initialize data // Initialize data+// Initialize data // Initialize data(// Initialize datap// Initialize data.// Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data // Initialize data*// Initialize data // Initialize datap// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data)// Initialize data,// Initialize data // Initialize data0// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data{// Initialize data // Initialize data.// Initialize data.// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize data,// Initialize data // Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data}// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data.// Initialize datas// Initialize datao// Initialize datar// Initialize datat// Initialize data(// Initialize data(// Initialize dataa// Initialize data,// Initialize data // Initialize datab// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datab// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data-// Initialize data // Initialize dataa// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data-// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data"// Initialize data>// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data-// Initialize datab// Initialize dataa// Initialize datar// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize dataS// Initialize datae// Initialize datam// Initialize data // Initialize datad// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize dataa// Initialize datar// Initialize dataa// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datab// Initialize datai// Initialize datar// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize dataa// Initialize datax// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datam// Initialize dataa// Initialize datax// Initialize data(// Initialize data.// Initialize data.// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize datas// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data)// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize datas// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datam// Initialize dataa// Initialize datax// Initialize data(// Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data/// Initialize data // Initialize datam// Initialize dataa// Initialize datax// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data)// Initialize data // Initialize data*// Initialize data // Initialize data1// Initialize data0// Initialize data0// Initialize data,// Initialize data // Initialize data8// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data`// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datah// Initialize data-// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datah// Initialize data-// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data-// Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data}// Initialize data // Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datah// Initialize data-// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data-// Initialize datab// Initialize dataa// Initialize datar// Initialize data-// Initialize dataw// Initialize datar// Initialize dataa// Initialize datap// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datah// Initialize data-// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data-// Initialize datab// Initialize dataa// Initialize datar// Initialize data"// Initialize data // Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize dataw// Initialize datai// Initialize datad// Initialize datat// Initialize datah// Initialize data:// Initialize data$// Initialize data{// Initialize datap// Initialize datac// Initialize datat// Initialize data}// Initialize data%// Initialize data;// Initialize data // Initialize datab// Initialize dataa// Initialize datac// Initialize datak// Initialize datag// Initialize datar// Initialize datao// Initialize datau// Initialize datan// Initialize datad// Initialize data:// Initialize data // Initialize datal// Initialize datai// Initialize datan// Initialize datae// Initialize dataa// Initialize datar// Initialize data-// Initialize datag// Initialize datar// Initialize dataa// Initialize datad// Initialize datai// Initialize datae// Initialize datan// Initialize datat// Initialize data(// Initialize data9// Initialize data0// Initialize datad// Initialize datae// Initialize datag// Initialize data,// Initialize data // Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data}// Initialize data,// Initialize data // Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data}// Initialize data9// Initialize data9// Initialize data)// Initialize data;// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datat// Initialize dataC// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datac// Initialize datay// Initialize data(// Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data)// Initialize data}// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize dataj// Initialize datao// Initialize datai// Initialize datan// Initialize data(// Initialize data'// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataR// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataW// Initialize datae// Initialize datae// Initialize datak// Initialize dataC// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datar// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize data-// Initialize dataw// Initialize datae// Initialize datae// Initialize datak// Initialize data-// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize data[// Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize dataL// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize data[// Initialize data'// Initialize dataD// Initialize datao// Initialize datam// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataS// Initialize datae// Initialize datag// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataT// Initialize datae// Initialize datar// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataQ// Initialize datau// Initialize dataa// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataQ// Initialize datau// Initialize datai// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataS// Initialize datae// Initialize datax// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataS// Initialize dataÃ// Initialize data¡// Initialize datab// Initialize data'// Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datan// Initialize datao// Initialize dataw// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize dataf// Initialize datao// Initialize datar// Initialize data // Initialize data(// Initialize datal// Initialize datae// Initialize datat// Initialize data // Initialize datai// Initialize data // Initialize data=// Initialize data // Initialize data6// Initialize data;// Initialize data // Initialize datai// Initialize data // Initialize data>// Initialize data=// Initialize data // Initialize data0// Initialize data;// Initialize data // Initialize datai// Initialize data-// Initialize data-// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize datan// Initialize datao// Initialize dataw// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data.// Initialize datas// Initialize datae// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datai// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize dataS// Initialize datat// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data.// Initialize datat// Initialize datao// Initialize dataI// Initialize dataS// Initialize dataO// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data)// Initialize data.// Initialize datas// Initialize datap// Initialize datal// Initialize datai// Initialize datat// Initialize data(// Initialize data'// Initialize dataT// Initialize data'// Initialize data)// Initialize data[// Initialize data0// Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize dataL// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize datas// Initialize data[// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataD// Initialize dataa// Initialize datay// Initialize data(// Initialize data)// Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data // Initialize data&// Initialize data&// Initialize data // Initialize datam// Initialize data.// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data.// Initialize datas// Initialize datat// Initialize dataa// Initialize datar// Initialize datat// Initialize datas// Initialize dataW// Initialize datai// Initialize datat// Initialize datah// Initialize data(// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize dataS// Initialize datat// Initialize datar// Initialize data)// Initialize data)// Initialize data.// Initialize datar// Initialize datae// Initialize datad// Initialize datau// Initialize datac// Initialize datae// Initialize data(// Initialize data(// Initialize datas// Initialize datau// Initialize datam// Initialize data,// Initialize data // Initialize datam// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datas// Initialize datau// Initialize datam// Initialize data // Initialize data+// Initialize data // Initialize datam// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data,// Initialize data // Initialize data0// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize data'// Initialize data // Initialize data&// Initialize data&// Initialize data // Initialize datam// Initialize data.// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data.// Initialize datas// Initialize datat// Initialize dataa// Initialize datar// Initialize datat// Initialize datas// Initialize dataW// Initialize datai// Initialize datat// Initialize datah// Initialize data(// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize dataS// Initialize datat// Initialize datar// Initialize data)// Initialize data)// Initialize data.// Initialize datar// Initialize datae// Initialize datad// Initialize datau// Initialize datac// Initialize datae// Initialize data(// Initialize data(// Initialize datas// Initialize datau// Initialize datam// Initialize data,// Initialize data // Initialize datam// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datas// Initialize datau// Initialize datam// Initialize data // Initialize data+// Initialize data // Initialize datam// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data,// Initialize data // Initialize data0// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize data.// Initialize datap// Initialize datau// Initialize datas// Initialize datah// Initialize data(// Initialize data{// Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize dataS// Initialize datat// Initialize datar// Initialize data,// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data,// Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datai// Initialize datas// Initialize dataT// Initialize datao// Initialize datad// Initialize dataa// Initialize datay// Initialize data:// Initialize data // Initialize datai// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data0// Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize dataa// Initialize datax// Initialize dataV// Initialize dataa// Initialize datal// Initialize data // Initialize data=// Initialize data // Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datam// Initialize dataa// Initialize datax// Initialize data(// Initialize data.// Initialize data.// Initialize data.// Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datad// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datam// Initialize dataa// Initialize datax// Initialize data(// Initialize datad// Initialize data.// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datad// Initialize data.// Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize datas// Initialize data)// Initialize data)// Initialize data,// Initialize data // Initialize data1// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize datas// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datad// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize dataH// Initialize data // Initialize data=// Initialize data // Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datam// Initialize dataa// Initialize datax// Initialize data(// Initialize data(// Initialize datad// Initialize data.// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data/// Initialize data // Initialize datam// Initialize dataa// Initialize datax// Initialize dataV// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data*// Initialize data // Initialize data1// Initialize data0// Initialize data0// Initialize data,// Initialize data // Initialize data2// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize dataH// Initialize data // Initialize data=// Initialize data // Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datam// Initialize dataa// Initialize datax// Initialize data(// Initialize data(// Initialize datad// Initialize data.// Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize datas// Initialize data // Initialize data/// Initialize data // Initialize datam// Initialize dataa// Initialize datax// Initialize dataV// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data*// Initialize data // Initialize data1// Initialize data0// Initialize data0// Initialize data,// Initialize data // Initialize data2// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data`// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataw// Initialize datae// Initialize datae// Initialize datak// Initialize data-// Initialize datad// Initialize dataa// Initialize datay// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataw// Initialize datae// Initialize datae// Initialize datak// Initialize data-// Initialize datad// Initialize dataa// Initialize datay// Initialize data-// Initialize datab// Initialize dataa// Initialize datar// Initialize datas// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataw// Initialize datae// Initialize datae// Initialize datak// Initialize data-// Initialize datab// Initialize dataa// Initialize datar// Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data"// Initialize data // Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize datah// Initialize datae// Initialize datai// Initialize datag// Initialize datah// Initialize datat// Initialize data:// Initialize data$// Initialize data{// Initialize datad// Initialize data.// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data // Initialize data?// Initialize data // Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize dataH// Initialize data // Initialize data:// Initialize data // Initialize data2// Initialize data}// Initialize data%// Initialize data"// Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize dataa// Initialize data-// Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data=// Initialize data"// Initialize dataâ// Initialize data†// Initialize data“// Initialize data$// Initialize data{// Initialize datad// Initialize data.// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data}// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataw// Initialize datae// Initialize datae// Initialize datak// Initialize data-// Initialize datab// Initialize dataa// Initialize datar// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize datas// Initialize data"// Initialize data // Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize datah// Initialize datae// Initialize datai// Initialize datag// Initialize datah// Initialize datat// Initialize data:// Initialize data$// Initialize data{// Initialize datad// Initialize data.// Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize datas// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data // Initialize data?// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize dataH// Initialize data // Initialize data:// Initialize data // Initialize data2// Initialize data}// Initialize data%// Initialize data"// Initialize data // Initialize datad// Initialize dataa// Initialize datat// Initialize dataa// Initialize data-// Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data=// Initialize data"// Initialize dataâ// Initialize data†// Initialize data‘// Initialize data$// Initialize data{// Initialize datad// Initialize data.// Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize datas// Initialize data}// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataw// Initialize datae// Initialize datae// Initialize datak// Initialize data-// Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data"// Initialize data // Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize data$// Initialize data{// Initialize datad// Initialize data.// Initialize datai// Initialize datas// Initialize dataT// Initialize datao// Initialize datad// Initialize dataa// Initialize datay// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data:// Initialize datav// Initialize dataa// Initialize datar// Initialize data(// Initialize data-// Initialize data-// Initialize dataa// Initialize datac// Initialize datac// Initialize datae// Initialize datan// Initialize datat// Initialize data)// Initialize data;// Initialize dataf// Initialize datao// Initialize datan// Initialize datat// Initialize data-// Initialize dataw// Initialize datae// Initialize datai// Initialize datag// Initialize datah// Initialize datat// Initialize data:// Initialize data7// Initialize data0// Initialize data0// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize data'// Initialize data}// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datad// Initialize data.// Initialize datad// Initialize dataa// Initialize datay// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize dataj// Initialize datao// Initialize datai// Initialize datan// Initialize data(// Initialize data'// Initialize data'// Initialize data)// Initialize data // Initialize data+// Initialize data // Initialize data` generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize datap// Initialize datao// Initialize datas// Initialize datai// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data:// Initialize dataa// Initialize datab// Initialize datas// Initialize datao// Initialize datal// Initialize datau// Initialize datat// Initialize datae// Initialize data;// Initialize datat// Initialize datao// Initialize datap// Initialize data:// Initialize data0// Initialize data;// Initialize datar// Initialize datai// Initialize datag// Initialize datah// Initialize datat// Initialize data:// Initialize data0// Initialize data;// Initialize datad// Initialize datai// Initialize datas// Initialize datap// Initialize datal// Initialize dataa// Initialize datay// Initialize data:// Initialize dataf// Initialize datal// Initialize datae// Initialize datax// Initialize data;// Initialize datag// Initialize dataa// Initialize datap// Initialize data:// Initialize data1// Initialize datar// Initialize datae// Initialize datam// Initialize data;// Initialize dataf// Initialize datao// Initialize datan// Initialize datat// Initialize data-// Initialize datas// Initialize datai// Initialize dataz// Initialize datae// Initialize data:// Initialize data0// Initialize data.// Initialize data7// Initialize datar// Initialize datae// Initialize datam// Initialize data;// Initialize datac// Initialize datao// Initialize datal// Initialize datao// Initialize datar// Initialize data:// Initialize datav// Initialize dataa// Initialize datar// Initialize data(// Initialize data-// Initialize data-// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize data-// Initialize datam// Initialize datau// Initialize datat// Initialize datae// Initialize datad// Initialize data)// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize datad// Initialize datai// Initialize datas// Initialize datap// Initialize datal// Initialize dataa// Initialize datay// Initialize data:// Initialize dataf// Initialize datal// Initialize datae// Initialize datax// Initialize data;// Initialize dataa// Initialize datal// Initialize datai// Initialize datag// Initialize datan// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize datas// Initialize data:// Initialize datac// Initialize datae// Initialize datan// Initialize datat// Initialize datae// Initialize datar// Initialize data;// Initialize datag// Initialize dataa// Initialize datap// Initialize data:// Initialize data4// Initialize datap// Initialize datax// Initialize data"// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize dataw// Initialize datai// Initialize datad// Initialize datat// Initialize datah// Initialize data:// Initialize data1// Initialize data0// Initialize datap// Initialize datax// Initialize data;// Initialize datah// Initialize datae// Initialize datai// Initialize datag// Initialize datah// Initialize datat// Initialize data:// Initialize data1// Initialize data0// Initialize datap// Initialize datax// Initialize data;// Initialize datab// Initialize datao// Initialize datar// Initialize datad// Initialize datae// Initialize datar// Initialize data-// Initialize datar// Initialize dataa// Initialize datad// Initialize datai// Initialize datau// Initialize datas// Initialize data:// Initialize data2// Initialize datap// Initialize datax// Initialize data;// Initialize datab// Initialize dataa// Initialize datac// Initialize datak// Initialize datag// Initialize datar// Initialize datao// Initialize datau// Initialize datan// Initialize datad// Initialize data:// Initialize datav// Initialize dataa// Initialize datar// Initialize data(// Initialize data-// Initialize data-// Initialize datas// Initialize datau// Initialize datac// Initialize datac// Initialize datae// Initialize datas// Initialize datas// Initialize data)// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize dataE// Initialize datan// Initialize datat// Initialize datar// Initialize dataa// Initialize datad// Initialize dataa// Initialize datas// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize datad// Initialize datai// Initialize datas// Initialize datap// Initialize datal// Initialize dataa// Initialize datay// Initialize data:// Initialize dataf// Initialize datal// Initialize datae// Initialize datax// Initialize data;// Initialize dataa// Initialize datal// Initialize datai// Initialize datag// Initialize datan// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize datas// Initialize data:// Initialize datac// Initialize datae// Initialize datan// Initialize datat// Initialize datae// Initialize datar// Initialize data;// Initialize datag// Initialize dataa// Initialize datap// Initialize data:// Initialize data4// Initialize datap// Initialize datax// Initialize data"// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize dataw// Initialize datai// Initialize datad// Initialize datat// Initialize datah// Initialize data:// Initialize data1// Initialize data0// Initialize datap// Initialize datax// Initialize data;// Initialize datah// Initialize datae// Initialize datai// Initialize datag// Initialize datah// Initialize datat// Initialize data:// Initialize data1// Initialize data0// Initialize datap// Initialize datax// Initialize data;// Initialize datab// Initialize datao// Initialize datar// Initialize datad// Initialize datae// Initialize datar// Initialize data-// Initialize datar// Initialize dataa// Initialize datad// Initialize datai// Initialize datau// Initialize datas// Initialize data:// Initialize data2// Initialize datap// Initialize datax// Initialize data;// Initialize datab// Initialize dataa// Initialize datac// Initialize datak// Initialize datag// Initialize datar// Initialize datao// Initialize datau// Initialize datan// Initialize datad// Initialize data:// Initialize datav// Initialize dataa// Initialize datar// Initialize data(// Initialize data-// Initialize data-// Initialize datad// Initialize dataa// Initialize datan// Initialize datag// Initialize datae// Initialize datar// Initialize data)// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize dataS// Initialize dataa// Initialize dataÃ// Initialize data­// Initialize datad// Initialize dataa// Initialize datas// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data.// Initialize datap// Initialize datao// Initialize datas// Initialize datai// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize datar// Initialize datae// Initialize datal// Initialize dataa// Initialize datat// Initialize datai// Initialize datav// Initialize datae// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataR// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataL// Initialize datao// Initialize dataw// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datar// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize data-// Initialize datal// Initialize datao// Initialize dataw// Initialize data-// Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data-// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datal// Initialize datao// Initialize dataw// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataS// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data(// Initialize datap// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datao// Initialize datak// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize datas// Initialize datao// Initialize datar// Initialize datat// Initialize data(// Initialize data(// Initialize dataa// Initialize data,// Initialize data // Initialize datab// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize dataa// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data // Initialize data-// Initialize data // Initialize datab// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datal// Initialize datao// Initialize dataw// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datat// Initialize datar// Initialize data>// Initialize data<// Initialize datat// Initialize datad// Initialize data // Initialize datac// Initialize datao// Initialize datal// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data=// Initialize data"// Initialize data6// Initialize data"// Initialize data>// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data-// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datae// Initialize data-// Initialize datas// Initialize datam// Initialize data"// Initialize data>// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datac// Initialize datai// Initialize datar// Initialize datac// Initialize datal// Initialize datae// Initialize data-// Initialize datac// Initialize datah// Initialize datae// Initialize datac// Initialize datak// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data // Initialize dataT// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize data // Initialize datae// Initialize datas// Initialize datat// Initialize datao// Initialize dataq// Initialize datau// Initialize datae// Initialize data // Initialize dataa// Initialize datad// Initialize datae// Initialize dataq// Initialize datau// Initialize dataa// Initialize datad// Initialize datao// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data>// Initialize data<// Initialize data/// Initialize datat// Initialize datar// Initialize data>// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datat// Initialize datab// Initialize datao// Initialize datad// Initialize datay// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize datal// Initialize datao// Initialize dataw// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataS// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data(// Initialize datap// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datad// Initialize datae// Initialize dataf// Initialize datai// Initialize datac// Initialize datai// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize dataM// Initialize dataa// Initialize datat// Initialize datah// Initialize data.// Initialize datam// Initialize dataa// Initialize datax// Initialize data(// Initialize data0// Initialize data,// Initialize data // Initialize datap// Initialize data.// Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data // Initialize data-// Initialize data // Initialize datap// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datau// Initialize datag// Initialize datag// Initialize datae// Initialize datas// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datae// Initialize dataf// Initialize datai// Initialize datac// Initialize datai// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data // Initialize data?// Initialize data // Initialize data`// Initialize dataR// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize data // Initialize data$// Initialize data{// Initialize datad// Initialize datae// Initialize dataf// Initialize datai// Initialize datac// Initialize datai// Initialize datat// Initialize data}// Initialize data // Initialize datau// Initialize datan// Initialize data`// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize dataM// Initialize datao// Initialize datan// Initialize datai// Initialize datat// Initialize datao// Initialize datar// Initialize dataa// Initialize datar// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data`// Initialize data<// Initialize datat// Initialize datar// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataw// Initialize data-// Initialize datab// Initialize datao// Initialize datal// Initialize datad// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data>// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data?// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data // Initialize data+// Initialize data // Initialize data'// Initialize data // Initialize data'// Initialize data // Initialize data+// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize dataâ// Initialize data€// Initialize data”// Initialize data'// Initialize data}// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize data$// Initialize data{// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data'// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize datar// Initialize datai// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize datal// Initialize data'// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize data-// Initialize datad// Initialize dataa// Initialize datan// Initialize datag// Initialize datae// Initialize datar// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize data-// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data}// Initialize data // Initialize dataf// Initialize dataw// Initialize data-// Initialize datab// Initialize datao// Initialize datal// Initialize datad// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data}// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data>// Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data}// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datab// Initialize dataa// Initialize datad// Initialize datag// Initialize datae// Initialize data // Initialize data$// Initialize data{// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data}// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datat// Initialize datad// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize data-// Initialize dataa// Initialize datac// Initialize datac// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize dataf// Initialize dataw// Initialize data-// Initialize datab// Initialize datao// Initialize datal// Initialize datad// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datas// Initialize datau// Initialize datag// Initialize datag// Initialize datae// Initialize datas// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datat// Initialize datad// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datat// Initialize datar// Initialize data>// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize dataj// Initialize datao// Initialize datai// Initialize datan// Initialize data(// Initialize data'// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataR// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataM// Initialize datao// Initialize datas// Initialize datat// Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datad// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datar// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize data-// Initialize datam// Initialize datao// Initialize datas// Initialize datat// Initialize data-// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datad// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize data{// Initialize data}// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize datas// Initialize data[// Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data]// Initialize data // Initialize data=// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize datas// Initialize data[// Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data]// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data+// Initialize data // Initialize datam// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datao// Initialize datar// Initialize datat// Initialize datae// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize dataO// Initialize datab// Initialize dataj// Initialize datae// Initialize datac// Initialize datat// Initialize data.// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize datas// Initialize data) generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize data(// Initialize data[// Initialize datai// Initialize datad// Initialize data,// Initialize data // Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data]// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data(// Initialize data{// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data:// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datai// Initialize datad// Initialize data)// Initialize data,// Initialize data // Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data}// Initialize data)// Initialize data) generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data) generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data.// Initialize datas// Initialize datao// Initialize datar// Initialize datat// Initialize data(// Initialize data(// Initialize dataa// Initialize data,// Initialize data // Initialize datab// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datab// Initialize data.// Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data-// Initialize data // Initialize dataa// Initialize data.// Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data) generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data.// Initialize datas// Initialize datal// Initialize datai// Initialize datac// Initialize datae// Initialize data(// Initialize data0// Initialize data,// Initialize data // Initialize data7// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datas// Initialize datao// Initialize datar// Initialize datat// Initialize datae// Initialize datad// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datat// Initialize data-// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data"// Initialize data>// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize dataf// Initialize datai// Initialize datar// Initialize datae// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize dataS// Initialize datae// Initialize datam// Initialize data // Initialize datad// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize dataa// Initialize datax// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datas// Initialize datao// Initialize datar// Initialize datat// Initialize datae// Initialize datad// Initialize data[// Initialize data0// Initialize data]// Initialize data.// Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize datas// Initialize datao// Initialize datar// Initialize datat// Initialize datae// Initialize datad// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize data(// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data,// Initialize data // Initialize datai// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize data(// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data.// Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data/// Initialize data // Initialize datam// Initialize dataa// Initialize datax// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data)// Initialize data // Initialize data*// Initialize data // Initialize data1// Initialize data0// Initialize data0// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data`// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datam// Initialize datao// Initialize datas// Initialize datat// Initialize data-// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datad// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datam// Initialize datao// Initialize datas// Initialize datat// Initialize data-// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datad// Initialize data-// Initialize datar// Initialize dataa// Initialize datan// Initialize datak// Initialize data"// Initialize data>// Initialize data#// Initialize data$// Initialize data{// Initialize datai// Initialize data // Initialize data+// Initialize data // Initialize data1// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datam// Initialize datao// Initialize datas// Initialize datat// Initialize data-// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datad// Initialize data-// Initialize datai// Initialize datan// Initialize dataf// Initialize datao// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datam// Initialize datao// Initialize datas// Initialize datat// Initialize data-// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datad// Initialize data-// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datam// Initialize datao// Initialize datas// Initialize datat// Initialize data-// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datad// Initialize data-// Initialize datab// Initialize dataa// Initialize datar// Initialize data-// Initialize dataw// Initialize datar// Initialize dataa// Initialize datap// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datam// Initialize datao// Initialize datas// Initialize datat// Initialize data-// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datad// Initialize data-// Initialize datab// Initialize dataa// Initialize datar// Initialize data"// Initialize data // Initialize datas// Initialize datat// Initialize datay// Initialize datal// Initialize datae// Initialize data=// Initialize data"// Initialize dataw// Initialize datai// Initialize datad// Initialize datat// Initialize datah// Initialize data:// Initialize data$// Initialize data{// Initialize datap// Initialize datac// Initialize datat// Initialize data}// Initialize data%// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datam// Initialize datao// Initialize datas// Initialize datat// Initialize data-// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datad// Initialize data-// Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data.// Initialize datac// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datau// Initialize datan// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize dataj// Initialize datao// Initialize datai// Initialize datan// Initialize data(// Initialize data'// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataR// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataS// Initialize datau// Initialize datam// Initialize datam// Initialize dataa// Initialize datar// Initialize datay// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datar// Initialize datae// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize data-// Initialize datas// Initialize datau// Initialize datam// Initialize datam// Initialize dataa// Initialize datar// Initialize datay// Initialize data-// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datas// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataU// Initialize datan// Initialize datai// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datar// Initialize datae// Initialize datad// Initialize datau// Initialize datac// Initialize datae// Initialize data(// Initialize data(// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datas// Initialize data // Initialize data+// Initialize data // Initialize datap// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data,// Initialize data // Initialize data0// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize dataP// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datar// Initialize datae// Initialize datad// Initialize datau// Initialize datac// Initialize datae// Initialize data(// Initialize data(// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datas// Initialize data // Initialize data+// Initialize data // Initialize data(// Initialize datap// Initialize data.// Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data // Initialize data*// Initialize data // Initialize datap// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data)// Initialize data,// Initialize data // Initialize data0// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize dataS// Initialize dataa// Initialize datal// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datar// Initialize datae// Initialize datad// Initialize datau// Initialize datac// Initialize datae// Initialize data(// Initialize data(// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datas// Initialize data // Initialize data+// Initialize data // Initialize data(// Initialize datap// Initialize data.// Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data // Initialize data*// Initialize data // Initialize datap// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data)// Initialize data,// Initialize data // Initialize data0// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datao// Initialize datat// Initialize datae// Initialize datan// Initialize datat// Initialize datai// Initialize dataa// Initialize datal// Initialize dataP// Initialize datar// Initialize datao// Initialize dataf// Initialize datai// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize dataS// Initialize dataa// Initialize datal// Initialize datae// Initialize data // Initialize data-// Initialize data // Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize dataP// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize dataa// Initialize datav// Initialize datag// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data // Initialize data?// Initialize data // Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize dataS// Initialize dataa// Initialize datal// Initialize datae// Initialize data // Initialize data/// Initialize data // Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data:// Initialize data // Initialize data0// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data` generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data"// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data"// Initialize data>// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataC// Initialize dataa// Initialize datad// Initialize dataa// Initialize datas// Initialize datat// Initialize datar// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data"// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data"// Initialize data>// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize datas// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data"// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data"// Initialize data>// Initialize dataU// Initialize datan// Initialize datai// Initialize datad// Initialize dataa// Initialize datad// Initialize datae// Initialize datas// Initialize data // Initialize datae// Initialize datam// Initialize data // Initialize dataE// Initialize datas// Initialize datat// Initialize datao// Initialize dataq// Initialize datau// Initialize datae// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataU// Initialize datan// Initialize datai// Initialize datat// Initialize datas// Initialize data.// Initialize datat// Initialize datao// Initialize dataL// Initialize datao// Initialize datac// Initialize dataa// Initialize datal// Initialize datae// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data'// Initialize datap// Initialize datat// Initialize data-// Initialize dataB// Initialize dataR// Initialize data'// Initialize data)// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data"// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data"// Initialize data>// Initialize dataV// Initialize dataa// Initialize datal// Initialize datao// Initialize datar// Initialize data // Initialize dataC// Initialize datau// Initialize datas// Initialize datat// Initialize datao// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datat// Initialize dataC// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datac// Initialize datay// Initialize data(// Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize dataP// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize data)// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data"// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data"// Initialize data>// Initialize dataV// Initialize dataa// Initialize datal// Initialize datao// Initialize datar// Initialize data // Initialize dataV// Initialize datae// Initialize datan// Initialize datad// Initialize dataa// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize data-// Initialize dataa// Initialize datac// Initialize datac// Initialize datae// Initialize datan// Initialize datat// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datat// Initialize dataC// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datac// Initialize datay// Initialize data(// Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataV// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize dataS// Initialize dataa// Initialize datal// Initialize datae// Initialize data)// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data"// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data"// Initialize data>// Initialize dataL// Initialize datau// Initialize datac// Initialize datar// Initialize datao// Initialize data // Initialize dataP// Initialize datao// Initialize datat// Initialize datae// Initialize datan// Initialize datac// Initialize datai// Initialize dataa// Initialize datal// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize data-// Initialize datas// Initialize datau// Initialize datac// Initialize datac// Initialize datae// Initialize datas// Initialize datas// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datat// Initialize dataC// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datac// Initialize datay// Initialize data(// Initialize datap// Initialize datao// Initialize datat// Initialize datae// Initialize datan// Initialize datat// Initialize datai// Initialize dataa// Initialize datal// Initialize dataP// Initialize datar// Initialize datao// Initialize dataf// Initialize datai// Initialize datat// Initialize data)// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data"// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data"// Initialize data>// Initialize dataP// Initialize datar// Initialize datae// Initialize dataÃ// Initialize data§// Initialize datao// Initialize data // Initialize dataM// Initialize dataÃ// Initialize data©// Initialize datad// Initialize datai// Initialize datao// Initialize data // Initialize dataV// Initialize datae// Initialize datan// Initialize datad// Initialize dataa// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize dataa// Initialize datat// Initialize dataC// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datac// Initialize datay// Initialize data(// Initialize dataa// Initialize datav// Initialize datag// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data)// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data"// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal// Initialize data"// Initialize data>// Initialize dataT// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize data // Initialize dataM// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataÃ// Initialize data§// Initialize dataÃ// Initialize dataµ// Initialize datae// Initialize datas// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize data-// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datat// Initialize datao// Initialize datat// Initialize dataa// Initialize datal// Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data}// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize dataC// Initialize dataS// Initialize dataV// Initialize data // Initialize dataE// Initialize dataX// Initialize dataP// Initialize dataO// Initialize dataR// Initialize dataT// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataC// Initialize dataS// Initialize dataV// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize datae// Initialize dataa// Initialize datad// Initialize datae// Initialize datar// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize data[// Initialize data'// Initialize dataN// Initialize datao// Initialize datam// Initialize datae// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataM// Initialize dataa// Initialize datar// Initialize datac// Initialize dataa// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataV// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataP// Initialize datar// Initialize datae// Initialize dataÃ// Initialize data§// Initialize datao// Initialize data // Initialize dataC// Initialize datao// Initialize datam// Initialize datap// Initialize datar// Initialize dataa// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataP// Initialize datar// Initialize datae// Initialize dataÃ// Initialize data§// Initialize datao// Initialize data // Initialize dataV// Initialize datae// Initialize datan// Initialize datad// Initialize dataa// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataE// Initialize datas// Initialize datat// Initialize datao// Initialize dataq// Initialize datau// Initialize datae// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataE// Initialize datas// Initialize datat// Initialize datao// Initialize dataq// Initialize datau// Initialize datae// Initialize data // Initialize dataM// Initialize dataÃ// Initialize data­// Initialize datan// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataF// Initialize datao// Initialize datar// Initialize datan// Initialize datae// Initialize datac// Initialize datae// Initialize datad// Initialize datao// Initialize datar// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize dataS// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data'// Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datar// Initialize datao// Initialize dataw// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataS// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data(// Initialize datap// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data[ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data,// Initialize data // Initialize datap// Initialize data.// Initialize datab// Initialize datar// Initialize dataa// Initialize datan// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data'// Initialize data'// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data?// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize data'// Initialize data,// Initialize data // Initialize datap// Initialize data.// Initialize datav// Initialize datao// Initialize datal// Initialize datau// Initialize datam// Initialize datae// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize data.// Initialize datap// Initialize datau// Initialize datar// Initialize datac// Initialize datah// Initialize dataa// Initialize datas// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data.// Initialize datat// Initialize datao// Initialize dataF// Initialize datai// Initialize datax// Initialize datae// Initialize datad// Initialize data(// Initialize data2// Initialize data)// Initialize data,// Initialize data // Initialize datap// Initialize data.// Initialize datas// Initialize dataa// Initialize datal// Initialize datae// Initialize dataP// Initialize datar// Initialize datai// Initialize datac// Initialize datae// Initialize data.// Initialize datat// Initialize datao// Initialize dataF// Initialize datai// Initialize datax// Initialize datae// Initialize datad// Initialize data(// Initialize data2// Initialize data)// Initialize data, generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data,// Initialize data // Initialize datap// Initialize data.// Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data,// Initialize data // Initialize datap// Initialize data.// Initialize datas// Initialize datau// Initialize datap// Initialize datap// Initialize datal// Initialize datai// Initialize datae// Initialize datar// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data'// Initialize data'// Initialize data,// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datal// Initialize dataa// Initialize datab// Initialize datae// Initialize datal generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data]// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize datas// Initialize datav// Initialize dataC// Initialize datao// Initialize datan// Initialize datat// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize data[// Initialize datah// Initialize datae// Initialize dataa// Initialize datad// Initialize datae// Initialize datar// Initialize datas// Initialize data,// Initialize data // Initialize data.// Initialize data.// Initialize data.// Initialize datar// Initialize datao// Initialize dataw// Initialize datas// Initialize data]// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datar// Initialize datao// Initialize dataw// Initialize data // Initialize data=// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datao// Initialize dataw// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datac// Initialize datae// Initialize datal// Initialize datal// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data`// Initialize data"// Initialize data$// Initialize data{// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize datac// Initialize datae// Initialize datal// Initialize datal// Initialize data)// Initialize data.// Initialize datar// Initialize datae// Initialize datap// Initialize datal// Initialize dataa// Initialize datac// Initialize datae// Initialize data(// Initialize data/// Initialize data"// Initialize data/// Initialize datag// Initialize data,// Initialize data // Initialize data'// Initialize data"// Initialize data"// Initialize data'// Initialize data)// Initialize data}// Initialize data"// Initialize data`// Initialize data)// Initialize data.// Initialize dataj// Initialize datao// Initialize datai// Initialize datan// Initialize data(// Initialize data'// Initialize data,// Initialize data'// Initialize data) generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data)// Initialize data.// Initialize dataj// Initialize datao// Initialize datai// Initialize datan// Initialize data(// Initialize data'// Initialize data\// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize dataB// Initialize dataO// Initialize dataM// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data\// Initialize datau// Initialize dataF// Initialize dataE// Initialize dataF// Initialize dataF// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datab// Initialize datal// Initialize datao// Initialize datab// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataB// Initialize datal// Initialize datao// Initialize datab// Initialize data(// Initialize data[// Initialize dataB// Initialize dataO// Initialize dataM// Initialize data // Initialize data+// Initialize data // Initialize datac// Initialize datas// Initialize datav// Initialize dataC// Initialize datao// Initialize datan// Initialize datat// Initialize datae// Initialize datan// Initialize datat// Initialize data]// Initialize data,// Initialize data // Initialize data{// Initialize data // Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data:// Initialize data // Initialize data'// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize data/// Initialize datac// Initialize datas// Initialize datav// Initialize data;// Initialize datac// Initialize datah// Initialize dataa// Initialize datar// Initialize datas// Initialize datae// Initialize datat// Initialize data=// Initialize datau// Initialize datat// Initialize dataf// Initialize data-// Initialize data8// Initialize data;// Initialize data'// Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datau// Initialize datar// Initialize datal// Initialize data // Initialize data=// Initialize data // Initialize dataU// Initialize dataR// Initialize dataL// Initialize data.// Initialize datac// Initialize datar// Initialize datae// Initialize dataa// Initialize datat// Initialize datae// Initialize dataO// Initialize datab// Initialize dataj// Initialize datae// Initialize datac// Initialize datat// Initialize dataU// Initialize dataR// Initialize dataL// Initialize data(// Initialize datab// Initialize datal// Initialize datao// Initialize datab// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datal// Initialize datai// Initialize datan// Initialize datak// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datac// Initialize datar// Initialize datae// Initialize dataa// Initialize datat// Initialize datae// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data(// Initialize data'// Initialize dataa// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datal// Initialize datai// Initialize datan// Initialize datak// Initialize data.// Initialize datah// Initialize datar// Initialize datae// Initialize dataf// Initialize data // Initialize data=// Initialize data // Initialize datau// Initialize datar// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datal// Initialize datai// Initialize datan// Initialize datak// Initialize data.// Initialize datad// Initialize datao// Initialize dataw// Initialize datan// Initialize datal// Initialize datao// Initialize dataa// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize data`// Initialize datab// Initialize datae// Initialize datav// Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data_// Initialize datae// Initialize datas// Initialize datat// Initialize datao// Initialize dataq// Initialize datau// Initialize datae// Initialize data_// Initialize data$// Initialize data{// Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data.// Initialize datat// Initialize datao// Initialize dataI// Initialize dataS// Initialize dataO// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data)// Initialize data.// Initialize datas// Initialize datap// Initialize datal// Initialize datai// Initialize datat// Initialize data(// Initialize data'// Initialize dataT// Initialize data'// Initialize data)// Initialize data[// Initialize data0// Initialize data]// Initialize data}// Initialize data.// Initialize datac// Initialize datas// Initialize datav// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datal// Initialize datai// Initialize datan// Initialize datak// Initialize data.// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize dataU// Initialize dataR// Initialize dataL// Initialize data.// Initialize datar// Initialize datae// Initialize datav// Initialize datao// Initialize datak// Initialize datae// Initialize dataO// Initialize datab// Initialize dataj// Initialize datae// Initialize datac// Initialize datat// Initialize dataU// Initialize dataR// Initialize dataL// Initialize data(// Initialize datau// Initialize datar// Initialize datal// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datal// Initialize dataa// Initialize datat// Initialize dataÃ// Initialize data³// Initialize datar// Initialize datai// Initialize datao// Initialize data // Initialize datae// Initialize datax// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datad// Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize data // Initialize datas// Initialize datau// Initialize datac// Initialize datae// Initialize datas// Initialize datas// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datas// Initialize datau// Initialize datac// Initialize datac// Initialize datae// Initialize datas// Initialize datas// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize dataN// Initialize dataO// Initialize dataT// Initialize dataI// Initialize dataF// Initialize dataI// Initialize dataC// Initialize dataA// Initialize dataT// Initialize dataI// Initialize dataO// Initialize dataN// Initialize dataS// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataN// Initialize datao// Initialize datat// Initialize datai// Initialize dataf// Initialize datai// Initialize datac// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize datas// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datan// Initialize datao// Initialize datat// Initialize datai// Initialize dataf// Initialize datai// Initialize datac// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data-// Initialize datal// Initialize datai// Initialize datas// Initialize datat// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize dataa// Initialize datal// Initialize datae// Initialize datar// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataS// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data(// Initialize datap// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datao// Initialize datak// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize datas// Initialize datao// Initialize datar// Initialize datat// Initialize data(// Initialize data(// Initialize dataa// Initialize data,// Initialize data // Initialize datab// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize dataa// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data // Initialize data-// Initialize data // Initialize datab// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize dataa// Initialize datal// Initialize datae// Initialize datar// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datan// Initialize datao// Initialize datat// Initialize datai// Initialize dataf// Initialize datai// Initialize datac// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data-// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data"// Initialize data>// Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize datac// Initialize datai// Initialize datar// Initialize datac// Initialize datal// Initialize datae// Initialize data-// Initialize datac// Initialize datah// Initialize datae// Initialize datac// Initialize datak// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data>// Initialize data<// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize dataN// Initialize datae// Initialize datan// Initialize datah// Initialize datau// Initialize datam// Initialize data // Initialize dataa// Initialize datal// Initialize datae// Initialize datar// Initialize datat// Initialize dataa// Initialize data // Initialize dataa// Initialize datat// Initialize datai// Initialize datav// Initialize datao// Initialize data<// Initialize data/// Initialize datas// Initialize datap// Initialize dataa// Initialize datan// Initialize data>// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datae// Initialize datar// Initialize data.// Initialize datai// Initialize datan// Initialize datan// Initialize datae// Initialize datar// Initialize dataH// Initialize dataT// Initialize dataM// Initialize dataL// Initialize data // Initialize data=// Initialize data // Initialize dataa// Initialize datal// Initialize datae// Initialize datar// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datam// Initialize dataa// Initialize datap// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize dataS// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data(// Initialize datap// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datai// Initialize datas// Initialize dataC// Initialize datar// Initialize datai// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize datal// Initialize data // Initialize data=// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize datar// Initialize datai// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize datal// Initialize data'// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize datas// Initialize datat// Initialize dataa// Initialize datat// Initialize datau// Initialize datas// Initialize data.// Initialize datal// Initialize datae// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datae// Initialize datam// Initialize datap// Initialize datat// Initialize datay// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data // Initialize data`// Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datan// Initialize datao// Initialize datat// Initialize datai// Initialize dataf// Initialize datai// Initialize datac// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data-// Initialize datai// Initialize datat// Initialize datae// Initialize datam// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datan// Initialize datao// Initialize datat// Initialize datai// Initialize dataf// Initialize data-// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data // Initialize data$// Initialize data{// Initialize datai// Initialize datas// Initialize dataC// Initialize datar// Initialize datai// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize datal// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize datac// Initialize datar// Initialize datai// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize datal// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data}// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datai// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize dataf// Initialize dataa// Initialize data-// Initialize datas// Initialize datao// Initialize datal// Initialize datai// Initialize datad// Initialize data // Initialize dataf// Initialize dataa// Initialize data-// Initialize data$// Initialize data{// Initialize datai// Initialize datas// Initialize dataC// Initialize datar// Initialize datai// Initialize datat// Initialize datai// Initialize datac// Initialize dataa// Initialize datal// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize datac// Initialize datai// Initialize datar// Initialize datac// Initialize datal// Initialize datae// Initialize data-// Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize dataa// Initialize datam// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize datat// Initialize datar// Initialize datai// Initialize dataa// Initialize datan// Initialize datag// Initialize datal// Initialize datae// Initialize data-// Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize dataa// Initialize datam// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data'// Initialize data}// Initialize data"// Initialize data>// Initialize data<// Initialize data/// Initialize datai// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datan// Initialize datao// Initialize datat// Initialize datai// Initialize dataf// Initialize data-// Initialize datai// Initialize datan// Initialize dataf// Initialize datao// Initialize data"// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datan// Initialize datao// Initialize datat// Initialize datai// Initialize dataf// Initialize data-// Initialize datat// Initialize datai// Initialize datat// Initialize datal// Initialize datae// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize datad// Initialize datai// Initialize datav// Initialize data // Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize data=// Initialize data"// Initialize datan// Initialize datao// Initialize datat// Initialize datai// Initialize dataf// Initialize data-// Initialize datad// Initialize datae// Initialize datas// Initialize datac// Initialize data"// Initialize data>// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data?// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datai// Initialize datac// Initialize datao// Initialize datan// Initialize data // Initialize data+// Initialize data // Initialize data'// Initialize data // Initialize data'// Initialize data // Initialize data+// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize data'// Initialize data}// Initialize data // Initialize dataâ// Initialize data€// Initialize data¢// Initialize data // Initialize dataE// Initialize datas// Initialize datat// Initialize datao// Initialize dataq// Initialize datau// Initialize datae// Initialize data:// Initialize data // Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize dataq// Initialize datau// Initialize dataa// Initialize datan// Initialize datat// Initialize datai// Initialize datat// Initialize datay// Initialize data}// Initialize data // Initialize datau// Initialize datan// Initialize data // Initialize data(// Initialize datam// Initialize dataÃ// Initialize data­// Initialize datan// Initialize data:// Initialize data // Initialize data$// Initialize data{// Initialize datap// Initialize data.// Initialize datam// Initialize datai// Initialize datan// Initialize dataS// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data}// Initialize data)// Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data> generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data<// Initialize data/// Initialize datad// Initialize datai// Initialize datav// Initialize data>// Initialize data`// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data.// Initialize dataj// Initialize datao// Initialize datai// Initialize datan// Initialize data(// Initialize data'// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datas// Initialize datae// Initialize datat// Initialize datau// Initialize datap// Initialize dataN// Initialize datao// Initialize datat// Initialize datai// Initialize dataf// Initialize datai// Initialize datac// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize datas// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datab// Initialize datat// Initialize datan// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datan// Initialize datao// Initialize datat// Initialize datai// Initialize dataf// Initialize datai// Initialize datac// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data-// Initialize datab// Initialize datat// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize dataa// Initialize datan// Initialize datae// Initialize datal// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datan// Initialize datao// Initialize datat// Initialize datai// Initialize dataf// Initialize datai// Initialize datac// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data-// Initialize datap// Initialize dataa// Initialize datan// Initialize datae// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataB// Initialize datat// Initialize datan// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize data-// Initialize datan// Initialize datao// Initialize datat// Initialize datai// Initialize dataf// Initialize datai// Initialize datac// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize datas// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datab// Initialize datat// Initialize datan// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize dataa// Initialize datan// Initialize datae// Initialize datal// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize datat// Initialize datao// Initialize datag// Initialize datag// Initialize datal// Initialize datae// Initialize data(// Initialize data'// Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize dataa// Initialize datan// Initialize datae// Initialize datal// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datas// Initialize data(// Initialize data'// Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize data'// Initialize data)// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataN// Initialize datao// Initialize datat// Initialize datai// Initialize dataf// Initialize datai// Initialize datac// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataB// Initialize datat// Initialize datan// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize dataa// Initialize datan// Initialize datae// Initialize datal// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize data(// Initialize data'// Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize data'// Initialize data)// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize datae// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize dataa// Initialize datan// Initialize datae// Initialize datal// Initialize data.// Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datas// Initialize data(// Initialize datae// Initialize data.// Initialize datat// Initialize dataa// Initialize datar// Initialize datag// Initialize datae// Initialize datat// Initialize data)// Initialize data // Initialize data&// Initialize data&// Initialize data // Initialize data!// Initialize datab// Initialize datat// Initialize datan// Initialize data.// Initialize datac// Initialize datao// Initialize datan// Initialize datat// Initialize dataa// Initialize datai// Initialize datan// Initialize datas// Initialize data(// Initialize datae// Initialize data.// Initialize datat// Initialize dataa// Initialize datar// Initialize datag// Initialize datae// Initialize datat// Initialize data)// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize dataa// Initialize datan// Initialize datae// Initialize datal// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize data(// Initialize data'// Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize dataG// Initialize dataL// Initialize dataO// Initialize dataB// Initialize dataA// Initialize dataL// Initialize data // Initialize dataS// Initialize dataE// Initialize dataA// Initialize dataR// Initialize dataC// Initialize dataH// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datas// Initialize datae// Initialize datat// Initialize datau// Initialize datap// Initialize dataG// Initialize datal// Initialize datao// Initialize datab// Initialize dataa// Initialize datal// Initialize dataS// Initialize datae// Initialize dataa// Initialize datar// Initialize datac// Initialize datah// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datai// Initialize datan// Initialize datap// Initialize datau// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datag// Initialize datal// Initialize datao// Initialize datab// Initialize dataa// Initialize datal// Initialize data-// Initialize datas// Initialize datae// Initialize dataa// Initialize datar// Initialize datac// Initialize datah// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize datan// Initialize datap// Initialize datau// Initialize datat// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datak// Initialize datae// Initialize datay// Initialize datad// Initialize datao// Initialize dataw// Initialize datan// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize datae// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datae// Initialize data.// Initialize datak// Initialize datae// Initialize datay// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize dataE// Initialize datan// Initialize datat// Initialize datae// Initialize datar// Initialize data'// Initialize data // Initialize data&// Initialize data&// Initialize data // Initialize datai// Initialize datan// Initialize datap// Initialize datau// Initialize datat// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data.// Initialize datat// Initialize datar// Initialize datai// Initialize datam// Initialize data(// Initialize data)// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datan// Initialize dataa// Initialize datav// Initialize datai// Initialize datag// Initialize dataa// Initialize datat// Initialize datae// Initialize dataT// Initialize datao// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datas// Initialize datae// Initialize dataa// Initialize datar// Initialize datac// Initialize datah// Initialize data'// Initialize data)// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datai// Initialize datan// Initialize datap// Initialize datau// Initialize datat// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data.// Initialize datat// Initialize datar// Initialize datai// Initialize datam// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize datan// Initialize datap// Initialize datau// Initialize datat// Initialize data.// Initialize datav// Initialize dataa// Initialize datal// Initialize datau// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize data'// Initialize data'// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize dataF// Initialize dataI// Initialize dataL// Initialize dataT// Initialize dataE// Initialize dataR// Initialize data // Initialize dataE// Initialize dataV// Initialize dataE// Initialize dataN// Initialize dataT// Initialize data // Initialize dataL// Initialize dataI// Initialize dataS// Initialize dataT// Initialize dataE// Initialize dataN// Initialize dataE// Initialize dataR// Initialize dataS// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datas// Initialize datae// Initialize datat// Initialize datau// Initialize datap// Initialize dataF// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize datas// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data[// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize datas// Initialize datae// Initialize dataa// Initialize datar// Initialize datac// Initialize datah// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data-// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data-// Initialize datas// Initialize datat// Initialize datao// Initialize datac// Initialize datak// Initialize data'// Initialize data]// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize datai// Initialize datad// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datae// Initialize datal// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datai// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datae// Initialize datal// Initialize data)// Initialize data // Initialize datae// Initialize datal// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize datae// Initialize datal// Initialize data.// Initialize datat// Initialize dataa// Initialize datag// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize dataS// Initialize dataE// Initialize dataL// Initialize dataE// Initialize dataC// Initialize dataT// Initialize data'// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize datac// Initialize datah// Initialize dataa// Initialize datan// Initialize datag// Initialize datae// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize datai// Initialize datan// Initialize datap// Initialize datau// Initialize datat// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data[// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datas// Initialize datae// Initialize dataa// Initialize datar// Initialize datac// Initialize datah// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data-// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data-// Initialize datas// Initialize datat// Initialize dataa// Initialize datar// Initialize datat// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data-// Initialize datae// Initialize datan// Initialize datad// Initialize data'// Initialize data]// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize datai// Initialize datad// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datae// Initialize datal// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datai// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datae// Initialize datal// Initialize data)// Initialize data // Initialize datae// Initialize datal// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize datae// Initialize datal// Initialize data.// Initialize datat// Initialize dataa// Initialize datag// Initialize dataN// Initialize dataa// Initialize datam// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize dataS// Initialize dataE// Initialize dataL// Initialize dataE// Initialize dataC// Initialize dataT// Initialize data'// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize datae// Initialize datal// Initialize data.// Initialize datat// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data'// Initialize data // Initialize data?// Initialize data // Initialize data'// Initialize datac// Initialize datah// Initialize dataa// Initialize datan// Initialize datag// Initialize datae// Initialize data'// Initialize data // Initialize data:// Initialize data // Initialize data'// Initialize datai// Initialize datan// Initialize datap// Initialize datau// Initialize datat// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize dataI// Initialize dataN// Initialize dataI// Initialize dataT// Initialize dataI// Initialize dataA// Initialize dataL// Initialize dataI// Initialize dataZ// Initialize dataA// Initialize dataT// Initialize dataI// Initialize dataO// Initialize dataN// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data=// Initialize data= generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datau// Initialize datap// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize dataD// Initialize datai// Initialize datas// Initialize datap// Initialize datal// Initialize dataa// Initialize datay// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datae// Initialize datal// Initialize data // Initialize data=// Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize datau// Initialize datar// Initialize datar// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datae// Initialize datal// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datae// Initialize datal// Initialize data.// Initialize datat// Initialize datae// Initialize datax// Initialize datat// Initialize dataC// Initialize datao// Initialize datan// Initialize datat// Initialize datae// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datae// Initialize dataw// Initialize data // Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data.// Initialize datat// Initialize datao// Initialize dataL// Initialize datao// Initialize datac// Initialize dataa// Initialize datal// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize dataS// Initialize datat// Initialize datar// Initialize datai// Initialize datan// Initialize datag// Initialize data(// Initialize data'// Initialize datap// Initialize datat// Initialize data-// Initialize dataB// Initialize dataR// Initialize data'// Initialize data,// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize dataw// Initialize datae// Initialize datae// Initialize datak// Initialize datad// Initialize dataa// Initialize datay// Initialize data:// Initialize data // Initialize data'// Initialize datas// Initialize datah// Initialize datao// Initialize datar// Initialize datat// Initialize data'// Initialize data,// Initialize data // Initialize datad// Initialize dataa// Initialize datay// Initialize data:// Initialize data // Initialize data'// Initialize data2// Initialize data-// Initialize datad// Initialize datai// Initialize datag// Initialize datai// Initialize datat// Initialize data'// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datan// Initialize datat// Initialize datah// Initialize data:// Initialize data // Initialize data'// Initialize datas// Initialize datah// Initialize datao// Initialize datar// Initialize datat// Initialize data'// Initialize data,// Initialize data // Initialize datay// Initialize datae// Initialize dataa// Initialize datar// Initialize data:// Initialize data // Initialize data'// Initialize datan// Initialize datau// Initialize datam// Initialize datae// Initialize datar// Initialize datai// Initialize datac// Initialize data' generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize data\// Initialize datar// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize data\// Initialize datan// Initialize data\// Initialize datan// Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datai// Initialize datan// Initialize datai// Initialize datat// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataS// Initialize datae// Initialize datat// Initialize datau// Initialize datap// Initialize data // Initialize datac// Initialize datao// Initialize datar// Initialize datae// Initialize data // Initialize dataU// Initialize dataI generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datae// Initialize datat// Initialize datau// Initialize datap// Initialize dataS// Initialize datai// Initialize datad// Initialize datae// Initialize datab// Initialize dataa// Initialize datar// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datae// Initialize datat// Initialize datau// Initialize datap// Initialize dataN// Initialize dataa// Initialize datav// Initialize datai// Initialize datag// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datae// Initialize datat// Initialize datau// Initialize datap// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize dataC// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataH// Initialize dataa// Initialize datan// Initialize datad// Initialize datal// Initialize datae// Initialize datar// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datae// Initialize datat// Initialize datau// Initialize datap// Initialize dataN// Initialize datao// Initialize datat// Initialize datai// Initialize dataf// Initialize datai// Initialize datac// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datae// Initialize datat// Initialize datau// Initialize datap// Initialize dataG// Initialize datal// Initialize datao// Initialize datab// Initialize dataa// Initialize datal// Initialize dataS// Initialize datae// Initialize dataa// Initialize datar// Initialize datac// Initialize datah// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datae// Initialize datat// Initialize datau// Initialize datap// Initialize dataF// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datau// Initialize datap// Initialize datad// Initialize dataa// Initialize datat// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize datae// Initialize dataD// Initialize datai// Initialize datas// Initialize datap// Initialize datal// Initialize dataa// Initialize datay// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataF// Initialize datao// Initialize datar// Initialize datam// Initialize data // Initialize datas// Initialize datau// Initialize datab// Initialize datam// Initialize datai// Initialize datas// Initialize datas// Initialize datai// Initialize datao// Initialize datan// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data-// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize data'// Initialize data)// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datas// Initialize datau// Initialize datab// Initialize datam// Initialize datai// Initialize datat// Initialize data'// Initialize data,// Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data-// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize data'// Initialize data)// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datas// Initialize datau// Initialize datab// Initialize datam// Initialize datai// Initialize datat// Initialize data'// Initialize data,// Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data-// Initialize dataf// Initialize datao// Initialize datar// Initialize datam// Initialize data'// Initialize data)// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datas// Initialize datau// Initialize datab// Initialize datam// Initialize datai// Initialize datat// Initialize data'// Initialize data,// Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataB// Initialize datau// Initialize datat// Initialize datat// Initialize datao// Initialize datan// Initialize data // Initialize datah// Initialize dataa// Initialize datan// Initialize datad// Initialize datal// Initialize datae// Initialize datar// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datab// Initialize datat// Initialize datan// Initialize data-// Initialize dataa// Initialize datad// Initialize datad// Initialize data-// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data)// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datab// Initialize datat// Initialize datan// Initialize data-// Initialize dataa// Initialize datad// Initialize datad// Initialize data-// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data)// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datab// Initialize datat// Initialize datan// Initialize data-// Initialize datan// Initialize datae// Initialize dataw// Initialize data-// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datae// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datab// Initialize datat// Initialize datan// Initialize data-// Initialize datan// Initialize datae// Initialize dataw// Initialize data-// Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize data'// Initialize data)// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataM// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datae// Initialize datax// Initialize datai// Initialize datat// Initialize data'// Initialize data)// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datab// Initialize datat// Initialize datan// Initialize data-// Initialize datae// Initialize datax// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize data-// Initialize datac// Initialize datas// Initialize datav// Initialize data'// Initialize data)// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize datae// Initialize datax// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataC// Initialize dataS// Initialize dataV// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datad// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data-// Initialize datab// Initialize datat// Initialize datan// Initialize data'// Initialize data)// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data'// Initialize data,// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize data // Initialize datai// Initialize datan// Initialize datai// Initialize datat// Initialize datai// Initialize dataa// Initialize datal// Initialize data // Initialize datap// Initialize dataa// Initialize datag// Initialize datae generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataD// Initialize dataa// Initialize datas// Initialize datah// Initialize datab// Initialize datao// Initialize dataa// Initialize datar// Initialize datad// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataK// Initialize datae// Initialize datay// Initialize datab// Initialize datao// Initialize dataa// Initialize datar// Initialize datad// Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize datar// Initialize datat// Initialize datac// Initialize datau// Initialize datat// Initialize data:// Initialize data // Initialize dataE// Initialize datas// Initialize datac// Initialize dataa// Initialize datap// Initialize datae// Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize datas// Initialize data // Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize datak// Initialize datae// Initialize datay// Initialize datad// Initialize datao// Initialize dataw// Initialize datan// Initialize data'// Initialize data,// Initialize data // Initialize data(// Initialize datae// Initialize data)// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datae// Initialize data.// Initialize datak// Initialize datae// Initialize datay// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize dataE// Initialize datas// Initialize datac// Initialize dataa// Initialize datap// Initialize datae// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize dataq// Initialize datau// Initialize datae// Initialize datar// Initialize datay// Initialize dataS// Initialize datae// Initialize datal// Initialize datae// Initialize datac// Initialize datat// Initialize datao// Initialize datar// Initialize dataA// Initialize datal// Initialize datal// Initialize data(// Initialize data'// Initialize data.// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data-// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize datal// Initialize dataa// Initialize datay// Initialize data.// Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize data'// Initialize data)// Initialize data.// Initialize dataf// Initialize datao// Initialize datar// Initialize dataE// Initialize dataa// Initialize datac// Initialize datah// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize datam// Initialize data.// Initialize datai// Initialize datad// Initialize data)// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize datag// Initialize datae// Initialize datat// Initialize dataE// Initialize datal// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize data'// Initialize datan// Initialize datao// Initialize datat// Initialize datai// Initialize dataf// Initialize datai// Initialize datac// Initialize dataa// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data-// Initialize datap// Initialize dataa// Initialize datan// Initialize datae// Initialize datal// Initialize data'// Initialize data)// Initialize data.// Initialize datac// Initialize datal// Initialize dataa// Initialize datas// Initialize datas// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize data.// Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize data(// Initialize data'// Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data/// Initialize data/// Initialize data // Initialize dataG// Initialize datal// Initialize datao// Initialize datab// Initialize dataa// Initialize datal// Initialize data // Initialize dataf// Initialize datau// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize data // Initialize datae// Initialize datax// Initialize datae// Initialize datac// Initialize datau// Initialize datat// Initialize datae// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize data(// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data|// Initialize data|// Initialize data // Initialize data!// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data)// Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data.// Initialize datal// Initialize datae// Initialize datan// Initialize datag// Initialize datat// Initialize datah// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data // Initialize data>// Initialize data // Initialize data0// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataN// Initialize dataã// Initialize datao// Initialize data // Initialize dataé// Initialize data // Initialize datap// Initialize datao// Initialize datas// Initialize datas// Initialize dataí// Initialize datav// Initialize datae// Initialize datal// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize datai// Initialize datar// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize data—// Initialize data // Initialize datae// Initialize datax// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datam// Initialize data // Initialize data$// Initialize data{// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize dataC// Initialize datao// Initialize datau// Initialize datan// Initialize datat// Initialize data}// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize dataa// Initialize dataç// Initialize dataã// Initialize datao// Initialize data(// Initialize dataõ// Initialize datae// Initialize datas// Initialize data)// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize dataa// Initialize data(// Initialize datas// Initialize data)// Initialize data.// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datat// Initialize datau// Initialize datar// Initialize datan// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data/// Initialize data/// Initialize data // Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datar// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize dataa// Initialize datas// Initialize datas// Initialize datao// Initialize datac// Initialize datai// Initialize dataa// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize data(// Initialize datao// Initialize datap// Initialize datac// Initialize datai// Initialize datao// Initialize datan// Initialize dataa// Initialize datal// Initialize data)// Initialize data // Initialize data-// Initialize data // Initialize datam// Initialize dataa// Initialize datan// Initialize datat// Initialize datae// Initialize datam// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize data // Initialize datac// Initialize datao// Initialize datam// Initialize datap// Initialize datao// Initialize datar// Initialize datat// Initialize dataa// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize data // Initialize datao// Initialize datar// Initialize datai// Initialize datag// Initialize datai// Initialize datan// Initialize dataa// Initialize datal// Initialize data // Initialize datar// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datan// Initialize datad// Initialize datao// Initialize data // Initialize datat// Initialize dataa// Initialize datam// Initialize datab// Initialize dataé// Initialize datam// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datai// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datao// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datam// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datam// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize dataI// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data,// Initialize data // Initialize datam// Initialize datao// Initialize datav// Initialize datae// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize datao// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize data'// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data'// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datag// Initialize datae// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataB// Initialize datay// Initialize dataI// Initialize datad// Initialize data(// Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize datas// Initialize datat// Initialize data // Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data.// Initialize datas// Initialize datao// Initialize datam// Initialize datae// Initialize data(// Initialize datap// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datap// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datai// Initialize dataf// Initialize data // Initialize data(// Initialize datah// Initialize dataa// Initialize datas// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize datas// Initialize data)// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data'// Initialize dataR// Initialize datae// Initialize datam// Initialize datao// Initialize datav// Initialize dataa// Initialize data // Initialize datat// Initialize datao// Initialize datad// Initialize datao// Initialize datas// Initialize data // Initialize datao// Initialize datas// Initialize data // Initialize datap// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datat// Initialize datao// Initialize datas// Initialize data // Initialize datad// Initialize datae// Initialize datas// Initialize datat// Initialize dataa// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize datap// Initialize datar// Initialize datai// Initialize datam// Initialize datae// Initialize datai// Initialize datar// Initialize datao// Initialize data!// Initialize data'// Initialize data,// Initialize data // Initialize data'// Initialize datae// Initialize datar// Initialize datar// Initialize datao// Initialize datar// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data}// Initialize data // Initialize datae// Initialize datal// Initialize datas// Initialize datae// Initialize data // Initialize data{ generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data.// Initialize dataf// Initialize datai// Initialize datal// Initialize datat// Initialize datae// Initialize datar// Initialize data(// Initialize datac// Initialize data // Initialize data=// Initialize data>// Initialize data // Initialize datac// Initialize data.// Initialize datai// Initialize datad// Initialize data // Initialize data!// Initialize data=// Initialize data=// Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize dataa// Initialize datav// Initialize datae// Initialize dataD// Initialize dataa// Initialize datat// Initialize dataa// Initialize data(// Initialize dataS// Initialize dataT// Initialize dataO// Initialize dataR// Initialize dataA// Initialize dataG// Initialize dataE// Initialize data_// Initialize dataK// Initialize dataE// Initialize dataY// Initialize dataS// Initialize data.// Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data,// Initialize data // Initialize datac// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datas// Initialize datah// Initialize datao// Initialize dataw// Initialize dataT// Initialize datao// Initialize dataa// Initialize datas// Initialize datat// Initialize data(// Initialize data`// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize dataa// Initialize data // Initialize data"// Initialize data$// Initialize data{// Initialize datac// Initialize dataa// Initialize datat// Initialize data?// Initialize data.// Initialize datan// Initialize dataa// Initialize datam// Initialize datae// Initialize data}// Initialize data"// Initialize data // Initialize datae// Initialize datax// Initialize datac// Initialize datal// Initialize datau// Initialize dataí// Initialize datad// Initialize dataa// Initialize data`// Initialize data,// Initialize data // Initialize data'// Initialize dataw// Initialize dataa// Initialize datar// Initialize datan// Initialize datai// Initialize datan// Initialize datag// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize datar// Initialize datae// Initialize datan// Initialize datad// Initialize datae// Initialize datar// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datai// Initialize datae// Initialize datas// Initialize data(// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize data} generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataI// Initialize datad// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datap// Initialize datae// Initialize datan// Initialize datad// Initialize datai// Initialize datan// Initialize datag// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataT// Initialize datay// Initialize datap// Initialize datae// Initialize data // Initialize data=// Initialize data // Initialize datan// Initialize datau// Initialize datal// Initialize datal// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data // Initialize data // Initialize data // Initialize data // Initialize datac// Initialize datal// Initialize datao// Initialize datas// Initialize datae// Initialize dataM// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data(// Initialize data'// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize data-// Initialize datam// Initialize datao// Initialize datad// Initialize dataa// Initialize datal// Initialize data'// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data}// Initialize datan// Initialize datac// Initialize datat// Initialize datai// Initialize datao// Initialize datan// Initialize datas// Initialize data // Initialize dataf// Initialize datao// Initialize datar// Initialize data // Initialize datai// Initialize datan// Initialize datal// Initialize datai// Initialize datan// Initialize datae// Initialize data // Initialize datao// Initialize datan// Initialize datac// Initialize datal// Initialize datai// Initialize datac// Initialize datak// Initialize data // Initialize datah// Initialize dataa// Initialize datan// Initialize datad// Initialize datal// Initialize datae// Initialize datar// Initialize datas generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataw// Initialize datai// Initialize datan// Initialize datad// Initialize datao// Initialize dataw// Initialize data.// Initialize datae// Initialize datad// Initialize datai// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datae// Initialize datad// Initialize datai// Initialize datat// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataw// Initialize datai// Initialize datan// Initialize datad// Initialize datao// Initialize dataw// Initialize data.// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataP// Initialize datar// Initialize datao// Initialize datad// Initialize datau// Initialize datac// Initialize datat// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataw// Initialize datai// Initialize datan// Initialize datad// Initialize datao// Initialize dataw// Initialize data.// Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataQ// Initialize datau// Initialize datai// Initialize datac// Initialize datak// Initialize dataE// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data // Initialize data=// Initialize data // Initialize datao// Initialize datap// Initialize datae// Initialize datan// Initialize dataQ// Initialize datau// Initialize datai// Initialize datac// Initialize datak// Initialize dataE// Initialize datan// Initialize datat// Initialize datar// Initialize datay// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataw// Initialize datai// Initialize datan// Initialize datad// Initialize datao// Initialize dataw// Initialize data.// Initialize datae// Initialize datad// Initialize datai// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data // Initialize data=// Initialize data // Initialize datae// Initialize datad// Initialize datai// Initialize datat// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize dataw// Initialize datai// Initialize datan// Initialize datad// Initialize datao// Initialize dataw// Initialize data.// Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data // Initialize data=// Initialize data // Initialize datac// Initialize datao// Initialize datan// Initialize dataf// Initialize datai// Initialize datar// Initialize datam// Initialize dataD// Initialize datae// Initialize datal// Initialize datae// Initialize datat// Initialize datae// Initialize dataC// Initialize dataa// Initialize datat// Initialize datae// Initialize datag// Initialize datao// Initialize datar// Initialize datay// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize datad// Initialize datao// Initialize datac// Initialize datau// Initialize datam// Initialize datae// Initialize datan// Initialize datat// Initialize data.// Initialize dataa// Initialize datad// Initialize datad// Initialize dataE// Initialize datav// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datai// Initialize datas// Initialize datat// Initialize datae// Initialize datan// Initialize datae// Initialize datar// Initialize data(// Initialize data'// Initialize dataD// Initialize dataO// Initialize dataM// Initialize dataC// Initialize datao// Initialize datan// Initialize datat// Initialize datae// Initialize datan// Initialize datat// Initialize dataL// Initialize datao// Initialize dataa// Initialize datad// Initialize datae// Initialize datad// Initialize data'// Initialize data,// Initialize data // Initialize datai// Initialize datan// Initialize datai// Initialize datat// Initialize data)// Initialize data; generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

 generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data generateId() {
    // Mais robusto: combina timestamp + duas partes aleatórias para evitar colisões
    const timestamp = Date.now().toString(36);
    const randomPart = Math.random().toString(36).substring(2, 8);
    const microsPart = Math.floor(Math.random() * 1000000).toString(36);
    return 'id-' + timestamp + '-' + randomPart + '-' + microsPart;
}

// Initialize data
